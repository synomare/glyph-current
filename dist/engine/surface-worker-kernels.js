var SURFACE_OPACITY_KEYS = {
        rationalCusp: 'rationalCuspOpacity',
        bladeBody: 'bladeBodyOpacity',
        blackBastion: 'blackBastionOpacity',
        inktrapAbyss: 'inktrapAbyssOpacity',
        strangulation: 'strangulationOpacity',
        gillArray: 'gillArrayOpacity',
        screwExtrusion: 'screwExtrusionOpacity',
        harmonicCage: 'harmonicCageOpacity',
        beltramiFlow: 'beltramiFlowOpacity',
        riemannRamification: 'riemannRamificationOpacity',
        linkedTwist: 'linkedTwistOpacity',
        enneperBody: 'enneperBodyOpacity',
        nibRecast: 'nibRecastOpacity',
        anatomyWarp: 'anatomyWarpOpacity',
        kreslingShell: 'kreslingShellOpacity',
        growthBuckle: 'growthBuckleOpacity',
        inversionBody: 'inversionBodyOpacity',
        gravityLens: 'gravityLensOpacity',
        liquidRope: 'liquidRopeOpacity',
        wulffBody: 'wulffBodyOpacity',
        repulsiveCurves: 'repulsiveCurvesOpacity',
        wassersteinLetters: 'wassersteinLettersOpacity',
        stressGlass: 'stressGlassOpacity',
        vortexBath: 'vortexBathOpacity',
        miuraVault: 'miuraVaultOpacity',
        hopfLoom: 'hopfLoomOpacity',
        densityRecast: 'densityRecastOpacity',
        spinodalAlloy: 'spinodalAlloyOpacity',
        nodalGlaze: 'nodalGlazeOpacity',
        loadpathFoundry: 'loadpathFoundryOpacity',
        hyperbolicAtlas: 'hyperbolicAtlasOpacity',
        quasicrystalBody: 'quasicrystalBodyOpacity',
        nematicFilm: 'nematicFilmOpacity',
        diffractiveGlyph: 'diffractiveGlyphOpacity',
        dendriteCast: 'dendriteCastOpacity',
        ornamentReserve: 'ornamentReserveOpacity',
        tensionMembrane: 'tensionMembraneOpacity',
        tensorFiligree: 'tensorFiligreeOpacity',
        chromaticSwarm: 'chromaticSwarmOpacity',
        gyroidSculpture: 'gyroidSculptureOpacity',
        causticGlass: 'causticGlassOpacity',
        scrollType: 'scrollOpacity',
        anamorphicType: 'anamorphicOpacity',
        contextualFit: 'contextualOpacity',
        plotterTrace: 'traceOpacity', webbing: 'webOpacity', blobTrack: 'blobOpacity', softBlob: 'softBlobOpacity', etchantBloom: 'etchantOpacity',
        calligraphicStress: 'calligraphyOpacity', asemicDuctus: 'asemicOpacity', spectralType: 'spectralOpacity', differentialType: 'differentialOpacity', conformalType: 'conformalOpacity', auxeticType: 'auxeticOpacity', marblingType: 'marblingOpacity', counterformEngine: 'counterformOpacity', terminalExcess: 'terminalOpacity', ligatureBody: 'ligatureBodyOpacity',
        fiberBody: 'fiberOpacity', glyphMutation: 'mutationOpacity', suspendedSyntax: 'suspensionOpacity', livingTextField: 'livingOpacity', innerEruption: 'eruptionOpacity', recursiveGraft: 'graftOpacity', structuralCollision: 'collisionOpacity', peelWeave: 'peelOpacity', voidPressure: 'voidPressureOpacity',
        rubyUsurper: 'rubyOpacity', punctuationLoom: 'punctuationLoomOpacity', counterpage: 'counterpageOpacity', rendererDebt: 'rendererDebtOpacity', ligatureContagion: 'contagionOpacity', strokeCommons: 'commonsOpacity',
        sigilForge: 'sigilOpacity', thornCrown: 'thornOpacity', cipherLiturgy: 'cipherOpacity',
        boneScaffold: 'boneOpacity', roseEngine: 'roseOpacity', chromeReliquary: 'chromeOpacity',
        ligatureCrypt: 'ligatureOpacity', moireChoir: 'moireOpacity', naveCutter: 'naveOpacity',
        cloisterFold: 'cloisterOpacity', prismSacrament: 'prismOpacity', texturaMatrix: 'texturaOpacity',
        voidPortal: 'voidOpacity', recursiveShrine: 'recursiveOpacity', morphProcession: 'morphOpacity', chimeraGraft: 'chimeraOpacity',
        monolithCast: 'monolithOpacity',
        rasterPress: 'rasterOpacity', hatchEngrave: 'hatchOpacity', contourEtch: 'contourOpacity',
        pressureStroke: 'pressureOpacity', sinewTorque: 'sinewOpacity', cellFracture: 'fractureOpacity', ribbonEcho: 'ribbonOpacity',
        copyDecay: 'copyOpacity', risoSeparation: 'risoOpacity', misregistration: 'misregOpacity', dataMosh: 'dataMoshOpacity', slitSweep: 'slitOpacity'
      };

var SURFACE_SOURCE_OPACITY_KEYS = {
        rationalCusp: 'rationalCuspSourceOpacity',
        bladeBody: 'bladeBodySourceOpacity',
        blackBastion: 'blackBastionSourceOpacity',
        inktrapAbyss: 'inktrapAbyssSourceOpacity',
        strangulation: 'strangulationSourceOpacity',
        gillArray: 'gillArraySourceOpacity',
        screwExtrusion: 'screwExtrusionSourceOpacity',
        harmonicCage: 'harmonicCageSourceOpacity',
        beltramiFlow: 'beltramiFlowSourceOpacity',
        riemannRamification: 'riemannRamificationSourceOpacity',
        linkedTwist: 'linkedTwistSourceOpacity',
        enneperBody: 'enneperBodySourceOpacity',
        nibRecast: 'nibRecastSourceOpacity',
        anatomyWarp: 'anatomyWarpSourceOpacity',
        kreslingShell: 'kreslingShellSourceOpacity',
        growthBuckle: 'growthBuckleSourceOpacity',
        inversionBody: 'inversionBodySourceOpacity',
        gravityLens: 'gravityLensSourceOpacity',
        liquidRope: 'liquidRopeSourceOpacity',
        wulffBody: 'wulffBodySourceOpacity',
        repulsiveCurves: 'repulsiveCurvesSourceOpacity',
        wassersteinLetters: 'wassersteinLettersSourceOpacity',
        stressGlass: 'stressGlassSourceOpacity',
        vortexBath: 'vortexBathSourceOpacity',
        miuraVault: 'miuraVaultSourceOpacity',
        hopfLoom: 'hopfLoomSourceOpacity',
        densityRecast: 'densityRecastSourceOpacity',
        spinodalAlloy: 'spinodalAlloySourceOpacity',
        nodalGlaze: 'nodalGlazeSourceOpacity',
        loadpathFoundry: 'loadpathFoundrySourceOpacity',
        hyperbolicAtlas: 'hyperbolicAtlasSourceOpacity',
        quasicrystalBody: 'quasicrystalBodySourceOpacity',
        nematicFilm: 'nematicFilmSourceOpacity',
        diffractiveGlyph: 'diffractiveGlyphSourceOpacity',
        dendriteCast: 'dendriteCastSourceOpacity',
        ornamentReserve: 'ornamentReserveSourceOpacity',
        tensionMembrane: 'tensionMembraneSourceOpacity',
        tensorFiligree: 'tensorFiligreeSourceOpacity',
        chromaticSwarm: 'chromaticSwarmSourceOpacity',
        gyroidSculpture: 'gyroidSculptureSourceOpacity',
        causticGlass: 'causticGlassSourceOpacity',
        scrollType: 'scrollSourceOpacity',
        anamorphicType: 'anamorphicSourceOpacity',
        contextualFit: 'contextualSourceOpacity',
        plotterTrace: 'traceSourceOpacity', webbing: 'webSourceOpacity', blobTrack: 'blobSourceOpacity', softBlob: 'softBlobSourceOpacity', etchantBloom: 'etchantSourceOpacity',
        calligraphicStress: 'calligraphySourceOpacity', asemicDuctus: 'asemicSourceOpacity', spectralType: 'spectralSourceOpacity', differentialType: 'differentialSourceOpacity', conformalType: 'conformalSourceOpacity', auxeticType: 'auxeticSourceOpacity', marblingType: 'marblingSourceOpacity', counterformEngine: 'counterformSourceOpacity', terminalExcess: 'terminalSourceOpacity', ligatureBody: 'ligatureBodySourceOpacity',
        fiberBody: 'fiberSourceOpacity', glyphMutation: 'mutationSourceOpacity', suspendedSyntax: 'suspensionSourceOpacity', livingTextField: 'livingSourceOpacity', innerEruption: 'eruptionSourceOpacity', recursiveGraft: 'graftSourceOpacity', structuralCollision: 'collisionSourceOpacity', peelWeave: 'peelSourceOpacity', voidPressure: 'voidPressureSourceOpacity',
        rubyUsurper: 'rubySourceOpacity', punctuationLoom: 'punctuationLoomSourceOpacity', counterpage: 'counterpageSourceOpacity', rendererDebt: 'rendererDebtSourceOpacity', ligatureContagion: 'contagionSourceOpacity', strokeCommons: 'commonsSourceOpacity',
        sigilForge: 'sigilSourceOpacity', thornCrown: 'thornSourceOpacity', cipherLiturgy: 'cipherSourceOpacity',
        boneScaffold: 'boneSourceOpacity', roseEngine: 'roseSourceOpacity', chromeReliquary: 'chromeSourceOpacity',
        ligatureCrypt: 'ligatureSourceOpacity', moireChoir: 'moireSourceOpacity', naveCutter: 'naveSourceOpacity',
        cloisterFold: 'cloisterSourceOpacity', prismSacrament: 'prismSourceOpacity', texturaMatrix: 'texturaSourceOpacity',
        voidPortal: 'voidSourceOpacity', recursiveShrine: 'recursiveSourceOpacity', morphProcession: 'morphSourceOpacity', chimeraGraft: 'chimeraSourceOpacity',
        monolithCast: 'monolithSourceOpacity',
        rasterPress: 'rasterSourceOpacity', hatchEngrave: 'hatchSourceOpacity', contourEtch: 'contourSourceOpacity',
        pressureStroke: 'pressureSourceOpacity', sinewTorque: 'sinewSourceOpacity', cellFracture: 'fractureSourceOpacity', ribbonEcho: 'ribbonSourceOpacity',
        copyDecay: 'copySourceOpacity', risoSeparation: 'risoSourceOpacity', misregistration: 'misregSourceOpacity', dataMosh: 'dataMoshSourceOpacity', slitSweep: 'slitSourceOpacity'
      };

var BATCH_PARAM_OPTIONS = {
        bladeSide: ['single', 'double'],
        abyssPierce: ['stop', 'pierce'],
        cageMode: ['opposed', 'fan', 'shear'],
        beltramiMode: ['vortex', 'saddle', 'braid'],
        kreslingMode: ['helix', 'counter'],
        growthMode: ['rim', 'bands', 'spots'],
        lensMode: ['single', 'binary'],
        ropeHistory: ['accelerate', 'decelerate'],
        wassersteinView: ['ink', 'density', 'tracks'],
        stressMode: ['bridge', 'cantilever', 'shear'],

        vortexMode: ['lift', 'shear', 'stir'],

        miuraMode: ['sheet', 'fretwork', 'layers'],

        hopfMode: ['shell', 'lamina', 'fibers'],

        recastForm: ['disk', 'annulus', 'channel'],

        alloyMode: ['duplex', 'wetting', 'drawn'],

        nodalBoundary: ['free', 'clamped', 'elastic'],

        foundryMode: ['bridge', 'cantilever', 'shear'],

        atlasMode: ['pentagon', 'square', 'triangle'],

        quasiMode: ['pentagrid', 'octagrid', 'heptagrid'],
        nematicMode: ['crossed', 'compensated', 'parallel'],

        diffractionMode: ['aperture', 'relief', 'grating'],
        dendriteMode: ['coral', 'frost', 'copper'],

        reserveMode: ['ribbon', 'lace', 'circuit'],
        membraneMode: ['canopy', 'drape', 'twist'],

        tensorMode: ['contour', 'vortex', 'braid'],
        swarmMark: ['rosette', 'orbit', 'cross'],
        gyroidMode: ['gyroid', 'diamond', 'primitive'],
        causticMode: ['lens', 'fluted', 'ripple'],

        counterShape: ['ellipse', 'rect', 'river'], concordanceDirection: ['center', 'left', 'right', 'alternate'],
        axisFieldDistribution: ['sequence', 'mirror', 'wave', 'steps'],
        scrollMode: ['spiral', 'roll', 'reverse'], scrollFinish: ['classic', 'studio'],
        anamorphicFinish: ['classic', 'studio', 'silhouette'],
        contextualAxis: ['horizontal', 'vertical', 'both'], contextualSide: ['both', 'before', 'after'], contextualBaseline: ['locked', 'free'],
        rotatePattern: ['uniform', 'alternate', 'wave', 'unicode'],
        skewPattern: ['uniform', 'alternate', 'wave', 'unicode'],
        baselinePattern: ['uniform', 'wave', 'triangle', 'unicode'],
        currentFlow: ['inflection', 'crest'],
        readingScope: ['paragraph', 'page', 'spread', 'document'],
        caesuraGrammar: ['aperture', 'terrace', 'hinge'],
        mirrorPattern: ['all', 'alternate', 'blocks', 'words', 'unicode'],
        misregSystem: ['legacy', 'paperStretch', 'slur', 'doubling', 'trap'],
        moshGrammar: ['legacy', 'prediction', 'motionVectors', 'entropy', 'keyframe'],
        blobVisual: ['legacy', 'flow', 'contours', 'mesh', 'diagnostic'],
        traceGrammar: ['legacy', 'circuit', 'relay', 'carriage', 'duet'],
        webTopology: ['legacy', 'tensile', 'radial', 'braided', 'membrane'],
        etchantRegime: ['legacy', 'labyrinth', 'mitosis', 'coral', 'crystal'],
        calligraphyTool: ['legacy', 'broad', 'brush', 'split', 'chisel'],
        calligraphyLegacyTool: ['broad', 'brush', 'split', 'chisel'],
        asemicGrammar: ['current', 'chamber', 'incised', 'polyphonic'],
        spectralMode: ['liquid', 'fluted', 'phase'],
        marblingMode: ['rake', 'eddy', 'plume'],
        counterformGrammar: ['legacy', 'apertureV29', 'stencilV29', 'trapV29', 'channelV29', 'aperture', 'stencil', 'trap', 'channel'],
        counterformLegacyGrammar: ['aperture', 'stencil', 'trap', 'channel'],
        terminalStyle: ['legacy', 'wedge', 'flare', 'ball', 'blade'],
        terminalLegacyStyle: ['wedge', 'flare', 'ball', 'blade'],
        ligatureBodyGrammar: ['legacy', 'counterbody', 'interlock', 'sharedStem', 'counterWeave', 'melt'],
        ligatureBodyLegacyGrammar: ['interlock', 'sharedStem', 'counterWeave', 'melt'],
        sigilTopology: ['legacy', 'cartouche', 'constellation', 'radial', 'branching'],
        thornGrammar: ['hybrid', 'lancet', 'hook', 'vine', 'trident'],
        thornConstruction: ['rooted', 'legacy'],
        cipherDialect: ['mixed', 'unicode', 'machine', 'ledger', 'ritual'],
        boneArchitecture: ['trabecular', 'adaptive', 'spine', 'ribcage', 'truss', 'lamellar'],
        roseCore: ['auto', 'oculus', 'foil', 'compass', 'knot', 'rosette'],
        chromeModel: ['acid', 'studio'],
        ligatureGrammar: ['ribbon', 'joinery', 'blackletter', 'suture'],
        moireLattice: ['linear', 'choral', 'concentric', 'radial', 'interlace'],
        navePlan: ['arcade', 'ribbed', 'fan', 'loop', 'clerestory'],
        cloisterTessellation: ['strips', 'miura', 'accordion', 'diamond', 'fan'],
        prismOptics: ['legacy', 'crystal', 'birefringent', 'fresnel', 'spectral', 'lenticular'],
        texturaGrammar: ['stems', 'textura', 'ductus', 'fraktur', 'bastarda', 'lattice'],
        voidArchitecture: ['legacy', 'archivolt', 'carceri', 'horizon'],
        recursiveStructure: ['legacy', 'reliquary', 'tracery', 'cantor', 'mandorla'],
        morphChoreography: ['legacy', 'corridor', 'braid', 'cascade', 'membrane'],
        chimeraSystem: ['legacy', 'transplant', 'anastomosis', 'mosaic', 'symbiosis'],
        fiberGrammar: ['microtype', 'cable', 'mycelial'],
        mutationGrammar: ['sector', 'fault', 'organ'],
        suspensionGrammar: ['catenary', 'gantry', 'mobile'],
        livingGrammar: ['particles', 'lexemes', 'constellation'],
        eruptionGrammar: ['lamina', 'shard', 'plume'],
        graftGrammar: ['dendrite', 'coral', 'splice'],
        collisionSystem: ['opposed', 'radial', 'stack'], collisionReaction: ['weld', 'void', 'flare'],
        peelGrammar: ['lamina', 'braid', 'knot'],
        voidPressureMode: ['counter', 'gutter', 'breach'],
        rubyGrammar: ['interlinear', 'branching', 'canopy'],
        punctuationAnchors: ['pairs', 'nested', 'terminals'],
        counterpageMode: ['counter', 'gutter', 'shared'],
        rendererPath: ['chain', 'drag', 'spill'],
        contagionTrait: ['bifurcate', 'curl', 'counter'],
        commonsAllocation: ['focus', 'wave', 'reciprocal'],
        monolithGrammar: ['legacy', 'strata', 'ashlar', 'vault', 'stereotomy'],
        sinewSystem: ['legacy', 'torsion', 'fascicle', 'compression', 'braid'],
        rasterScreen: ['legacy', 'adaptiveV29', 'stochasticV29', 'lineV29', 'mezzotintV29', 'gravure', 'adaptive', 'stochastic', 'line', 'mezzotint'],
        hatchGrammar: ['legacy', 'tonalV29', 'crosscutV29', 'burinV29', 'woodcutV29', 'tonal', 'crosscut', 'burin', 'woodcut', 'copperplate'],
        contourGrammar: ['legacy', 'isobars', 'index', 'terraces', 'watershed', 'relief'],
        pressureSystem: ['legacy', 'gesture', 'bristle', 'pool', 'flyingWhite'],
        fractureTopology: ['triangles', 'impact', 'fault', 'crystal', 'spall'],
        ribbonPath: ['legacy', 'extrude', 'helix', 'fan', 'braid', 'lamina'],
        risoPlateMap: ['legacy', 'field', 'area', 'edge', 'halftone', 'duotone'],
        copyFailure: ['legacy', 'generationalV29', 'bandingV29', 'ghostingV29', 'residualV29', 'generational', 'banding', 'ghosting', 'residual', 'transfer'],
        slitTimeMap: ['legacy', 'wave', 'staircase', 'fold', 'feedback']
      };

var uni = (function () {
        try {
          return {
            punct: new RegExp('\\p{P}', 'u'),
            hira: new RegExp('\\p{sc=Hiragana}', 'u'),
            kata: new RegExp('\\p{sc=Katakana}', 'u'),
            han: new RegExp('[\\p{sc=Han}\\p{sc=Hangul}]', 'u'),
            latin: new RegExp('[\\p{sc=Latin}0-9]', 'u'),
            // approximation of UAX#50: what stays upright in vertical flow
            upright: new RegExp('[\\p{sc=Han}\\p{sc=Hiragana}\\p{sc=Katakana}\\p{sc=Hangul}\\p{sc=Bopomofo}\\p{sc=Yi}\\uFF01-\\uFF60\\uFFE0-\\uFFE6\\u3000-\\u303F\\u3190-\\u319F\\u3200-\\u32FF\\u3300-\\u33FF\\u2460-\\u24FF\\u3040-\\u30FF\\uFE30-\\uFE4F]|\\p{Extended_Pictographic}', 'u'),
            rtl: new RegExp('[\\p{sc=Arabic}\\p{sc=Hebrew}\\p{sc=Syriac}\\p{sc=Thaana}\\p{sc=Nko}]', 'u'),
            // scripts whose letters connect / reshape across characters —
            // kept together per word so shaping survives the span split
            joining: new RegExp('[\\p{sc=Arabic}\\p{sc=Syriac}\\p{sc=Mongolian}\\p{sc=Nko}\\p{sc=Devanagari}\\p{sc=Bengali}\\p{sc=Gurmukhi}\\p{sc=Gujarati}\\p{sc=Oriya}\\p{sc=Tamil}\\p{sc=Telugu}\\p{sc=Kannada}\\p{sc=Malayalam}\\p{sc=Sinhala}\\p{sc=Myanmar}\\p{sc=Khmer}\\p{sc=Lao}\\p{sc=Thai}\\p{sc=Tibetan}]', 'u')
          };
        } catch (e) { return null; }
      })();

function classify(ch, cp) {
        if (uni) {
          if (uni.hira.test(ch)) return 'hira';
          if (uni.kata.test(ch)) return 'kata';
          if (uni.han.test(ch)) return 'kanji';
          if (uni.latin.test(ch)) return 'latin';
          return 'other';
        }
        if ((cp >= 0x0041 && cp <= 0x024F) || (cp >= 0x0030 && cp <= 0x0039)) return 'latin';
        if (cp >= 0x3040 && cp <= 0x309F) return 'hira';
        if (cp >= 0x30A0 && cp <= 0x30FF) return 'kata';
        if (cp >= 0x4E00 && cp <= 0x9FFF) return 'kanji';
        return 'other';
      }

var punctStr = '、。，．！？・：；（）［］｛｝「」『』—…‥,.!?;:(){}[]—–…‥';

function isPunctCluster(ch) {
        return uni ? uni.punct.test(ch) : punctStr.indexOf(ch) !== -1;
      }

var charInfoCache = new Map();

function charInfo(ch, cp) {
        var c = charInfoCache.get(ch);
        if (!c) {
          if (charInfoCache.size > 20000) charInfoCache.clear();
          var cls = classify(ch, cp);
          c = {
            cls: cls,
            punct: isPunctCluster(ch),
            upright: uni ? uni.upright.test(ch) : cls !== 'latin',
            joins: !!(uni && uni.joining.test(ch)),
            rtl: !!(uni && uni.rtl.test(ch))
          };
          charInfoCache.set(ch, c);
        }
        return c;
      }

function hash(j, cp, k) {
        return (Math.abs(Math.sin((j + 1) * (cp + 1) * (k + params.seed * 13.37))) * 43758.5453) % 1;
      }

function surfaceGlyphStrength(g, id) {
        var value = g && g.surface ? Number(g.surface[id]) : 0;
        return isFinite(value) ? Math.max(0, Math.min(1, value)) : 0;
      }

var SURFACE_SOURCE_MODE_KEYS = {
        rationalCusp: 'rationalCuspSourceMode',
        bladeBody: 'bladeBodySourceMode',
        blackBastion: 'blackBastionSourceMode',
        inktrapAbyss: 'inktrapAbyssSourceMode',
        strangulation: 'strangulationSourceMode',
        gillArray: 'gillArraySourceMode',
        screwExtrusion: 'screwExtrusionSourceMode',
        harmonicCage: 'harmonicCageSourceMode',
        beltramiFlow: 'beltramiFlowSourceMode',
        riemannRamification: 'riemannRamificationSourceMode',
        linkedTwist: 'linkedTwistSourceMode',
        enneperBody: 'enneperBodySourceMode',
        nibRecast: 'nibRecastSourceMode',
        anatomyWarp: 'anatomyWarpSourceMode',
        kreslingShell: 'kreslingShellSourceMode',
        growthBuckle: 'growthBuckleSourceMode',
        inversionBody: 'inversionBodySourceMode',
        gravityLens: 'gravityLensSourceMode',
        liquidRope: 'liquidRopeSourceMode',
        wulffBody: 'wulffBodySourceMode',
        repulsiveCurves: 'repulsiveCurvesSourceMode',
        wassersteinLetters: 'wassersteinLettersSourceMode',
        stressGlass: 'stressGlassSourceMode',
        vortexBath: 'vortexBathSourceMode',
        miuraVault: 'miuraVaultSourceMode',
        hopfLoom: 'hopfLoomSourceMode',
        densityRecast: 'densityRecastSourceMode',
        spinodalAlloy: 'spinodalAlloySourceMode',
        nodalGlaze: 'nodalGlazeSourceMode',
        loadpathFoundry: 'loadpathFoundrySourceMode',
        hyperbolicAtlas: 'hyperbolicAtlasSourceMode',
        quasicrystalBody: 'quasicrystalBodySourceMode',
        nematicFilm: 'nematicFilmSourceMode',
        diffractiveGlyph: 'diffractiveGlyphSourceMode',
        dendriteCast: 'dendriteCastSourceMode',
        ornamentReserve: 'ornamentReserveSourceMode',
        tensionMembrane: 'tensionMembraneSourceMode',
        tensorFiligree: 'tensorFiligreeSourceMode',
        chromaticSwarm: 'chromaticSwarmSourceMode',
        gyroidSculpture: 'gyroidSculptureSourceMode',
        causticGlass: 'causticGlassSourceMode',
        scrollType: 'scrollSourceMode',
        anamorphicType: 'anamorphicSourceMode',
        contextualFit: 'contextualSourceMode',
        plotterTrace: 'traceSourceMode', webbing: 'webSourceMode', blobTrack: 'blobSourceMode', softBlob: 'softBlobSourceMode', etchantBloom: 'etchantSourceMode',
        calligraphicStress: 'calligraphySourceMode', asemicDuctus: 'asemicSourceMode', spectralType: 'spectralSourceMode', differentialType: 'differentialSourceMode', conformalType: 'conformalSourceMode', auxeticType: 'auxeticSourceMode', marblingType: 'marblingSourceMode', counterformEngine: 'counterformSourceMode', terminalExcess: 'terminalSourceMode', ligatureBody: 'ligatureBodySourceMode',
        fiberBody: 'fiberSourceMode', glyphMutation: 'mutationSourceMode', suspendedSyntax: 'suspensionSourceMode', livingTextField: 'livingSourceMode', innerEruption: 'eruptionSourceMode', recursiveGraft: 'graftSourceMode', structuralCollision: 'collisionSourceMode', peelWeave: 'peelSourceMode', voidPressure: 'voidPressureSourceMode',
        rubyUsurper: 'rubySourceMode', punctuationLoom: 'punctuationLoomSourceMode', counterpage: 'counterpageSourceMode', rendererDebt: 'rendererDebtSourceMode', ligatureContagion: 'contagionSourceMode', strokeCommons: 'commonsSourceMode',
        sigilForge: 'sigilSourceMode', thornCrown: 'thornSourceMode', cipherLiturgy: 'cipherSourceMode',
        boneScaffold: 'boneSourceMode', roseEngine: 'roseSourceMode', chromeReliquary: 'chromeSourceMode',
        ligatureCrypt: 'ligatureSourceMode', moireChoir: 'moireSourceMode', naveCutter: 'naveSourceMode',
        cloisterFold: 'cloisterSourceMode', prismSacrament: 'prismSourceMode', texturaMatrix: 'texturaSourceMode',
        voidPortal: 'voidSourceMode', recursiveShrine: 'recursiveSourceMode', morphProcession: 'morphSourceMode', chimeraGraft: 'chimeraSourceMode',
        monolithCast: 'monolithSourceMode',
        rasterPress: 'rasterSourceMode', hatchEngrave: 'hatchSourceMode', contourEtch: 'contourSourceMode',
        pressureStroke: 'pressureSourceMode', sinewTorque: 'sinewSourceMode', cellFracture: 'fractureSourceMode', ribbonEcho: 'ribbonSourceMode',
        copyDecay: 'copySourceMode', risoSeparation: 'risoSourceMode', misregistration: 'misregSourceMode', dataMosh: 'dataMoshSourceMode', slitSweep: 'slitSourceMode'
      };

var SURFACE_COLOR_KEYS = {
        rationalCusp: 'rationalCuspColor',
        bladeBody: 'bladeBodyColor',
        blackBastion: 'blackBastionColor',
        inktrapAbyss: 'inktrapAbyssColor',
        strangulation: 'strangulationColor',
        gillArray: 'gillArrayColor',
        screwExtrusion: 'screwExtrusionColor',
        harmonicCage: 'harmonicCageColor',
        beltramiFlow: 'beltramiFlowColor',
        riemannRamification: 'riemannRamificationColor',
        linkedTwist: 'linkedTwistColor',
        enneperBody: 'enneperBodyColor',
        nibRecast: 'nibRecastColor',
        anatomyWarp: 'anatomyWarpColor',
        kreslingShell: 'kreslingShellColor',
        growthBuckle: 'growthBuckleColor',
        inversionBody: 'inversionBodyColor',
        gravityLens: 'gravityLensColor',
        liquidRope: 'liquidRopeColor',
        wulffBody: 'wulffBodyColor',
        repulsiveCurves: 'repulsiveCurvesColor',
        wassersteinLetters: 'wassersteinLettersColor',
        stressGlass: 'stressGlassColor',
        vortexBath: 'vortexBathColor',
        miuraVault: 'miuraVaultColor',
        hopfLoom: 'hopfLoomColor',
        densityRecast: 'densityRecastColor',
        spinodalAlloy: 'spinodalAlloyColor',
        nodalGlaze: 'nodalGlazeColor',
        loadpathFoundry: 'loadpathFoundryColor',
        hyperbolicAtlas: 'hyperbolicAtlasColor',
        quasicrystalBody: 'quasicrystalBodyColor',
        nematicFilm: 'nematicFilmColor',
        diffractiveGlyph: 'diffractiveGlyphColor',
        dendriteCast: 'dendriteCastColor',
        ornamentReserve: 'ornamentReserveColor',
        tensionMembrane: 'tensionMembraneColor',
        tensorFiligree: 'tensorFiligreeColor',
        chromaticSwarm: 'chromaticSwarmColor',
        gyroidSculpture: 'gyroidSculptureColor',
        causticGlass: 'causticGlassColor',
        scrollType: 'scrollColor',
        anamorphicType: 'anamorphicColor',
        contextualFit: 'contextualColor',
        plotterTrace: 'traceColor', webbing: 'webColor', blobTrack: 'blobColor', softBlob: 'softBlobColor', etchantBloom: 'etchantColor',
        calligraphicStress: 'calligraphyColor', asemicDuctus: 'asemicColor', spectralType: 'spectralColor', differentialType: 'differentialColor', conformalType: 'conformalColor', auxeticType: 'auxeticColor', marblingType: 'marblingColor', counterformEngine: 'counterformColor', terminalExcess: 'terminalColor', ligatureBody: 'ligatureBodyColor',
        fiberBody: 'fiberColor', glyphMutation: 'mutationColor', suspendedSyntax: 'suspensionColor', livingTextField: 'livingColor', innerEruption: 'eruptionColor', recursiveGraft: 'graftColor', structuralCollision: 'collisionColor', peelWeave: 'peelColor', voidPressure: 'voidPressureColor',
        rubyUsurper: 'rubyColor', punctuationLoom: 'punctuationLoomColor', counterpage: 'counterpageColor', rendererDebt: 'rendererDebtColor', ligatureContagion: 'contagionColor', strokeCommons: 'commonsColor',
        sigilForge: 'sigilColor', thornCrown: 'thornColor', cipherLiturgy: 'cipherColor',
        boneScaffold: 'boneColor', roseEngine: 'roseColor', chromeReliquary: 'chromeColor',
        ligatureCrypt: 'ligatureColor', moireChoir: 'moireColor', naveCutter: 'naveColor',
        cloisterFold: 'cloisterColor', prismSacrament: 'prismColorA', texturaMatrix: 'texturaColor',
        voidPortal: 'voidColor', recursiveShrine: 'recursiveColor', morphProcession: 'morphColor', chimeraGraft: 'chimeraColor',
        monolithCast: 'monolithColor',
        rasterPress: 'rasterColor', hatchEngrave: 'hatchColor', contourEtch: 'contourColor',
        pressureStroke: 'pressureColor', sinewTorque: 'sinewColor', cellFracture: 'fractureColor', ribbonEcho: 'ribbonColor',
        copyDecay: 'copyColor', risoSeparation: 'risoColorA', misregistration: 'misregColorA', dataMosh: 'moshColorA', slitSweep: 'slitColor'
      };

function surfaceSourceMode(id) {
        var key = SURFACE_SOURCE_MODE_KEYS[id];
        return key && params[key] ? params[key] : 'keep';
      }

function surfaceSourceOpacity(id) {
        var key = SURFACE_SOURCE_OPACITY_KEYS[id];
        var value = key ? Number(params[key]) : NaN;
        if (isFinite(value)) return Math.max(0, Math.min(1, value));
        var mode = surfaceSourceMode(id);
        return mode === 'hide' ? 0 : (mode === 'ghost' ? 0.22 : 1);
      }

function surfaceEffectColor(id) {
        var key = SURFACE_COLOR_KEYS[id];
        return key && params[key] ? params[key] : params.ink;
      }

function surfaceOutputOpacity(id) {
        var key = SURFACE_OPACITY_KEYS[id];
        var value = key ? Number(params[key]) : 1;
        return isFinite(value) ? Math.max(0, Math.min(1, value)) : 1;
      }

function surfaceAggregate(glyphs, id, key, fallback) {
        var total = 0, weight = 0, maximum = 0;
        for (var i = 0; i < glyphs.length; i++) {
          var strength = surfaceGlyphStrength(glyphs[i], id);
          if (strength <= 0.002) continue;
          var raw = glyphs[i].surface && Number(glyphs[i].surface[key]);
          var value = isFinite(raw) ? raw : fallback;
          total += value * strength;
          weight += strength;
          maximum = Math.max(maximum, strength);
        }
        return { value: weight ? total / weight : fallback, strength: maximum, weight: weight };
      }

function surfaceChoice(glyphs, id, key, fallback, options) {
        var scores = Object.create(null), maximum = 0, choice = fallback;
        for (var i = 0; i < glyphs.length; i++) {
          var strength = surfaceGlyphStrength(glyphs[i], id);
          if (strength <= 0.002) continue;
          var value = glyphs[i].surface && glyphs[i].surface[key];
          if (options.indexOf(value) === -1) value = fallback;
          scores[value] = (scores[value] || 0) + strength;
          if (scores[value] > maximum) {
            maximum = scores[value];
            choice = value;
          }
        }
        return choice;
      }

function surfaceScratch(name, width, height, readFrequently) {
        var canvas = surfaceFxScratchCanvases[name];
        var renderContext=globalThis.TypeDeformerRenderContext,contextKey=renderContext?renderContext.key():'analysis';
        if (!canvas || canvas._tdScratchKey !== contextKey) {
          canvas = renderContext ? renderContext.createCanvas(width,height,{tiled:true}) : (globalThis.TypeDeformerRenderContext ? globalThis.TypeDeformerRenderContext.createCanvas() : document.createElement('canvas'));
          canvas._tdScratchKey=contextKey;
          surfaceFxScratchCanvases[name] = canvas;
        }
        if (renderContext) renderContext.account(canvas);
        if (canvas.width !== width) canvas.width = width;
        if (canvas.height !== height) canvas.height = height;
        var ctx = canvas.getContext('2d', readFrequently ? { willReadFrequently: true } : undefined);
        ctx.setTransform(1, 0, 0, 1, 0, 0);
        ctx.globalAlpha = 1;
        ctx.globalCompositeOperation = 'source-over';
        if ('filter' in ctx) ctx.filter = 'none';
        ctx.clearRect(0, 0, width, height);
        return { canvas: canvas, ctx: ctx };
      }

function drawSurfaceGlyph(ctx, g, pixelScale, L, fm, alpha, color) {
        if (alpha <= 0.001) return;
        ctx.save();
        ctx.setTransform(pixelScale, 0, 0, pixelScale, 0, 0);
        ctx.translate(L.dx, L.dy);
        ctx.scale(L.s, L.s);
        ctx.translate(g.ox + g.tx, g.oy + g.ty);
        ctx.rotate((g.rot || 0) * Math.PI / 180);
        ctx.transform(1, Math.tan((g.skewY || 0) * Math.PI / 180), Math.tan((g.skewX || 0) * Math.PI / 180), 1, 0, 0);
        ctx.scale(g.scaleX, g.scaleY);
        ctx.translate(-g.ox, -g.oy);
        ctx.globalAlpha = alpha * (g.opacity == null ? 1 : g.opacity);
        ctx.font = g.fontAxes ? glyphFontSpec(g, params.fontSize).font : params.fontWeight + ' ' + params.fontSize + 'px ' + params.fontFamily;
        ctx.fillStyle = color || '#fff';
        if (params.vertical) {
          var cx = g.x + g.w / 2;
          var cy = g.y + g.h / 2;
          ctx.textAlign = 'center';
          ctx.textBaseline = 'middle';
          if (!g.upright) {
            ctx.translate(cx, cy);
            ctx.rotate(Math.PI / 2);
            ctx.fillText(g.ch, 0, 0);
          } else ctx.fillText(g.ch, cx, cy);
        } else {
          ctx.textAlign = g.grid ? 'center' : 'left';
          ctx.textBaseline = 'alphabetic';
          ctx.fillText(g.ch, g.grid ? g.x + g.w / 2 : g.x, g.y + baselineOffset(g, fm));
        }
        ctx.restore();
      }

function buildSurfaceMask(glyphs, id, width, height, pixelScale, L, fm) {
        var mask = surfaceScratch('mask-' + id, width, height, true);
        for (var i = 0; i < glyphs.length; i++) {
          var strength = surfaceGlyphStrength(glyphs[i], id);
          if (strength > 0.002) drawSurfaceGlyph(mask.ctx, glyphs[i], pixelScale, L, fm, strength);
        }
        return mask.canvas;
      }

function paintSurfaceMask(targetCtx, mask, color, opacity, name) {
        if (opacity <= 0.001) return;
        var painted = surfaceScratch(name, mask.width, mask.height, false);
        painted.ctx.drawImage(mask, 0, 0);
        painted.ctx.globalCompositeOperation = 'source-in';
        painted.ctx.fillStyle = color;
        painted.ctx.fillRect(0, 0, mask.width, mask.height);
        painted.ctx.globalCompositeOperation = 'source-over';
        targetCtx.save();
        targetCtx.setTransform(1, 0, 0, 1, 0, 0);
        targetCtx.globalAlpha = opacity;
        targetCtx.drawImage(painted.canvas, 0, 0, targetCtx.canvas.width, targetCtx.canvas.height);
        targetCtx.restore();
      }

function compositeSurfaceSource(targetCtx, mask, id, coverBase) {
        if (!coverBase) return;
        var sourceOpacity = surfaceSourceOpacity(id);
        if (sourceOpacity >= 0.999) return;
        var veilOpacity = 1 - sourceOpacity;
        // The paper veil is continuous: presets are only convenient points on
        // the same 0–100% control. Effect ink is always painted separately.
        if (veilOpacity < 0.995) {
          paintSurfaceMask(targetCtx, mask, params.paper, veilOpacity, id + '-source');
          return;
        }
        // Repeated, slightly offset impressions saturate the anti-aliased edge.
        // A single paper pass leaves a dark hairline from the DOM glyph below.
        var veil = surfaceScratch(id + '-source-hide', mask.width, mask.height, false);
        var spread = 0.82;
        veil.ctx.drawImage(mask, 0, 0);
        veil.ctx.drawImage(mask, 0, 0);
        veil.ctx.drawImage(mask, -spread, 0);
        veil.ctx.drawImage(mask, spread, 0);
        veil.ctx.drawImage(mask, 0, -spread);
        veil.ctx.drawImage(mask, 0, spread);
        paintSurfaceMask(targetCtx, veil.canvas, params.paper, 1, id + '-source');
      }

function surfaceGlyphNode(g, operatorId, pixelScale, L, phase, chaos, speed, index, localU, localV, radiusScale) {
        localU = localU == null ? 0.5 : localU;
        localV = localV == null ? 0.5 : localV;
        radiusScale = radiusScale == null ? 1 : radiusScale;
        var localX = g.x + g.w * localU;
        var localY = g.y + g.h * localV;
        var rad = (g.rot || 0) * Math.PI / 180;
        var cos = Math.cos(rad), sin = Math.sin(rad);
        var tanX = Math.tan((g.skewX || 0) * Math.PI / 180);
        var tanY = Math.tan((g.skewY || 0) * Math.PI / 180);
        var a = cos * g.scaleX - sin * tanY * g.scaleX;
        var b = sin * g.scaleX + cos * tanY * g.scaleX;
        var c = cos * tanX * g.scaleY - sin * g.scaleY;
        var d = sin * tanX * g.scaleY + cos * g.scaleY;
        var lx = localX - g.ox, ly = localY - g.oy;
        var logicalX = g.ox + g.tx + a * lx + c * ly;
        var logicalY = g.oy + g.ty + b * lx + d * ly;
        var transformedW = Math.abs(g.w * g.scaleX);
        var transformedH = Math.abs(g.h * g.scaleY);
        var baseRadius = Math.max(2, Math.min(transformedW, transformedH) * 0.24 * L.s * pixelScale) * radiusScale;
        var wobblePhase = phase * (0.35 + speed * 2.4) + index * 1.618;
        return {
          x: (L.dx + logicalX * L.s) * pixelScale + Math.cos(wobblePhase * 1.07) * baseRadius * chaos * 0.48,
          y: (L.dy + logicalY * L.s) * pixelScale + Math.sin(wobblePhase * 0.91) * baseRadius * chaos * 0.48,
          r: baseRadius * (0.72 + hash(index, params.seed % 61, 503) * 0.56),
          strength: surfaceGlyphStrength(g, operatorId),
          glyphIndex: index,
          glyph: g
        };
      }

function surfaceBoundaryDistance(alphaData, width, height) {
        var count = width * height;
        var inside = new Uint8Array(count);
        var distance = new Float32Array(count);
        var infinity = 1000000000;
        var minX = width, minY = height, maxX = -1, maxY = -1;
        var x, y, index;
        for (index = 0; index < count; index++) {
          inside[index] = alphaData[index * 4 + 3] > 20 ? 1 : 0;
          if (inside[index]) {
            x = index % width;
            y = Math.floor(index / width);
            if (x < minX) minX = x;
            if (x > maxX) maxX = x;
            if (y < minY) minY = y;
            if (y > maxY) maxY = y;
          }
        }
        for (y = 0; y < height; y++) for (x = 0; x < width; x++) {
          index = y * width + x;
          var value = inside[index];
          var boundary = !!value && (x === 0 || y === 0 || x === width - 1 || y === height - 1);
          if (x > 0 && inside[index - 1] !== value) boundary = true;
          if (x + 1 < width && inside[index + 1] !== value) boundary = true;
          if (y > 0 && inside[index - width] !== value) boundary = true;
          if (y + 1 < height && inside[index + width] !== value) boundary = true;
          distance[index] = boundary ? 0 : infinity;
        }
        var diagonal = 1.41421356;
        for (y = 0; y < height; y++) for (x = 0; x < width; x++) {
          index = y * width + x;
          var d = distance[index];
          if (x > 0) d = Math.min(d, distance[index - 1] + 1);
          if (y > 0) d = Math.min(d, distance[index - width] + 1);
          if (x > 0 && y > 0) d = Math.min(d, distance[index - width - 1] + diagonal);
          if (x + 1 < width && y > 0) d = Math.min(d, distance[index - width + 1] + diagonal);
          distance[index] = d;
        }
        for (y = height - 1; y >= 0; y--) for (x = width - 1; x >= 0; x--) {
          index = y * width + x;
          var reverse = distance[index];
          if (x + 1 < width) reverse = Math.min(reverse, distance[index + 1] + 1);
          if (y + 1 < height) reverse = Math.min(reverse, distance[index + width] + 1);
          if (x + 1 < width && y + 1 < height) reverse = Math.min(reverse, distance[index + width + 1] + diagonal);
          if (x > 0 && y + 1 < height) reverse = Math.min(reverse, distance[index + width - 1] + diagonal);
          distance[index] = reverse;
        }
        return { inside: inside, distance: distance, bounds: maxX >= 0 ? [minX, minY, maxX, maxY] : null };
      }

function surfaceVoidTopology(field, width, height) {
        var count = width * height;
        var outside = new Uint8Array(count);
        var counter = new Uint8Array(count);
        var visited = new Uint8Array(count);
        var queue = new Int32Array(count);
        var head = 0, tail = 0;
        function enqueue(index) {
          if (index < 0 || index >= count || field.inside[index] || outside[index]) return;
          outside[index] = 1;
          queue[tail++] = index;
        }
        var x, y;
        for (x = 0; x < width; x++) {
          enqueue(x);
          enqueue((height - 1) * width + x);
        }
        for (y = 1; y + 1 < height; y++) {
          enqueue(y * width);
          enqueue(y * width + width - 1);
        }
        while (head < tail) {
          var current = queue[head++];
          var cx = current % width;
          var cy = Math.floor(current / width);
          if (cx > 0) enqueue(current - 1);
          if (cx + 1 < width) enqueue(current + 1);
          if (cy > 0) enqueue(current - width);
          if (cy + 1 < height) enqueue(current + width);
        }
        var components = [];
        for (var index = 0; index < count; index++) {
          if (field.inside[index] || outside[index]) continue;
          counter[index] = 1;
        }
        for (var start = 0; start < count; start++) {
          if (!counter[start] || visited[start]) continue;
          head = 0; tail = 0; queue[tail++] = start; visited[start] = 1;
          var size = 0, sumX = 0, sumY = 0;
          var minX = width, minY = height, maxX = -1, maxY = -1;
          while (head < tail) {
            var cell = queue[head++];
            var cellX = cell % width, cellY = Math.floor(cell / width);
            size++; sumX += cellX; sumY += cellY;
            minX = Math.min(minX, cellX); maxX = Math.max(maxX, cellX);
            minY = Math.min(minY, cellY); maxY = Math.max(maxY, cellY);
            var neighbors = [cellX > 0 ? cell - 1 : -1, cellX + 1 < width ? cell + 1 : -1,
              cellY > 0 ? cell - width : -1, cellY + 1 < height ? cell + width : -1];
            for (var n = 0; n < neighbors.length; n++) {
              var next = neighbors[n];
              if (next >= 0 && counter[next] && !visited[next]) {
                visited[next] = 1;
                queue[tail++] = next;
              }
            }
          }
          if (size >= 3) components.push({
            x: sumX / size, y: sumY / size, size: size,
            minX: minX, minY: minY, maxX: maxX, maxY: maxY
          });
        }
        components.sort(function (a, b) { return b.size - a.size; });
        if (components.length > 96) components.length = 96;
        return { outside: outside, counter: counter, components: components };
      }

function surfaceGlyphAnatomyMetas(glyphs, operatorId, pixelScale, L) {
        var metas = [];
        for (var i = 0; i < glyphs.length; i++) {
          var glyph = glyphs[i];
          var strength = surfaceGlyphStrength(glyph, operatorId);
          if (strength <= 0.002 || (glyph.opacity != null && glyph.opacity <= 0.002)) continue;
          var center = surfaceGlyphNode(glyph, operatorId, pixelScale, L, 0, 0, 0, i, 0.5, 0.5, 1);
          var scaleX = Math.max(0.05, Math.abs(glyph.scaleX || 1));
          var scaleY = Math.max(0.05, Math.abs(glyph.scaleY || 1));
          metas.push({
            glyph: glyph,
            index: i,
            x: center.x,
            y: center.y,
            halfW: Math.max(5, Math.abs(glyph.bw || glyph.w) * scaleX * pixelScale * L.s * 0.62),
            halfH: Math.max(5, Math.abs(glyph.bh || glyph.h) * scaleY * pixelScale * L.s * 0.62),
            strength: strength,
            line: glyph.line == null ? 0 : glyph.line,
            word: glyph.word == null ? 0 : glyph.word
          });
        }
        return metas;
      }

function surfaceNearestGlyphMeta(metas, x, y) {
        var best = null, bestScore = Infinity;
        for (var i = 0; i < metas.length; i++) {
          var meta = metas[i];
          var nx = (x - meta.x) / meta.halfW;
          var ny = (y - meta.y) / meta.halfH;
          var score = nx * nx + ny * ny;
          if (score < bestScore) { bestScore = score; best = meta; }
        }
        return best;
      }

function asemicMetaParam(meta, key, fallback, min, max, integer) {
        var raw = meta && meta.glyph && meta.glyph.surface ? Number(meta.glyph.surface[key]) : NaN;
        var value = isFinite(raw) ? raw : fallback;
        value = Math.max(min, Math.min(max, value));
        return integer ? Math.round(value) : value;
      }

function asemicMetaChoice(meta, key, fallback, options) {
        var value = meta && meta.glyph && meta.glyph.surface ? meta.glyph.surface[key] : fallback;
        return options.indexOf(value) === -1 ? fallback : value;
      }

function asemicCounterComponents(topology, metas, meta) {
        var counters = [];
        for (var i = 0; i < topology.components.length && counters.length < 4; i++) {
          var component = topology.components[i];
          if (surfaceNearestGlyphMeta(metas, component.x, component.y) !== meta) continue;
          var nx = Math.abs(component.x - meta.x) / Math.max(1, meta.halfW);
          var ny = Math.abs(component.y - meta.y) / Math.max(1, meta.halfH);
          var componentW = component.maxX - component.minX + 1;
          var componentH = component.maxY - component.minY + 1;
          if (nx > 1.08 || ny > 1.08 || componentW > meta.halfW * 1.9 || componentH > meta.halfH * 1.9) continue;
          counters.push(component);
        }
        return counters;
      }

function asemicCanonicalPoint(meta, grammar, index, count, seed) {
        var t = count <= 1 ? 0.5 : index / (count - 1);
        var phase = hash(index + meta.index * 7, seed % 997, 1709) * Math.PI * 2;
        var u, v;
        if (grammar === 'chamber') {
          var angle = -Math.PI * 0.5 + t * Math.PI * 2;
          u = 0.5 + Math.cos(angle) * (0.28 + hash(index, seed, 1721) * 0.08);
          v = 0.5 + Math.sin(angle) * (0.34 + hash(index, seed, 1733) * 0.08);
        } else if (grammar === 'incised') {
          u = 0.14 + t * 0.72;
          v = (index % 2 ? 0.76 : 0.24) + Math.sin(phase) * 0.08;
        } else {
          u = 0.12 + t * 0.76;
          var amplitude = grammar === 'polyphonic' ? 0.21 : 0.29;
          v = 0.5 + Math.sin(t * Math.PI * (1.4 + hash(meta.index, seed, 1747) * 1.6) + phase * 0.24) * amplitude;
        }
        if (params.vertical && grammar !== 'chamber') {
          var swap = u; u = v; v = swap;
        }
        return {
          x: meta.x + (u - 0.5) * meta.halfW * 2,
          y: meta.y + (v - 0.5) * meta.halfH * 2
        };
      }

function asemicCandidatePool(field, width, height, metas, meta, budget, memory, seed) {
        var grid = Math.max(9, Math.min(19, budget * 2 + 3));
        var minX = Math.max(0, Math.floor(meta.x - meta.halfW * 1.04));
        var maxX = Math.min(width - 1, Math.ceil(meta.x + meta.halfW * 1.04));
        var minY = Math.max(0, Math.floor(meta.y - meta.halfH * 1.04));
        var maxY = Math.min(height - 1, Math.ceil(meta.y + meta.halfH * 1.04));
        var stepX = Math.max(1, (maxX - minX) / Math.max(1, grid - 1));
        var stepY = Math.max(1, (maxY - minY) / Math.max(1, grid - 1));
        var sampleX = Math.max(1, Math.round(stepX * 0.58));
        var sampleY = Math.max(1, Math.round(stepY * 0.58));
        var pool = [];
        for (var gy = 0; gy < grid; gy++) for (var gx = 0; gx < grid; gx++) {
          var x = Math.max(0, Math.min(width - 1, Math.round(minX + gx * stepX)));
          var y = Math.max(0, Math.min(height - 1, Math.round(minY + gy * stepY)));
          var index = y * width + x;
          if (!field.inside[index] || surfaceNearestGlyphMeta(metas, x, y) !== meta) continue;
          var distance = field.distance[index];
          var left = field.distance[y * width + Math.max(0, x - sampleX)];
          var right = field.distance[y * width + Math.min(width - 1, x + sampleX)];
          var top = field.distance[Math.max(0, y - sampleY) * width + x];
          var bottom = field.distance[Math.min(height - 1, y + sampleY) * width + x];
          var ridge = (distance + 0.2 >= left && distance + 0.2 >= right ? 1 : 0)
            + (distance + 0.2 >= top && distance + 0.2 >= bottom ? 1 : 0);
          var keepInterior = hash(gx + meta.index * 31, gy + seed, 1759) < 0.08 + (1 - memory) * 0.2;
          if (!ridge && !keepInterior) continue;
          var nx = (x - meta.x) / Math.max(1, meta.halfW);
          var ny = (y - meta.y) / Math.max(1, meta.halfH);
          var centrality = Math.max(0, 1 - Math.sqrt(nx * nx + ny * ny) * 0.52);
          pool.push({
            x: x, y: y,
            score: distance * (0.68 + ridge * 0.72) + centrality * Math.min(meta.halfW, meta.halfH) * 0.14
              + hash(gx, gy + seed, 1777) * 0.9
          });
        }
        if (pool.length >= 2) return pool;
        for (var fallbackIndex = 0; fallbackIndex < Math.max(2, budget); fallbackIndex++) {
          var fallback = asemicCanonicalPoint(meta, 'current', fallbackIndex, Math.max(2, budget), seed);
          pool.push({ x: fallback.x, y: fallback.y, score: 1 });
        }
        return pool;
      }

function asemicSelectAnchors(pool, count, meta, seed) {
        if (!pool.length) return [];
        pool = pool.slice().sort(function (a, b) { return b.score - a.score; });
        var startBand = Math.min(pool.length, 5);
        var start = Math.floor(hash(meta.index, seed % 991, 1789) * startBand);
        var selected = [pool.splice(start, 1)[0]];
        var normalizer = Math.max(1, meta.halfW * meta.halfW + meta.halfH * meta.halfH);
        while (pool.length && selected.length < count) {
          var bestIndex = 0, bestScore = -Infinity;
          for (var i = 0; i < pool.length; i++) {
            var minimum = Infinity;
            for (var j = 0; j < selected.length; j++) {
              var dx = pool[i].x - selected[j].x;
              var dy = pool[i].y - selected[j].y;
              minimum = Math.min(minimum, dx * dx + dy * dy);
            }
            var score = minimum / normalizer * 7.2 + pool[i].score / Math.max(1, Math.min(meta.halfW, meta.halfH))
              + hash(i + selected.length * 17, seed, 1801) * 0.08;
            if (score > bestScore) { bestScore = score; bestIndex = i; }
          }
          selected.push(pool.splice(bestIndex, 1)[0]);
        }
        return selected;
      }

function asemicOrderAnchors(points, grammar, meta, seed) {
        var ordered = points.slice();
        if (grammar === 'chamber') {
          ordered.sort(function (a, b) {
            return Math.atan2(a.y - meta.y, a.x - meta.x) - Math.atan2(b.y - meta.y, b.x - meta.x);
          });
        } else {
          ordered.sort(function (a, b) {
            var primaryA = params.vertical ? a.y : a.x;
            var primaryB = params.vertical ? b.y : b.x;
            return primaryA - primaryB || (params.vertical ? a.x - b.x : a.y - b.y);
          });
          if (grammar === 'incised' && ordered.length > 3) {
            var folded = [], left = 0, right = ordered.length - 1;
            while (left <= right) {
              folded.push(ordered[left++]);
              if (left <= right) folded.push(ordered[right--]);
            }
            ordered = folded;
          }
        }
        if (hash(meta.index, seed, 1811) > 0.5 && grammar !== 'chamber') ordered.reverse();
        return ordered;
      }

function asemicBuildAnchors(field, width, height, metas, meta, grammar, memory, gestureCount, flow, flourish, phase, seed) {
        var pool = asemicCandidatePool(field, width, height, metas, meta, gestureCount, memory, seed);
        var source = asemicOrderAnchors(asemicSelectAnchors(pool, gestureCount, meta, seed), grammar, meta, seed);
        var anchors = [];
        var sourceShare = 0.12 + memory * 0.88;
        var motionReach = Math.min(meta.halfW, meta.halfH) * (0.014 + Math.abs(flow) * 0.018);
        for (var i = 0; i < source.length; i++) {
          var canonical = asemicCanonicalPoint(meta, grammar, i, source.length, seed);
          var motion = Math.sin(phase + i * 1.47 + seed * 0.013);
          anchors.push({
            x: canonical.x * (1 - sourceShare) + source[i].x * sourceShare
              + Math.cos(phase + i * 0.91) * motionReach * motion,
            y: canonical.y * (1 - sourceShare) + source[i].y * sourceShare
              + Math.sin(phase * 2 + i * 1.13) * motionReach * motion
          });
        }
        if (grammar !== 'chamber' && anchors.length >= 2 && flourish > 0.01) {
          var first = anchors[0], second = anchors[1];
          var last = anchors[anchors.length - 1], before = anchors[anchors.length - 2];
          var startDx = second.x - first.x, startDy = second.y - first.y;
          var endDx = last.x - before.x, endDy = last.y - before.y;
          var startLength = Math.max(0.001, Math.hypot(startDx, startDy));
          var endLength = Math.max(0.001, Math.hypot(endDx, endDy));
          var startReach = flourish * (0.58 + hash(meta.index, seed, 1823) * 0.42);
          var endReach = flourish * (0.72 + hash(meta.index, seed, 1831) * 0.52);
          anchors.unshift({
            x: first.x - startDx / startLength * startReach - startDy / startLength * flow * startReach * 0.11,
            y: first.y - startDy / startLength * startReach + startDx / startLength * flow * startReach * 0.11
          });
          anchors.push({
            x: last.x + endDx / endLength * endReach - endDy / endLength * flow * endReach * 0.14,
            y: last.y + endDy / endLength * endReach + endDx / endLength * flow * endReach * 0.14
          });
        }
        return anchors;
      }

function asemicOffsetPath(points, offset) {
        if (!offset) return points.slice();
        var shifted = [];
        for (var i = 0; i < points.length; i++) {
          var previous = points[Math.max(0, i - 1)];
          var next = points[Math.min(points.length - 1, i + 1)];
          var dx = next.x - previous.x, dy = next.y - previous.y;
          var length = Math.max(0.001, Math.hypot(dx, dy));
          shifted.push({ x: points[i].x - dy / length * offset, y: points[i].y + dx / length * offset });
        }
        return shifted;
      }

function asemicRenderPath(ctx, points, weight, contrast, flow, phase, seed, grammar, closed, voiceScale) {
        if (points.length < 2) return;
        var path = points.slice();
        if (closed) path.push(points[0]);
        var nibAngle = -0.52 + hash(seed % 257, points.length, 1847) * 1.04 + flow * 0.045;
        ctx.lineCap = grammar === 'incised' ? 'butt' : 'round';
        ctx.lineJoin = grammar === 'incised' ? 'miter' : 'round';
        ctx.strokeStyle = '#fff';
        ctx.fillStyle = '#fff';
        for (var i = 0; i + 1 < path.length; i++) {
          var a = path[i], b = path[i + 1];
          var dx = b.x - a.x, dy = b.y - a.y;
          var distance = Math.max(0.001, Math.hypot(dx, dy));
          var angle = Math.atan2(dy, dx);
          var orientation = Math.abs(Math.cos(angle - nibAngle));
          var widthScale = Math.max(0.22, 1 - contrast * 0.08 + orientation * contrast * 0.22);
          var segmentWidth = Math.max(0.35, weight * widthScale * (voiceScale == null ? 1 : voiceScale));
          var gap = grammar === 'incised' ? Math.min(distance * 0.2, segmentWidth * 0.45) : 0;
          var ux = dx / distance, uy = dy / distance;
          var ax = a.x + ux * gap, ay = a.y + uy * gap;
          var bx = b.x - ux * gap, by = b.y - uy * gap;
          ctx.lineWidth = segmentWidth;
          ctx.beginPath();
          ctx.moveTo(ax, ay);
          if (grammar === 'incised') {
            ctx.lineTo(bx, by);
          } else {
            var curvature = Math.max(-0.44, Math.min(0.44,
              flow * 0.075 * Math.sin(i * 1.31 + phase + seed * 0.017)));
            var controlX = (ax + bx) * 0.5 - uy * distance * curvature;
            var controlY = (ay + by) * 0.5 + ux * distance * curvature;
            ctx.quadraticCurveTo(controlX, controlY, bx, by);
          }
          ctx.stroke();
          if (grammar !== 'incised' && i + 1 < path.length - 1) {
            ctx.beginPath();
            ctx.ellipse(b.x, b.y, segmentWidth * 0.44, segmentWidth * 0.35,
              angle + Math.PI * 0.5, 0, Math.PI * 2);
            ctx.fill();
          }
        }
        if (grammar === 'incised') {
          for (var joint = 0; joint < points.length; joint++) {
            var radius = weight * (0.28 + contrast * 0.035);
            ctx.save();
            ctx.translate(points[joint].x, points[joint].y);
            ctx.rotate(Math.PI * 0.25 + flow * 0.08);
            ctx.fillRect(-radius * 0.72, -radius * 0.72, radius * 1.44, radius * 1.44);
            ctx.restore();
          }
        }
      }

function asemicRenderChamber(ctx, anchors, counters, meta, weight, contrast, flow, phase, seed) {
        asemicRenderPath(ctx, anchors, weight, contrast, flow, phase, seed, 'chamber', true, 1);
        var spineStart = params.vertical ? { x: meta.x - meta.halfW * 0.58, y: meta.y }
          : { x: meta.x, y: meta.y - meta.halfH * 0.62 };
        var spineEnd = params.vertical ? { x: meta.x + meta.halfW * 0.58, y: meta.y }
          : { x: meta.x, y: meta.y + meta.halfH * 0.62 };
        asemicRenderPath(ctx, [spineStart, { x: meta.x + flow * weight * 0.42, y: meta.y }, spineEnd],
          weight * 0.78, contrast, flow, phase, seed + 19, 'current', false, 1);
        var loopSources = counters.length ? counters : [{
          x: meta.x, y: meta.y,
          minX: meta.x - meta.halfW * 0.18, maxX: meta.x + meta.halfW * 0.18,
          minY: meta.y - meta.halfH * 0.16, maxY: meta.y + meta.halfH * 0.16
        }];
        for (var c = 0; c < loopSources.length && c < 3; c++) {
          var source = loopSources[c];
          var radiusX = Math.max(weight * 1.15, Math.min(meta.halfW * 0.31, (source.maxX - source.minX + 1) * 0.58));
          var radiusY = Math.max(weight, Math.min(meta.halfH * 0.3, (source.maxY - source.minY + 1) * 0.58));
          var loop = [];
          for (var p = 0; p < 8; p++) {
            var angle = p / 8 * Math.PI * 2;
            var pulse = 1 + Math.sin(angle * 3 + seed * 0.021 + phase) * 0.08;
            loop.push({ x: source.x + Math.cos(angle) * radiusX * pulse, y: source.y + Math.sin(angle) * radiusY / pulse });
          }
          asemicRenderPath(ctx, loop, weight * 0.62, contrast * 0.72, flow * 0.45,
            phase, seed + c * 23, 'chamber', true, 1);
        }
      }

function asemicBodyBounds(anchors, counters, meta, weight, contrast, flow, grammar, width, height) {
        var minX = Infinity, minY = Infinity, maxX = -Infinity, maxY = -Infinity;
        function include(x, y) {
          minX = Math.min(minX, x); minY = Math.min(minY, y);
          maxX = Math.max(maxX, x); maxY = Math.max(maxY, y);
        }
        for (var i = 0; i < anchors.length; i++) include(anchors[i].x, anchors[i].y);
        if (grammar === 'chamber') {
          include(meta.x - meta.halfW * 0.58, meta.y - meta.halfH * 0.62);
          include(meta.x + meta.halfW * 0.58, meta.y + meta.halfH * 0.62);
          include(meta.x + flow * weight * 0.42, meta.y);
          var loopSources = counters.length ? counters : [meta];
          // Loop pulses lie in [.92, 1.08], including synthetic chambers.
          var loopX = Math.max(weight * 1.15, meta.halfW * 0.31) * 1.08;
          var loopY = Math.max(weight, meta.halfH * 0.3) / 0.92;
          for (var c = 0; c < loopSources.length && c < 3; c++) {
            include(loopSources[c].x - loopX, loopSources[c].y - loopY);
            include(loopSources[c].x + loopX, loopSources[c].y + loopY);
          }
        }
        if (!Number.isFinite(minX)) return null;
        var voiceGap = grammar === 'polyphonic' ? weight * (1.08 + contrast * 0.055) : 0;
        // A quadratic lies inside its endpoint/control-point convex hull.
        var bend = grammar === 'incised' ? 0 : Math.min(0.44, Math.abs(flow) * 0.075);
        var controlReach = bend * Math.hypot(maxX - minX + voiceGap * 2, maxY - minY + voiceGap * 2);
        var nibReach = Math.max(0.5, weight * (1 + contrast * 0.14) * 0.5,
          weight * (0.28 + contrast * 0.035) * Math.SQRT2 * 0.72);
        var padding = voiceGap + controlReach + nibReach + 2; // antialias fringe
        var x = Math.max(0, Math.floor(minX - padding)), y = Math.max(0, Math.floor(minY - padding));
        var right = Math.min(width, Math.ceil(maxX + padding)), bottom = Math.min(height, Math.ceil(maxY + padding));
        return right > x && bottom > y ? { x: x, y: y, width: right - x, height: bottom - y } : null;
      }

function asemicCutCounters(ctx, counters, meta, amount, weight, grammar, flow, seed) {
        if (amount <= 0.01) return;
        var cutSources = counters.slice(0, 3);
        if (!cutSources.length && (grammar === 'chamber' || amount > 1.02)) {
          cutSources.push({
            x: meta.x, y: meta.y,
            minX: meta.x - weight * 1.2, maxX: meta.x + weight * 1.2,
            minY: meta.y - weight, maxY: meta.y + weight
          });
        }
        ctx.save();
        ctx.globalCompositeOperation = 'destination-out';
        ctx.fillStyle = '#000';
        ctx.strokeStyle = '#000';
        for (var i = 0; i < cutSources.length; i++) {
          var source = cutSources[i];
          var componentW = source.maxX - source.minX + 1;
          var componentH = source.maxY - source.minY + 1;
          var radiusX = Math.max(weight * 0.46, Math.min(meta.halfW * 0.28, componentW * (0.2 + amount * 0.24)));
          var radiusY = Math.max(weight * 0.38, Math.min(meta.halfH * 0.27, componentH * (0.2 + amount * 0.24)));
          ctx.beginPath();
          ctx.ellipse(source.x, source.y, radiusX, radiusY,
            flow * 0.08 + (hash(i, seed, 1861) - 0.5) * 0.24, 0, Math.PI * 2);
          ctx.fill();
          if (amount > 0.88) {
            var direction = (hash(i, seed, 1871) > 0.5 ? 1 : -1);
            ctx.lineWidth = Math.max(1, weight * (amount - 0.72) * 0.48);
            ctx.lineCap = 'round';
            ctx.beginPath();
            ctx.moveTo(source.x, source.y);
            if (params.vertical) ctx.lineTo(meta.x + direction * meta.halfW * 1.08, source.y + flow * weight * 0.35);
            else ctx.lineTo(source.x + flow * weight * 0.35, meta.y + direction * meta.halfH * 1.08);
            ctx.stroke();
          }
        }
        ctx.restore();
      }

function spectralTraceContours(data, width, height) {
        // Interpolate the half-coverage boundary instead of tracing pixel
        // boxes. Locked counters then retain smooth alpha-derived outlines.
        // Samples are at pixel centers; a zero ring closes border contours.
        var threshold = 127.5, stride = width + 2;
        var vertices = new Map(), outgoing = new Map(), segments = [];
        function value(x, y) {
          return x < 0 || y < 0 || x >= width || y >= height ? 0 : data[(y * width + x) * 4 + 3];
        }
        function crossing(x, y, vertical, a, b) {
          var key = ((y + 1) * stride + x + 1) * 2 + (vertical ? 1 : 0);
          if (!vertices.has(key)) {
            var t = (threshold - a) / (b - a);
            vertices.set(key, { x: x + 0.5 + (vertical ? 0 : t), y: y + 0.5 + (vertical ? t : 0) });
          }
          return key;
        }
        function segment(start, end) {
          outgoing.set(start, segments.length);
          segments.push({ start: start, end: end });
        }
        for (var y = -1; y < height; y++) for (var x = -1; x < width; x++) {
          var a = value(x, y), b = value(x + 1, y), c = value(x + 1, y + 1), d = value(x, y + 1);
          var mask = (a > threshold ? 1 : 0) | (b > threshold ? 2 : 0) | (c > threshold ? 4 : 0) | (d > threshold ? 8 : 0);
          // Allocate edge bookkeeping only for boundary cells.
          if (mask === 0 || mask === 15) continue;
          var values = [a, b, c, d], high = [!!(mask & 1), !!(mask & 2), !!(mask & 4), !!(mask & 8)];
          var cross = [-1, -1, -1, -1], enter = -1, leave = -1, count = 0;
          for (var side = 0; side < 4; side++) {
            var next = (side + 1) % 4;
            if (high[side] === high[next]) continue;
            if (side === 0) cross[side] = crossing(x, y, false, values[0], values[1]);
            else if (side === 1) cross[side] = crossing(x + 1, y, true, values[1], values[2]);
            else if (side === 2) cross[side] = crossing(x, y + 1, false, values[3], values[2]);
            else cross[side] = crossing(x, y, true, values[0], values[3]);
            if (high[side]) leave = cross[side]; else enter = cross[side];
            count++;
          }
          // In the Canvas y-down frame, ink stays to the segment's right.
          if (count === 2) segment(leave, enter);
          // Four-connected ink keeps diagonal dots separate at a saddle.
          else if (count === 4) for (var corner = 0; corner < 4; corner++) {
            if (high[corner]) segment(cross[corner], cross[(corner + 3) % 4]);
          }
        }
        var visited = new Uint8Array(segments.length), contours = [];
        for (var first = 0; first < segments.length; first++) {
          if (visited[first]) continue;
          var index = first, points = [], area = 0, closed = false, origin = segments[first].start;
          while (index !== undefined && !visited[index]) {
            var edge = segments[index], from = vertices.get(edge.start), to = vertices.get(edge.end);
            visited[index] = 1; points.push(from); area += from.x * to.y - to.x * from.y;
            if (edge.end === origin) { closed = true; break; }
            index = outgoing.get(edge.end);
          }
          if (closed && points.length >= 3 && Math.abs(area) > 0.000001) contours.push({ points: points, area: area * 0.5 });
        }
        return contours;
      }

function spectralGlyphFrame(ctx, g, pixelScale, L, fm, data) {
        ctx.setTransform(pixelScale, 0, 0, pixelScale, 0, 0);
        ctx.translate(L.dx, L.dy); ctx.scale(L.s, L.s);
        ctx.translate(g.ox + g.tx, g.oy + g.ty); ctx.rotate((g.rot || 0) * Math.PI / 180);
        ctx.transform(1, Math.tan((g.skewY || 0) * Math.PI / 180), Math.tan((g.skewX || 0) * Math.PI / 180), 1, 0, 0);
        ctx.scale(g.scaleX, g.scaleY); ctx.translate(-g.ox, -g.oy);
        var scale = params.fontSize / 192;
        if (params.vertical) {
          ctx.translate(g.x + g.w / 2, g.y + g.h / 2);
          if (!g.upright) ctx.rotate(Math.PI / 2);
          ctx.scale(scale, scale);
          // Canvas middle baseline is em-box based, not the visible ink box.
          ctx.translate(-data.advance / 2, data.middleOffset);
        } else {
          ctx.translate(g.grid ? g.x + g.w / 2 : g.x, g.y + baselineOffset(g, fm));
          ctx.scale(scale, scale);
          if (g.grid) ctx.translate(-data.advance / 2, 0);
        }
      }

var DifferentialGrowth = (function () {
      // EXPERIMENTAL: offline research only; not registered in the editor.
      // Original code. No reference source, font, image or third-party dependency is included.
      function resample(points, spacing) {
        const lengths=[0]; let total=0;
        points.forEach((p,i)=>{const q=points[(i+1)%points.length]; total+=Math.hypot(p.x-q.x,p.y-q.y); lengths.push(total);});
        const n=Math.max(12,Math.ceil(total/spacing)); let k=0;
        return Array.from({length:n},(_,i)=>{const d=total*i/n; while(k+1<points.length&&lengths[k+1]<d)k++; const a=points[k],b=points[(k+1)%points.length],t=(d-lengths[k])/(lengths[k+1]-lengths[k]||1);return {x:a.x+(b.x-a.x)*t,y:a.y+(b.y-a.y)*t};});
      }


      // Original continuous vertex/edge contact constraint; no third-party code.
      // Coordinates and clearance are in the 192px glyph frame. This is a tolerant
      // floating-point solver, not exact predicates or a repair of invalid input.
      function cross(ax, ay, bx, by) { return ax * by - ay * bx; }

      function vertexEdgeHit(p, a, b) {
        const ex = b.x - a.x, ey = b.y - a.y, rx = p.x - a.x, ry = p.y - a.y;
        const evx = b.mx - a.mx, evy = b.my - a.my, rvx = p.mx - a.mx, rvy = p.my - a.my;
        const c = cross(ex, ey, rx, ry);
        const d = cross(ex, ey, rvx, rvy) + cross(evx, evy, rx, ry), e = cross(evx, evy, rvx, rvy);
        const mid = c + d * .5, end = c + d + e;
        const tolerance = 1e-11 * (1 + Math.abs(c) + Math.abs(d) + Math.abs(e));
        // Bernstein control values bound the orientation polynomial over the step.
        if (c > tolerance && mid > tolerance && end > tolerance
          || c < -tolerance && mid < -tolerance && end < -tolerance) return null;
        const roots = [];
        if (Math.abs(e) < tolerance) {
          if (Math.abs(d) > tolerance) roots.push(-c / d);
        } else {
          const discriminant = d * d - 4 * e * c;
          if (discriminant >= 0) {
            const sq = Math.sqrt(discriminant), q = -.5 * (d + (d >= 0 ? sq : -sq));
            roots.push(q / e);
            if (q) roots.push(c / q);
          }
        }
        roots.sort((x, y) => x - y);
        for (const time of roots) {
          if (time < 1e-8 || time > 1) continue;
          const dx = ex + evx * time, dy = ey + evy * time;
          const px = rx + rvx * time, py = ry + rvy * time, dd = dx * dx + dy * dy;
          if (dd < 1e-16) continue;
          const u = (px * dx + py * dy) / dd;
          if (u < -1e-8 || u > 1 + 1e-8) continue;
          // Ignore opening/grazing roots. Persistent collinearity and initial contact
          // are outside this test; a closing hit is separated before it is committed.
          const side = c >= 0 ? 1 : -1;
          if ((d + 2 * e * time) * side >= -tolerance) continue;
          const length = Math.sqrt(dd);
          return { p, a, b, time, u: Math.max(0, Math.min(1, u)), nx: -dy / length * side, ny: dx / length * side };
        }
        return null;
      }

      function createContactGuard(cellSize) {
        if (!Number.isFinite(cellSize) || cellSize <= 0) throw new RangeError('Expected positive contact cell size');
        const edgePool = [], rows = new Map(), rowPool = [], bucketPool = [];
        let contacts = 0, stops = 0;
        function* grid(rings) {
          rows.clear(); let edgeCount = 0, rowCount = 0, bucketCount = 0;
          for (const ring of rings) for (let i = 0; i < ring.points.length; i++) {
            const a = ring.points[i], b = ring.points[(i + 1) % ring.points.length];
            const edge = edgePool[edgeCount] || (edgePool[edgeCount] = {}); edgeCount++;
            edge.a = a; edge.b = b;
            edge.minX = Math.min(a.x, a.x + a.mx, b.x, b.x + b.mx);
            edge.maxX = Math.max(a.x, a.x + a.mx, b.x, b.x + b.mx);
            edge.minY = Math.min(a.y, a.y + a.my, b.y, b.y + b.my);
            edge.maxY = Math.max(a.y, a.y + a.my, b.y, b.y + b.my);
            edge.cx = Math.floor(edge.minX / cellSize); edge.cy = Math.floor(edge.minY / cellSize);
            for (let y = edge.cy; y <= Math.floor(edge.maxY / cellSize); y++) {
              let row = rows.get(y);
              if (!row) { row = rowPool[rowCount] || (rowPool[rowCount] = new Map()); rowCount++; row.clear(); rows.set(y, row); }
              for (let x = edge.cx; x <= Math.floor(edge.maxX / cellSize); x++) {
                let bucket = row.get(x);
                if (!bucket) { bucket = bucketPool[bucketCount] || (bucketPool[bucketCount] = []); bucketCount++; bucket.length = 0; row.set(x, bucket); }
                bucket.push(edge);
              }
            }
            if (edgeCount % 128 === 0) yield;
          }
        }
        function hit(a, b) {
          if (a.a === b.a || a.a === b.b || a.b === b.a || a.b === b.b) return null;
          if (a.minX > b.maxX || a.maxX < b.minX || a.minY > b.maxY || a.maxY < b.minY) return null;
          let earliest = null;
          for (const candidate of [vertexEdgeHit(a.a, b.a, b.b), vertexEdgeHit(a.b, b.a, b.b),
            vertexEdgeHit(b.a, a.a, a.b), vertexEdgeHit(b.b, a.a, a.b)]) {
            if (candidate && (!earliest || candidate.time < earliest.time)) earliest = candidate;
          }
          return earliest;
        }
        return {
          // A read-only test for a proposed history chord. Ordinary floating-point
          // tolerances and the same valid-input limitations as the growth guard.
          intersects(rings) {
            for (const pause of grid(rings)) { /* drain the read-only broad phase */ }
            for (const [cy, row] of rows) for (const [cx, bucket] of row)
              for (let i = 0; i < bucket.length; i++) for (let j = i + 1; j < bucket.length; j++) {
                const a = bucket[i], b = bucket[j];
                if (cx === Math.max(a.cx, b.cx) && cy === Math.max(a.cy, b.cy) && hit(a, b)) return true;
              }
            return false;
          },
          *constrain(rings) {
            for (let pass = 0; pass < 12; pass++) {
              yield* grid(rings);
              let found = 0, visited = 0;
              for (const [cy, row] of rows) for (const [cx, bucket] of row)
                for (let i = 0; i < bucket.length; i++) for (let j = i + 1; j < bucket.length; j++) {
                  const a = bucket[i], b = bucket[j];
                  // Visit a pair in its first common grid cell, once per pass.
                  if (cx !== Math.max(a.cx, b.cx) || cy !== Math.max(a.cy, b.cy)) continue;
                  const collision = hit(a, b);
                  if (collision) {
                    found++; contacts++;
                    const { p, a, b, u, nx, ny } = collision;
                    if (pass >= 8) { p.mx = p.my = a.mx = a.my = b.mx = b.my = 0; }
                    else {
                      const signed = (p.x + p.mx - a.x - a.mx - u * (b.x + b.mx - a.x - a.mx)) * nx
                        + (p.y + p.my - a.y - a.my - u * (b.y + b.my - a.y - a.my)) * ny;
                      // Distribute the normal correction; retain tangential motion.
                      const impulse = Math.max(0, (.0125 - signed) / (1 + (1 - u) * (1 - u) + u * u));
                      p.mx += nx * impulse; p.my += ny * impulse;
                      a.mx -= nx * impulse * (1 - u); a.my -= ny * impulse * (1 - u);
                      b.mx -= nx * impulse * u; b.my -= ny * impulse * u;
                    }
                  }
                  if (++visited % 256 === 0) yield;
                }
              if (!found) return;
              yield;
            }
            // Conservative last resort: reject this positional step, not the whole
            // trajectory. Observable stats let regressions detect repeated stalling.
            for (const ring of rings) for (const p of ring.points) p.mx = p.my = 0;
            stops++;
          },
          get stats() { return { contacts, stops }; },
          dispose() { edgePool.length = 0; rows.clear(); rowPool.length = 0; bucketPool.length = 0; }
        };
      }

      // Original contact-aware temporal sampling. Growth positions are never edited.
      // Preserves the existing ~.2px trajectory fidelity in the 192px glyph frame,
      // while retaining extra computed steps when a straight playback chord crosses.

      function growthMaterialPoint(data, u, cursor) {
        while (cursor.i + 3 < data.length && data[cursor.i + 3] <= u) cursor.i += 3;
        const k = cursor.i, next = (k + 3) % data.length, nextU = next === 0 ? 1 : data[next];
        const w = (u - data[k]) / Math.max(Number.EPSILON, nextU - data[k]);
        cursor.x = data[k + 1] + (data[next + 1] - data[k + 1]) * w;
        cursor.y = data[k + 2] + (data[next + 2] - data[k + 2]) * w;
      }

      function createHistoryRefiner(radius, { tolerance = .2 } = {}) {
        if (!Number.isFinite(tolerance) || tolerance < 0) throw new RangeError('Expected finite temporal tolerance');
        const guard = createContactGuard(radius), scratch = [];
        let tests = 0, contactSplits = 0, errorSplits = 0;
        function motion(from, to) {
          scratch.length = to.rings.length;
          for (let ri = 0; ri < to.rings.length; ri++) {
            const data = to.rings[ri].data, previous = from.rings[ri].data, cursor = { i: 0 };
            const ring = scratch[ri] || (scratch[ri] = { points: [] }); ring.points.length = data.length / 3;
            for (let i = 0; i < data.length; i += 3) {
              growthMaterialPoint(previous, data[i], cursor);
              const p = ring.points[i / 3] || (ring.points[i / 3] = {});
              p.x = cursor.x; p.y = cursor.y; p.mx = data[i + 1] - p.x; p.my = data[i + 2] - p.y;
            }
          }
          return scratch;
        }
        function errorSquared(frame, from, to) {
          const t = (frame.step - from.step) / (to.step - from.step); let maximum = 0;
          for (let ri = 0; ri < to.rings.length; ri++) {
            const data = to.rings[ri].data, a = from.rings[ri].data, b = frame.rings[ri].data;
            const ca = { i: 0 }, cb = { i: 0 };
            for (let i = 0; i < data.length; i += 3) {
              growthMaterialPoint(a, data[i], ca); growthMaterialPoint(b, data[i], cb);
              const dx = ca.x + (data[i + 1] - ca.x) * t - cb.x;
              const dy = ca.y + (data[i + 2] - ca.y) * t - cb.y;
              maximum = Math.max(maximum, dx * dx + dy * dy);
            }
          }
          return maximum;
        }
        return {
          // Input is every computed step in one short interval, with ordered material
          // coordinates and stable ring identity. Return original frames, not morphs.
          select(frames) {
            const retained = [];
            function span(lo, hi) {
              if (hi - lo <= 1) { retained.push(frames[hi]); return; }
              const from = frames[lo], to = frames[hi]; let maximum = 0, split = (lo + hi) >> 1;
              for (let i = lo + 1; i < hi; i++) {
                const error = errorSquared(frames[i], from, to);
                if (error > maximum) { maximum = error; split = i; }
              }
              if (maximum > tolerance * tolerance) { errorSplits++; span(lo, split); span(split, hi); return; }
              tests++;
              if (guard.intersects(motion(from, to))) {
                contactSplits++; split = (lo + hi) >> 1; span(lo, split); span(split, hi); return;
              }
              retained.push(to);
            }
            if (frames.length > 1) span(0, frames.length - 1);
            return retained;
          },
          get stats() { return { tests, contactSplits, errorSplits }; },
          dispose() { guard.dispose(); scratch.length = 0; }
        };
      }

      function createGrowthHistory(radius) {
        const refiner = createHistoryRefiner(radius), frames = [], recent = [], free = [], owned = [];
        let bytes = 0, disposed = false;
        function retain(frame) {
          // Retained frames own exact-sized buffers; scratch capacity is never hidden
          // in a retained subarray or recycled beneath an existing playback sample.
          const saved = { step: frame.step, rings: frame.rings.map(r => {
            const data = new Float64Array(r.data); bytes += data.byteLength;
            return { area: r.area, data };
          }) };
          frames.push(saved);
        }
        return {
          frames,
          capture(rings, step) {
            if (disposed) throw new Error('Growth history capture is disposed');
            const frame = free.pop() || { step: 0, rings: [], buffers: [] };
            if (!owned.includes(frame)) owned.push(frame);
            frame.step = step; frame.rings.length = rings.length;
            for (let ri = 0; ri < rings.length; ri++) {
              const r = rings[ri], size = r.points.length * 3;
              let buffer = frame.buffers[ri];
              if (!buffer || buffer.length < size) buffer = frame.buffers[ri] = new Float64Array(Math.max(24, 2 ** Math.ceil(Math.log2(size))));
              const data = buffer.subarray(0, size);
              for (let i = 0; i < r.points.length; i++) {
                const p = r.points[i]; data[i * 3] = p.u; data[i * 3 + 1] = p.x; data[i * 3 + 2] = p.y;
              }
              frame.rings[ri] = { area: r.area, data };
            }
            if (!frames.length) { retain(frame); free.push(frame); recent.push(frames[0]); }
            else recent.push(frame);
          },
          commit() {
            if (recent.length <= 1) return;
            for (const frame of refiner.select(recent)) retain(frame);
            for (let i = 1; i < recent.length; i++) free.push(recent[i]);
            recent.length = 0; recent.push(frames[frames.length - 1]);
          },
          get pendingSteps() { return Math.max(0, recent.length - 1); },
          get bytes() { return bytes; },
          get captureBytes() { return owned.reduce((n, f) => n + f.buffers.reduce((n, b) => n + b.byteLength, 0), 0); },
          get stats() { return refiner.stats; },
          disposeScratch() { refiner.dispose(); recent.length = 0; free.length = 0; owned.length = 0; disposed = true; }
        };
      }

      // Original incremental growth runtime, embedded in the v39 editor.
      // solver.mjs is retained as the independent, pre-optimization geometry oracle.

      function normalizeGrowthSettings(settings = {}) {
        const normalized = (() => {
        const ranges = { age: [0, 5, 1], grain: [2.5, 12, 4], tension: [.18, 1.2, .8],
          memory: [0, .05, .007], patch: [0, 1, .8], areaGain: [.5, 2, 1.1], seed: [-2147483648, 2147483647, 31] };
        const result = {};
        for (const [key, [lo, hi, fallback]] of Object.entries(ranges)) {
          result[key] = Number.isFinite(settings[key]) ? Math.max(lo, Math.min(hi, settings[key])) : fallback;
        }
        return result;
      })();
        return globalThis.TypeDeformerParameters ? globalThis.TypeDeformerParameters.core('differential',settings,normalized) : normalized;
      }

      function createGrowthJob(contours, settings = {}, options = {}) {
        const s = normalizeGrowthSettings(settings);
        if (!Array.isArray(contours)) throw new TypeError('Expected contour array');
        for (const c of contours) {
          if (!Number.isFinite(c.area) || !Array.isArray(c.points) || c.points.length < 3
            || c.points.some(p => !Number.isFinite(p.x) || !Number.isFinite(p.y))) {
            throw new TypeError('Expected finite closed contours');
          }
        }
        const rand = (x, y) => {
          const v = Math.sin(x * 127.1 + y * 311.7 + s.seed * 91.37) * 43758.5453123;
          return v - Math.floor(v);
        };
        function noise(x, y) {
          const ix = Math.floor(x), iy = Math.floor(y); let u = x - ix, v = y - iy;
          u = u * u * (3 - 2 * u); v = v * v * (3 - 2 * v);
          return (rand(ix, iy) * (1 - u) + rand(ix + 1, iy) * u) * (1 - v)
            + (rand(ix, iy + 1) * (1 - u) + rand(ix + 1, iy + 1) * u) * v;
        }
        function node(x, y, ax, ay, rest, u) {
          // Anchors never move: their seeded nutrient value need only be computed once.
          const nutrient = (1 - s.patch) + s.patch * (.2 + 1.8 * noise(ax / 28, ay / 28));
          return { x, y, ax, ay, rest, u, growth: 1 + .0018 * nutrient,
            ri: 0, i: 0, dx: 0, dy: 0, mx: 0, my: 0, bucket: null };
        }
        const rings = contours.map(c => {
          const sampled = resample(c.points, s.grain * .8);
          return { area: c.area, points: sampled.map((p, i) => node(p.x, p.y, p.x, p.y, 0, i / sampled.length)) };
        });
        for (const r of rings) r.points.forEach((p, i) => {
          const q = r.points[(i + 1) % r.points.length]; p.rest = Math.hypot(p.x - q.x, p.y - q.y);
        });
        const totalSteps = rings.length ? Math.round(s.age * 960) : 0, initialPoints = rings.reduce((n, r) => n + r.points.length, 0);
        const pointBudget = Math.max(4096, initialPoints), radius = s.grain * 2.5, radiusSquared = radius * radius;
        const contactGuard = createContactGuard(radius);
        // Linearized fourth-difference bending has spectral radius 16*tension;
        // neighboring .6 springs add at most 2.4 and the anchor adds memory.
        // Keep h*lambda below 2 with headroom, avoiding a hidden period-two mode at
        // high bending. This is not a stability proof for the nonlinear contact field.
        const integrationStep = Math.min(.11, 1.8 / (16 * s.tension + 2.4 + s.memory));
        let livePoints = initialPoints, maxPoints = initialPoints, completedSteps = 0, budgetReached = false;
        let status = totalSteps && rings.length ? 'pending' : 'complete';
        const historyEvery = Number.isFinite(options.historyEvery) ? Math.max(1, Math.round(options.historyEvery)) : 12;
        const recorder = options.history !== false ? createGrowthHistory(radius) : null;
        const history = recorder ? recorder.frames : [];
        // Reused numeric rows, buckets and ordered neighbor lists. No packed-key aliases.
        const grid = new Map(), bucketPool = [], rowPool = [], all = [];
        function releaseScratch() {
          contactGuard.dispose();
          recorder?.disposeScratch();
          for (const r of rings) for (const p of r.points) p.bucket = null;
          grid.clear(); bucketPool.length = 0; rowPool.length = 0; all.length = 0;
        }
        function record() {
          recorder?.commit();
        }
        recorder?.capture(rings, 0);
        if (!totalSteps) releaseScratch();
        function* simulate() {
          for (let step = 0; step < totalSteps && status !== 'cancelled'; step++) {
            grid.clear(); all.length = 0; let rowCount = 0, bucketCount = 0;
            for (let ri = 0; ri < rings.length; ri++) {
              const ps = rings[ri].points;
              for (let i = 0; i < ps.length; i++) {
                const p = ps[i]; p.ri = ri; p.i = i; p.dx = 0; p.dy = 0; all.push(p);
                const cx = Math.floor(p.x / radius), cy = Math.floor(p.y / radius);
                let row = grid.get(cy);
                if (!row) { row = rowPool[rowCount] || (rowPool[rowCount] = new Map()); rowCount++; row.clear(); grid.set(cy, row); }
                let bucket = row.get(cx);
                if (!bucket) {
                  bucket = bucketPool[bucketCount] || (bucketPool[bucketCount] = { x: 0, y: 0, points: [], neighbors: [] });
                  bucketCount++; bucket.x = cx; bucket.y = cy; bucket.points.length = 0; bucket.neighbors.length = 0; row.set(cx, bucket);
                }
                bucket.points.push(p); p.bucket = bucket;
              }
            }
            for (let i = 0; i < bucketCount; i++) {
              const bucket = bucketPool[i];
              for (let oy = -1; oy <= 1; oy++) {
                const row = grid.get(bucket.y + oy); if (!row) continue;
                for (let ox = -1; ox <= 1; ox++) {
                  const neighbor = row.get(bucket.x + ox); if (neighbor) bucket.neighbors.push(neighbor.points);
                }
              }
            }
            yield;
            for (const r of rings) {
              const ps = r.points, n = ps.length; let area = 0, perimeter = 0;
              for (let j = 0; j < n; j++) {
                const a = ps[j], b = ps[(j + 1) % n];
                area += a.x * b.y - b.x * a.y; perimeter += Math.hypot(a.x - b.x, a.y - b.y);
              }
              const pressure = Math.max(-.5, Math.min(.5, (r.area * s.areaGain - area * .5) / Math.max(1, perimeter) * .09));
              for (let i = 0; i < n; i++) {
                const p = ps[i], q = ps[(i + 1) % n], a = ps[(i + n - 1) % n], aa = ps[(i + n - 2) % n], qq = ps[(i + 2) % n];
                p.rest = Math.min(s.grain * 1.5, p.rest * p.growth);
                const vx = q.x - p.x, vy = q.y - p.y, d = Math.max(.0001, Math.hypot(vx, vy)), force = (d - p.rest) * .6;
                p.dx += vx / d * force; p.dy += vy / d * force; q.dx -= vx / d * force; q.dy -= vy / d * force;
                const tx = q.x - a.x, ty = q.y - a.y, td = Math.max(.001, Math.hypot(tx, ty));
                p.dx += (-aa.x + 4 * a.x - 6 * p.x + 4 * q.x - qq.x) * s.tension + (p.ax - p.x) * s.memory + ty / td * pressure;
                p.dy += (-aa.y + 4 * a.y - 6 * p.y + 4 * q.y - qq.y) * s.tension + (p.ay - p.y) * s.memory - tx / td * pressure;
                for (const neighbors of p.bucket.neighbors) for (let bi = 0; bi < neighbors.length; bi++) {
                  const b = neighbors[bi];
                  if (b === p || b.ri === p.ri && (Math.abs(b.i - i) <= 3 || Math.abs(b.i - i) >= n - 3)) continue;
                  const bx = p.x - b.x, by = p.y - b.y;
                  if (bx * bx + by * by >= radiusSquared) continue;
                  const bd = Math.hypot(bx, by);
                  if (bd < radius && bd > 1e-5) {
                    const repel = (1 - bd / radius) ** 2 * .8; p.dx += bx / bd * repel; p.dy += by / bd * repel;
                  }
                }
                if (i % 32 === 31) yield;
              }
            }
            // Point repulsion alone misses an edge slipping between its neighbors.
            // Constrain proposed displacements before publishing any positions.
            for (const p of all) {
              const limit = Math.min(integrationStep, s.grain * .15 / Math.max(.0001, Math.hypot(p.dx, p.dy)));
              p.mx = p.dx * limit; p.my = p.dy * limit;
            }
            yield* contactGuard.constrain(rings);
            // Publish a whole step, never half-updated positions.
            for (const p of all) { p.x += p.mx; p.y += p.my; }
            if (step % 12 === 11) for (const r of rings) {
              const next = [];
              for (let i = 0; i < r.points.length; i++) {
                const p = r.points[i], q = r.points[(i + 1) % r.points.length]; next.push(p);
                if (Math.hypot(p.x - q.x, p.y - q.y) > s.grain * 1.25 && livePoints < pointBudget) {
                  p.rest *= .5; livePoints++;
                  next.push(node((p.x + q.x) / 2, (p.y + q.y) / 2, (p.ax + q.ax) / 2, (p.ay + q.ay) / 2, p.rest,
                    (p.u + (i + 1 === r.points.length ? 1 : q.u)) / 2));
                }
              }
              r.points = next;
            }
            maxPoints = Math.max(maxPoints, livePoints); budgetReached = budgetReached || livePoints === pointBudget;
            completedSteps = step + 1;
            recorder?.capture(rings, completedSteps);
            // Initial font corners relax much faster than mature growth. Preserve
            // that short transient densely instead of inflating the entire history.
            if (completedSteps <= 48 && completedSteps % 4 === 0 || completedSteps % historyEvery === 0
              || completedSteps === totalSteps || recorder?.pendingSteps >= 12) record();
            if (completedSteps === totalSteps) { status = 'complete'; releaseScratch(); }
            yield;
          }
        }
        const iterator = simulate();
        function snapshot() {
          return { rings: rings.map(r => ({ area: r.area, points: r.points.map(p => ({ x: p.x, y: p.y, u: p.u })) })),
            steps: completedSteps, totalSteps, initialPoints, maxPoints, pointBudget, budgetReached,
            contact: contactGuard.stats, history: recorder?.stats || null, settings: { ...s }, status };
        }
        const job = {
          get status() { return status; }, get completedSteps() { return completedSteps; }, get totalSteps() { return totalSteps; },
          get progress() { return totalSteps ? completedSteps / totalSteps : 1; },
          get historyBytes() { return recorder?.bytes || 0; }, get historyFrames() { return history.length; },
          get historyCaptureBytes() { return recorder?.captureBytes || 0; },
          advance({ budgetMs = 4, maxSteps = Infinity, now = () => performance.now(), signal } = {}) {
            if (signal?.aborted) job.cancel();
            budgetMs = budgetMs === Infinity ? Infinity : Number.isFinite(budgetMs) ? Math.max(0, budgetMs) : 4;
            maxSteps = Number.isFinite(maxSteps) ? Math.floor(maxSteps) : Infinity;
            if (status === 'complete' || status === 'cancelled' || budgetMs <= 0 || maxSteps <= 0) return job;
            const start = now(), target = Math.min(totalSteps, completedSteps + maxSteps); status = 'running';
            while (completedSteps < target) {
              if (signal?.aborted) { job.cancel(); break; }
              if (now() - start >= budgetMs) break;
              if (iterator.next().done) { status = 'complete'; break; }
            }
            if (status === 'running') status = 'paused';
            return job;
          },
          cancel() { if (status !== 'complete') { status = 'cancelled'; iterator.return(); releaseScratch(); } return job; },
          snapshot,
          checkpoint() { record(); return job; },
          sample(age) { return sampleGrowthHistory(history, Number.isFinite(age) ? Math.max(0, age) * 960 : 0); }
        };
        return job;
      }

      function sampleGrowthHistory(history, step) {
        if (!history.length) return [];
        let lo = 0, hi = history.length - 1;
        if (step <= history[lo].step) hi = lo;
        else if (step >= history[hi].step) lo = hi;
        else {
          while (hi - lo > 1) { const mid = (lo + hi) >> 1; if (history[mid].step <= step) lo = mid; else hi = mid; }
          if (step === history[lo].step) hi = lo;
        }
        const from = history[lo], to = history[hi];
        const t = from === to ? 0 : (step - from.step) / (to.step - from.step);
        return to.rings.map((ring, ri) => {
          const a = from.rings[ri].data, b = ring.data, points = []; let k = 0;
          for (let j = 0; j < b.length; j += 3) {
            const u = b[j]; while (k + 3 < a.length && a[k + 3] <= u) k += 3;
            const next = (k + 3) % a.length, nextU = next === 0 ? 1 : a[next];
            const w = (u - a[k]) / Math.max(Number.EPSILON, nextU - a[k]);
            const x = a[k + 1] + (a[next + 1] - a[k + 1]) * w, y = a[k + 2] + (a[next + 2] - a[k + 2]) * w;
            points.push({ x: x + (b[j + 1] - x) * t, y: y + (b[j + 2] - y) * t, u });
          }
          return { area: ring.area, points };
        });
      }

      function growthLoopAge(phase, age, depth = 1) {
        const p = Number.isFinite(phase) ? phase - Math.floor(phase) : 0;
        return Math.max(0, Math.min(5, Number.isFinite(age) ? age : 0))
          * (1 - Math.max(0, Math.min(1, Number.isFinite(depth) ? depth : 0)) * (1 - Math.cos(p * Math.PI * 2)) / 2);
      }

      function grow(contours, settings = {}) {
        const job = createGrowthJob(contours, settings, { history: false });
        job.advance({ budgetMs: Infinity });
        return job.snapshot();
      }

      // Yield a task, not only a microtask. A UI adapter may inject its own scheduler.
      async function runGrowthJob(job, { budgetMs = 4, signal, onProgress, yieldTask } = {}) {
        const pause = yieldTask || (() => globalThis.scheduler?.yield ? globalThis.scheduler.yield()
          : new Promise(resolve => setTimeout(resolve, 0)));
        try {
          while (job.status !== 'complete' && job.status !== 'cancelled') {
            job.advance({ budgetMs: Number.isFinite(budgetMs) ? Math.max(.1, Math.min(12, budgetMs)) : 4, signal });
            if (signal?.aborted) job.cancel();
            if (job.status === 'cancelled') break;
            onProgress?.(job);
            if (job.status !== 'complete') await pause();
          }
        } catch (error) { job.cancel(); throw error; }
        return job;
      }

      // Shared, caller-owned leases. The v39 editor owns scheduling. No timers or DOM.

      function createGrowthPool({ idleEntries = 8, idleHistoryBytes = 16 * 1024 * 1024, historyEvery = 12 } = {}) {
        idleEntries = Number.isFinite(idleEntries) ? Math.max(0, Math.floor(idleEntries)) : 8;
        idleHistoryBytes = Number.isFinite(idleHistoryBytes) ? Math.max(0, idleHistoryBytes) : 16 * 1024 * 1024;
        historyEvery = Number.isFinite(historyEvery) ? Math.max(1, Math.floor(historyEvery)) : 12;
        const entries = new Map();
        let disposed = false, sequence = 0, cursor = 0;
        const clampAge = age => Number.isFinite(age) ? Math.max(0, Math.min(5, age)) : 0;
        const requiredStep = age => Math.min(4800, Math.ceil(clampAge(age) * 960 / historyEvery) * historyEvery);
        function trim() {
          const idle = [...entries.values()].filter(entry => !entry.leases.size).sort((a, b) => a.used - b.used);
          let bytes = idle.reduce((n, entry) => n + entry.job.historyBytes, 0);
          while (idle.length > idleEntries || bytes > idleHistoryBytes) {
            const entry = idle.shift(); if (!entry) break;
            bytes -= entry.job.historyBytes; entry.job.cancel(); entries.delete(entry.key);
          }
        }
        return {
          acquire(sourceKey, contours, settings = {}) {
            if (disposed) throw new Error('Growth pool is disposed');
            if (typeof sourceKey !== 'string' || !sourceKey) throw new TypeError('A stable font/mask/glyph identity is required');
            const normalized = normalizeGrowthSettings(settings), requestedAge = normalized.age;
            // The complete trajectory is independent of current age. Work only as far
            // as requested, on canonical history boundaries, so access order cannot
            // introduce arbitrary checkpoints that change subsequent interpolation.
            normalized.age = 5;
            const key = JSON.stringify([sourceKey, normalized]);
            let entry = entries.get(key);
            if (!entry) {
              entry = { key, job: createGrowthJob(contours, normalized, { historyEvery }), leases: new Map(), used: ++sequence };
              entries.set(key, entry);
            }
            const token = Symbol(), request = { age: requestedAge }; let released = false;
            entry.leases.set(token, request); entry.used = ++sequence;
            function assertLive() { if (released || disposed) throw new Error('Growth lease is released'); }
            return {
              request(age) { assertLive(); request.age = clampAge(age); entry.used = ++sequence; return this; },
              get state() {
                if (released || disposed) return { status: 'released', progress: 0 };
                const target = requiredStep(request.age), available = entry.job.completedSteps;
                const ready = !target || entry.job.status === 'complete' || available >= target;
                return { status: ready ? 'ready' : 'pending', age: request.age, availableAge: available / 960,
                  progress: target ? Math.min(1, available / target) : 1 };
              },
              sample(age = request.age) {
                assertLive();
                // Playback reads an earlier point without lowering the preparation
                // target. A complete Compose loop must be prepared before recording.
                age = clampAge(age);
                return this.state.status === 'ready' && age <= request.age ? entry.job.sample(age) : null;
              },
              release() {
                if (released) return;
                released = true; entry.leases.delete(token); entry.used = ++sequence; trim();
              }
            };
          },
          advance({ budgetMs = 4, now = () => performance.now() } = {}) {
            if (disposed) return this;
            budgetMs = Number.isFinite(budgetMs) ? Math.max(0, Math.min(12, budgetMs)) : 4;
            const start = now();
            const pending = [...entries.values()].filter(entry => entry.leases.size && entry.job.status !== 'complete'
              && [...entry.leases.values()].some(request => requiredStep(request.age) > entry.job.completedSteps));
            // One shared time budget, not four milliseconds multiplied by glyph count.
            for (let visited = 0; visited < pending.length; visited++) {
              const remaining = budgetMs - (now() - start); if (remaining <= 0) break;
              const entry = pending[cursor % pending.length]; cursor++;
              const target = Math.max(...[...entry.leases.values()].map(request => requiredStep(request.age)));
              entry.job.advance({ budgetMs: remaining, maxSteps: target - entry.job.completedSteps, now });
            }
            trim(); return this;
          },
          get stats() {
            const values = [...entries.values()];
            return { entries: values.length, activeEntries: values.filter(entry => entry.leases.size).length,
              leases: values.reduce((n, entry) => n + entry.leases.size, 0),
              historyBytes: values.reduce((n, entry) => n + entry.job.historyBytes, 0),
              idleHistoryBytes: values.filter(entry => !entry.leases.size).reduce((n, entry) => n + entry.job.historyBytes, 0),
              completedSteps: values.reduce((n, entry) => n + entry.job.completedSteps, 0), disposed };
          },
          dispose() {
            for (const entry of entries.values()) entry.job.cancel();
            entries.clear(); disposed = true;
          }
        };
      }

      // Editor preparation state. No DOM and no scheduler: the host owns one rAF.

      function growthRequestKey(sourceKey, settings) {
        return JSON.stringify([sourceKey, { ...normalizeGrowthSettings(settings), age: 5 }]);
      }

      function createGrowthController({ historyLimit = 64 * 1024 * 1024, now = () => performance.now() } = {}) {
        let pool = createGrowthPool({ idleEntries: 4, idleHistoryBytes: 8 * 1024 * 1024 });
        let records = new Map(), paused = false, memoryPaused = false, error = '', disposed = false;
        let limit = historyLimit;
        function state() {
          const values = [...records.values()];
          const ready = values.filter(r => r.lease && r.lease.state.status === 'ready').length;
          const progress = values.length ? values.reduce((n, r) => n + (r.lease ? r.lease.state.progress : 0), 0) / values.length : 1;
          return { total: values.length, ready, progress, pending: ready < values.length,
            paused, memoryPaused, error, historyBytes: pool.stats.historyBytes, historyLimit: limit, disposed };
        }
        return {
          sync(requests) {
            if (disposed) throw new Error('Growth controller is disposed');
            const desired = new Map();
            for (const request of requests) {
              const settings = normalizeGrowthSettings(request.settings);
              if (!settings.age) continue;
              const key = growthRequestKey(request.sourceKey, settings), previous = desired.get(key);
              if (!previous || previous.settings.age < settings.age) desired.set(key, { ...request, settings });
            }
            let changed = records.size !== desired.size;
            for (const [key, record] of records) if (!desired.has(key)) {
              if (record.lease) record.lease.release(); records.delete(key); changed = true;
            }
            for (const [key, request] of desired) {
              const record = records.get(key);
              if (record) {
                if (record.settings.age !== request.settings.age) changed = true;
                record.settings = request.settings;
                if (record.lease) record.lease.request(request.settings.age);
              } else { records.set(key, { ...request, lease: null, data: null }); changed = true; }
            }
            if (changed) error = '';
            if (!records.size) { paused = false; memoryPaused = false; limit = historyLimit; }
            if (memoryPaused && pool.stats.historyBytes < limit) memoryPaused = false;
            if (!this.state.pending) memoryPaused = false;
            return this.state;
          },
          advance(budgetMs = 4) {
            if (disposed || paused || error) return this.state;
            if (pool.stats.historyBytes >= limit) { memoryPaused = true; return this.state; }
            if (memoryPaused) return this.state;
            const start = now();
            try {
              // At most one mask/resample initialization per task. Repeated instances
              // use one record. No mask extraction happens inside the per-glyph draw.
              for (const record of records.values()) if (!record.lease) {
                record.data = record.load();
                if (!record.data || !record.data.contours.length) throw new Error('この字形の輪郭を取得できません。フォントを変更してください。');
                record.lease = pool.acquire(record.sourceKey, record.data.contours, record.settings);
                break;
              }
              pool.advance({ budgetMs: Math.max(0, budgetMs - (now() - start)), now });
            } catch (failure) { error = failure.message || '輪郭の計算に失敗しました。'; }
            if (pool.stats.historyBytes >= limit && this.state.pending) memoryPaused = true;
            return this.state;
          },
          read(sourceKey, settings, age = settings.age) {
            const record = records.get(growthRequestKey(sourceKey, settings));
            if (!record || !record.lease || record.lease.state.status !== 'ready') return null;
            const rings = record.lease.sample(age);
            return rings ? { data: record.data, rings } : null;
          },
          isReady(sourceKey, settings) {
            if (settings.age <= 0) return true;
            const record = records.get(growthRequestKey(sourceKey, settings));
            return !!(record && record.settings.age >= settings.age && record.lease && record.lease.state.status === 'ready');
          },
          setPaused(value) { paused = !!value; return this.state; },
          allowMoreMemory() { limit = Math.max(limit * 2, pool.stats.historyBytes + historyLimit); memoryPaused = false; return this.state; },
          get state() { return state(); },
          reset() {
            pool.dispose(); records.clear(); pool = createGrowthPool({ idleEntries: 4, idleHistoryBytes: 8 * 1024 * 1024 });
            paused = memoryPaused = false; error = ''; limit = historyLimit;
          },
          dispose() { pool.dispose(); records.clear(); disposed = true; }
        };
      }
      return { normalizeGrowthSettings, createGrowthJob, sampleGrowthHistory, growthLoopAge, createGrowthPool, growthRequestKey, createGrowthController };
      })();

var differentialController = DifferentialGrowth.createGrowthController();

var differentialFontRevision = 0;

var differentialMaskCache = new Map();

var differentialMaskPoints = 0;

function differentialGlyphSettings(g) {
        var profile = g.surface || {};
        function value(key) { return Number.isFinite(profile[key]) ? profile[key] : params[key]; }
        return DifferentialGrowth.normalizeGrowthSettings({ age: value('differentialAge'), grain: value('differentialGrain'),
          tension: value('differentialTension'), memory: value('differentialMemory'), patch: value('differentialPatch'),
          areaGain: value('differentialArea'), seed: ((params.seed * 131 + (String(g.ch).codePointAt(0) || 0)) | 0) });
      }

function differentialSourceKey(ch,g){var sourceSize=globalThis.TypeDeformerDraftMode?64:192;return differentialFontRevision+':'+sourceSize+':' +(g&&g.fontAxes?glyphFontSpec(g,sourceSize).key:params.fontWeight+' '+sourceSize+'px '+params.fontFamily)+'\u0000'+ch;}

function differentialGlyphData(ch,fontSpec){
    var sourceSize=globalThis.TypeDeformerDraftMode?64:192,unit=sourceSize/192,key=fontSpec?fontSpec.key:differentialSourceKey(ch);
    if(differentialMaskCache.has(key))return differentialMaskCache.get(key);
    var canvas=document.createElement('canvas'),ctx=canvas.getContext('2d',{willReadFrequently:true}),font=fontSpec&&fontSpec.glyph?glyphFontSpec(fontSpec.glyph,sourceSize).font:params.fontWeight+' '+sourceSize+'px '+params.fontFamily;
    ctx.font=font;var measure=ctx.measureText(ch);ctx.textBaseline='middle';var middle=ctx.measureText(ch),middleOffset=Number.isFinite(middle.actualBoundingBoxAscent)&&Number.isFinite(measure.actualBoundingBoxAscent)?(measure.actualBoundingBoxAscent-middle.actualBoundingBoxAscent)/unit:192*.35;
    var left=Math.ceil(Math.max(0,measure.actualBoundingBoxLeft||0))+4,ascent=Math.ceil(Math.max(1,measure.actualBoundingBoxAscent||sourceSize))+4;canvas.width=Math.max(8,Math.min(4096,left+Math.ceil(Math.max(measure.width,measure.actualBoundingBoxRight||0))+4));canvas.height=ascent+Math.ceil(Math.max(0,measure.actualBoundingBoxDescent||0))+4;
    ctx.font=font;ctx.textBaseline='alphabetic';ctx.fillStyle='#fff';ctx.fillText(ch,left,ascent);var contours=spectralTraceContours(ctx.getImageData(0,0,canvas.width,canvas.height).data,canvas.width,canvas.height),count=0;
    contours=contours.map(function(contour){count+=contour.points.length;return {area:contour.area/(unit*unit),points:contour.points.map(function(p){return {x:(p.x-left)/unit,y:(p.y-ascent)/unit};})};});
    var result={contours:contours,advance:measure.width/unit,middleOffset:middleOffset,ascent:(ascent-4)/unit,descent:Math.max(0,measure.actualBoundingBoxDescent||0)/unit,pointCount:count};
    while(differentialMaskCache.size&&(differentialMaskCache.size>=32||differentialMaskPoints+count>65536)){var oldest=differentialMaskCache.keys().next().value;differentialMaskPoints-=differentialMaskCache.get(oldest).pointCount;differentialMaskCache.delete(oldest);}if(count<=65536){differentialMaskCache.set(key,result);differentialMaskPoints+=count;}return result;
  }

function differentialRequests(glyphs) {
        var requests = new Map();
        for (var i = 0; i < glyphs.length; i++) {
          var g = glyphs[i];
          // Invisible Compose instances still prepare their complete trajectory, so
          // a later frame cannot start a new simulation halfway through recording.
          if (surfaceGlyphStrength(g, 'differentialType') <= .002 || !String(g.ch || '').trim()) continue;
          var settings = differentialGlyphSettings(g); if (settings.age <= 0) continue;
          var sourceKey = differentialSourceKey(g.ch,g), key = DifferentialGrowth.growthRequestKey(sourceKey, settings);
          var previous = requests.get(key);
          if (!previous || previous.settings.age < settings.age) requests.set(key, {
            sourceKey: sourceKey, settings: settings, load: differentialGlyphData.bind(null, g.ch,
              { key: sourceKey, font: params.fontWeight + ' 192px ' + params.fontFamily, glyph: g.fontAxes ? g : null })
          });
        }
        return Array.from(requests.values());
      }

function differentialScheduleWork(){}

function differentialPrepare(glyphs) {
        if(globalThis.TypeDeformerRenderJobs&&typeof Worker!=='undefined'&&!globalThis.TypeDeformerWorkerRuntime)return;
        differentialController.sync(differentialRequests(glyphs)); differentialScheduleWork();
      }

function differentialAssertReady(glyphs) {
        if(globalThis.TypeDeformerRenderJobs&&typeof Worker!=='undefined'&&!globalThis.TypeDeformerWorkerRuntime)return;
        var requests = differentialRequests(glyphs);
        for (var i = 0; i < requests.length; i++) if (!differentialController.isReady(requests[i].sourceKey, requests[i].settings)) {
          var failure = new Error('Differential Typeの成長計算が未完了です。キャンバスの「準備できました」を確認して、もう一度書き出してください。');
          failure.code = 'GROWTH_PENDING'; throw failure;
        }
      }

function differentialShape(g, settings) {
        var profile = g.surface || {}, depth = Number.isFinite(profile.differentialMotion) ? profile.differentialMotion : params.differentialMotion;
        var age = compositionState.enabled ? DifferentialGrowth.growthLoopAge(compositionState.phase, settings.age, depth) : settings.age;
        if (age <= .00001) return null;
        return differentialController.read(differentialSourceKey(g.ch,g), settings, age);
      }

function differentialEffectPad(glyphs) {
        var extra = 0, extents = new Map();
        for (var i = 0; i < glyphs.length; i++) {
          var g = glyphs[i]; if (surfaceGlyphStrength(g, 'differentialType') <= .002 || !String(g.ch || '').trim()) continue;
          var settings = differentialGlyphSettings(g), key = JSON.stringify([differentialSourceKey(g.ch,g), settings, g.surface && g.surface.differentialMotion]);
          if (!extents.has(key)) {
            var shape = differentialShape(g, settings), extent = -1;
            if (shape) {
              var d = shape.data; extent = 0;
              for (var r = 0; r < shape.rings.length; r++) for (var p = 0; p < shape.rings[r].points.length; p++) {
                var point = shape.rings[r].points[p];
                extent = Math.max(extent, -point.x, point.x - d.advance, -point.y - d.ascent, point.y - d.descent);
              }
            }
            extents.set(key, extent);
          }
          var extent = extents.get(key); if (extent < 0) continue;
          var stretch = Math.max(Math.abs(g.scaleX || 1), Math.abs(g.scaleY || 1));
          var shear = 1 + Math.abs(Math.tan((g.skewX || 0) * Math.PI / 180)) + Math.abs(Math.tan((g.skewY || 0) * Math.PI / 180));
          extra = Math.max(extra, (extent + 8) * params.fontSize / 192 * stretch * shear * Math.SQRT2);
        }
        return extra;
      }

function renderDifferentialType(targetCtx, glyphs, width, height, pixelScale, L, fm, coverBase) {
        if (coverBase) compositeSurfaceSource(targetCtx, buildSurfaceMask(glyphs, 'differentialType', width, height, pixelScale, L, fm), 'differentialType', true);
        var work = surfaceScratch('differential-type-v39-body', width, height, false), ctx = work.ctx;
        var color = surfaceEffectColor('differentialType'), samples = new Map();
        for (var i = 0; i < glyphs.length; i++) {
          var g = glyphs[i], strength = surfaceGlyphStrength(g, 'differentialType');
          if (strength <= .002 || (g.opacity != null && g.opacity <= .002) || !String(g.ch || '').trim()) continue;
          var settings = differentialGlyphSettings(g), key = JSON.stringify([differentialSourceKey(g.ch,g), settings, g.surface && g.surface.differentialMotion]);
          if (!samples.has(key)) samples.set(key, differentialShape(g, settings));
          var shape = samples.get(key);
          // Age zero retains native hinting/baseline. Pending preview is explicitly
          // native; strict render entry points reject it before drawing/exporting.
          if (!shape) { drawSurfaceGlyph(ctx, g, pixelScale, L, fm, strength, color); continue; }
          ctx.save(); spectralGlyphFrame(ctx, g, pixelScale, L, fm, shape.data);
          ctx.globalAlpha = strength * (g.opacity == null ? 1 : g.opacity); ctx.fillStyle = color;
          function trace(ring) {
            var points = ring.points; ctx.moveTo(points[0].x, points[0].y);
            for (var p = 1; p < points.length; p++) ctx.lineTo(points[p].x, points[p].y); ctx.closePath();
          }
          ctx.beginPath(); shape.rings.forEach(function (ring) { if (ring.area > 0) trace(ring); });
          ctx.clip(); ctx.beginPath(); shape.rings.forEach(trace); ctx.fill('evenodd'); ctx.restore();
        }
        targetCtx.save(); targetCtx.setTransform(1, 0, 0, 1, 0, 0);
        targetCtx.drawImage(work.canvas, 0, 0, targetCtx.canvas.width, targetCtx.canvas.height); targetCtx.restore();
      }

var MarblingType = (function () {
      // Lexical intrinsic bindings avoid repeated global-proxy lookups in hot interval arithmetic.
      var Math = globalThis.Math, Number = globalThis.Number, Object = globalThis.Object, Array = globalThis.Array;
      var performance = globalThis.performance;
      var modules = [];
      modules[0] = (function () {
      // Trial: integrate actual signed ink in the two anatomy sites' Voronoi cells.
      // Moments are measured around the tool site (not just its empty counter).
      function clipCell(points, site, other) {
        const nx = other.x - site.x, ny = other.y - site.y;
        if (nx === 0 && ny === 0) return points;
        const mx = site.x + nx / 2, my = site.y + ny / 2;
        const distance = p => (p.x - mx) * nx + (p.y - my) * ny;
        const out = [];
        for (let i = 0; i < points.length; i++) {
          const a = points[i], b = points[(i + 1) % points.length], da = distance(a), db = distance(b);
          if (da <= 0) out.push(a);
          if ((da <= 0) !== (db <= 0)) {
            const t = da / (da - db); out.push({ x: a.x + (b.x - a.x) * t, y: a.y + (b.y - a.y) * t });
          }
        }
        return out;
      }

      function marblingInkSupport(rings, anchors) {
        if (!Array.isArray(rings) || !Array.isArray(anchors) || anchors.length !== 2) throw new TypeError('Expected rings and two sites');
        return Object.freeze(anchors.map((site, index) => {
          let area = 0, x = 0, y = 0, xx = 0, xy = 0, yy = 0;
          for (const ring of rings) {
            const points = clipCell(ring.points, site, anchors[1 - index]);
            for (let i = 0; i < points.length; i++) {
              const a = points[i], b = points[(i + 1) % points.length];
              const ax = a.x - site.x, ay = a.y - site.y, bx = b.x - site.x, by = b.y - site.y;
              const cross = ax * by - bx * ay;
              area += cross / 2; x += (ax + bx) * cross / 6; y += (ay + by) * cross / 6;
              xx += (ax * ax + ax * bx + bx * bx) * cross / 12;
              xy += (2 * ax * ay + ax * by + bx * ay + 2 * bx * by) * cross / 24;
              yy += (ay * ay + ay * by + by * by) * cross / 12;
            }
          }
          if (Math.abs(area) < 1e-10) return Object.freeze({ ...site, area: 0, radius: 1, stretch: 1, angle: 0, covariance: Object.freeze([0, 0, 0]), centroid: Object.freeze({ ...site }) });
          const momentXX = Math.max(0, xx / area), momentYY = Math.max(0, yy / area), momentXY = xy / area;
          const trace = momentXX + momentYY, split = Math.hypot(momentXX - momentYY, 2 * momentXY);
          const major = Math.max(0, (trace + split) / 2), minor = Math.max(0, (trace - split) / 2);
          const regularizer = Math.max(1e-8, trace * .08), a = major + regularizer, b = minor + regularizer;
          return Object.freeze({ ...site, area: Math.abs(area), radius: 2.4 * Math.pow(a * b, .25),
            stretch: Math.pow(a / b, .25), angle: split > trace * 1e-10 ? Math.atan2(2 * momentXY, momentXX - momentYY) / 2 : 0,
            covariance: Object.freeze([momentXX, momentXY, momentYY]), centroid: Object.freeze({ x: site.x + x / area, y: site.y + y / area }) });
        }));
      }
      return {marblingInkSupport};
      })();
      modules[1] = (function () {
      // Original, stateless glyph-material advection prototype. This is NOT a fluid
      // solver, a tracing of a reference font, or an editor-registered operator yet.
      // Elementary shears and radius-dependent twists have explicit inverses and
      // determinant one. Their composition preserves the continuous ink domain.
      // A polygonal approximation is a separate, tolerance-bounded representation.
      const { marblingInkSupport } = modules[0];
      const TAU = Math.PI * 2;
      const finite = (v, d, lo, hi) => Number.isFinite(v) ? Math.max(lo, Math.min(hi, v)) : d;

      const MARBLING_MODES = Object.freeze(['rake', 'eddy', 'plume']);
      const MARBLING_PRESETS = Object.freeze({
        rake: Object.freeze({ mode: 'rake', amount: .65, pitch: 76, focus: .55, circulation: .9, angle: -18, motion: .55 }),
        eddy: Object.freeze({ mode: 'eddy', amount: .5, pitch: 118, focus: .35, circulation: 2.1, angle: -20, motion: .55 }),
        plume: Object.freeze({ mode: 'plume', amount: .75, pitch: 98, focus: .7, circulation: 1.8, angle: -12, motion: .55 }),
      });

      function normalizeMarblingSettings(input = {}) {
        const normalized = (() => {
        return Object.freeze({
          mode: MARBLING_MODES.includes(input.mode) ? input.mode : 'rake',
          amount: finite(input.amount, .65, 0, 4),
          pitch: finite(input.pitch, 76, 12, 180),
          focus: finite(input.focus, .55, 0, 1),
          circulation: finite(input.circulation, .9, -4, 4),
          angle: finite(input.angle, -18, -180, 180),
          motion: finite(input.motion, 0, 0, 1),
        });
      })();
        return globalThis.TypeDeformerParameters ? globalThis.TypeDeformerParameters.core('marbling',input,normalized) : normalized;
      }

      function marblingSignedArea(points) {
        if (!points.length) return 0;
        const origin = points[0]; let sum = 0;
        for (let i = 0; i < points.length; i++) {
          const a = points[i], b = points[(i + 1) % points.length];
          sum += (a.x - origin.x) * (b.y - origin.y) - (b.x - origin.x) * (a.y - origin.y);
        }
        return sum / 2;
      }

      function moments(points) {
        if (points.length < 3) return { area: 0, x: 0, y: 0 };
        const origin = points[0]; let twiceArea = 0, x = 0, y = 0;
        for (let i = 0; i < points.length; i++) {
          const a = points[i], b = points[(i + 1) % points.length];
          const ax = a.x - origin.x, ay = a.y - origin.y, bx = b.x - origin.x, by = b.y - origin.y;
          const cross = ax * by - bx * ay;
          twiceArea += cross; x += (ax + bx) * cross; y += (ay + by) * cross;
        }
        return Math.abs(twiceArea) < 1e-12 ? { area: 0, x: origin.x, y: origin.y }
          : { area: twiceArea / 2, x: origin.x + x / (3 * twiceArea), y: origin.y + y / (3 * twiceArea) };
      }

      // Signed polygon moments after clipping to a half-plane recover actual ink
      // mass above/below the glyph centre, including counter subtraction. No random
      // glyph hash or ornamental points substitute for the source anatomy.
      function halfPlane(points, axisY, sign) {
        const out = [];
        for (let i = 0; i < points.length; i++) {
          const a = points[i], b = points[(i + 1) % points.length];
          const insideA = sign * (a.y - axisY) >= 0, insideB = sign * (b.y - axisY) >= 0;
          if (insideA) out.push(a);
          if (insideA !== insideB) {
            const t = (axisY - a.y) / (b.y - a.y);
            out.push({ x: a.x + (b.x - a.x) * t, y: axisY });
          }
        }
        return out;
      }

      function anatomyAnchors(rings, cx, cy) {
        const dominant = rings.reduce((best, ring) => Math.abs(ring.area) > Math.abs(best?.area || 0) ? ring : best, null);
        const orientation = Math.sign(dominant?.area || 1);
        const holes = rings.filter(ring => ring.area * orientation < -1e-6)
          .map(ring => moments(ring.points)).sort((a, b) => Math.abs(b.area) - Math.abs(a.area));
        const halves = [-1, 1].map(sign => {
          let area = 0, x = 0, y = 0;
          for (const ring of rings) {
            const part = moments(halfPlane(ring.points, cy, sign));
            area += part.area; x += (part.x - cx) * part.area; y += (part.y - cy) * part.area;
          }
          return Math.abs(area) > 1e-8 ? { x: cx + x / area, y: cy + y / area } : { x: cx, y: cy };
        });
        let anchors;
        if (holes.length >= 2) anchors = holes.slice(0, 2);
        else if (holes.length === 1) {
          const hole = holes[0];
          const other = halves.reduce((a, b) => Math.hypot(a.x - hole.x, a.y - hole.y) > Math.hypot(b.x - hole.x, b.y - hole.y) ? a : b);
          anchors = [hole, other];
        } else anchors = halves;
        return Object.freeze(anchors.sort((a, b) => a.y - b.y || a.x - b.x).map(p => Object.freeze({ x: p.x, y: p.y })));
      }

      function prepareMarblingGlyph(contours) {
        if (!Array.isArray(contours)) throw new TypeError('Marbling: contours must be an array');
        let minX = Infinity, minY = Infinity, maxX = -Infinity, maxY = -Infinity, count = 0;
        const rings = contours.map(contour => {
          if (!Array.isArray(contour?.points) || contour.points.length < 3) throw new TypeError('Marbling: each ring needs at least three points');
          const points = contour.points.map(point => {
            if (!Number.isFinite(point?.x) || !Number.isFinite(point?.y)) throw new TypeError('Marbling: non-finite source coordinate');
            if (++count > 131072) throw new RangeError('Marbling: source point budget exceeded');
            minX = Math.min(minX, point.x); maxX = Math.max(maxX, point.x);
            minY = Math.min(minY, point.y); maxY = Math.max(maxY, point.y);
            return Object.freeze({ x: point.x, y: point.y });
          });
          return Object.freeze({ points: Object.freeze(points), area: marblingSignedArea(points) });
        });
        const cx = count ? minX + (maxX - minX) / 2 : 0;
        const cy = count ? minY + (maxY - minY) / 2 : 0;
        if (![cx, cy, maxX - minX, maxY - minY].every(Number.isFinite) && count) throw new RangeError('Marbling: source extent overflow');
        const anchors = anatomyAnchors(rings, cx, cy);
        return Object.freeze({ rings: Object.freeze(rings), cx, cy,
          anchors, eddySites: marblingInkSupport(rings, anchors),
          width: count ? maxX - minX : 0, height: count ? maxY - minY : 0, pointCount: count });
      }

      // Small outward allowance mitigates double precision rounding; these are
      // numerical interval estimates, not a machine-checked exact-arithmetic proof.
      const outward = (lo, hi) => {
        if (!Number.isFinite(lo) || !Number.isFinite(hi)) return [-Infinity, Infinity];
        const pad = (1 + Math.max(Math.abs(lo), Math.abs(hi))) * Number.EPSILON * 8;
        return [lo - pad, hi + pad];
      };
      const iadd = (a, b) => outward(a[0] + b[0], a[1] + b[1]);
      const ineg = a => [-a[1], -a[0]];
      const iscale = (a, s) => s === 0 ? [0, 0] : s > 0 ? outward(a[0] * s, a[1] * s) : outward(a[1] * s, a[0] * s);
      function imul(a, b) {
        const aa = a[0] * b[0], ab = a[0] * b[1], ba = a[1] * b[0], bb = a[1] * b[1];
        if (Number.isNaN(aa) || Number.isNaN(ab) || Number.isNaN(ba) || Number.isNaN(bb)) return [-Infinity, Infinity];
        return outward(Math.min(aa, ab, ba, bb), Math.max(aa, ab, ba, bb));
      }
      function isquare(a) {
        return outward(a[0] <= 0 && a[1] >= 0 ? 0 : Math.min(a[0] ** 2, a[1] ** 2), Math.max(a[0] ** 2, a[1] ** 2));
      }
      function isin(a) {
        if (!a.every(Number.isFinite) || a[1] - a[0] >= TAU) return [-1, 1];
        let lo = Math.min(Math.sin(a[0]), Math.sin(a[1])), hi = Math.max(Math.sin(a[0]), Math.sin(a[1]));
        const first = Math.ceil((a[0] - Math.PI / 2) / Math.PI);
        const last = Math.floor((a[1] - Math.PI / 2) / Math.PI);
        for (let k = first; k <= last; k++) if (k % 2 === 0) hi = 1; else lo = -1;
        return outward(lo, hi);
      }
      const icos = a => isin(iadd(a, [Math.PI / 2, Math.PI / 2]));
      const iexp = a => outward(Math.exp(a[0]), Math.exp(a[1]));
      const imax = a => Math.max(Math.abs(a[0]), Math.abs(a[1]));
      const jet = (a, b) => ({ v: [Math.min(a, b), Math.max(a, b)], d: [b - a, b - a], dd: [0, 0] });
      const add = (a, b) => ({ v: iadd(a.v, b.v), d: iadd(a.d, b.d), dd: iadd(a.dd, b.dd) });
      const scale = (a, s) => ({ v: iscale(a.v, s), d: iscale(a.d, s), dd: iscale(a.dd, s) });
      // Constant translation needs no temporary constant jet. Keep even the +0
      // operations and outward allowances, so every interval endpoint is unchanged.
      const shift = (a, b) => ({
        v: outward(a.v[0] + b, a.v[1] + b),
        d: outward(a.d[0] + 0, a.d[1] + 0),
        dd: outward(a.dd[0] + 0, a.dd[1] + 0),
      });
      function multiply(a, b) {
        return { v: imul(a.v, b.v), d: iadd(imul(a.d, b.v), imul(a.v, b.d)),
          dd: iadd(iadd(imul(a.dd, b.v), iscale(imul(a.d, b.d), 2)), imul(a.v, b.dd)) };
      }
      function square(a) {
        return { v: isquare(a.v), d: iscale(imul(a.v, a.d), 2), dd: iscale(iadd(isquare(a.d), imul(a.v, a.dd)), 2) };
      }
      function unary(a, v, derivative, second) {
        return { v, d: imul(derivative, a.d), dd: iadd(imul(second, isquare(a.d)), imul(derivative, a.dd)) };
      }
      function sinCos(a) {
        const s = isin(a.v), c = icos(a.v);
        return { sin: unary(a, s, c, ineg(s)), cos: unary(a, c, ineg(s), ineg(c)) };
      }
      const exp = a => { const value = iexp(a.v); return unary(a, value, value, value); };

      function shear(cx, cy, angle, amplitude, pitch, focus, phase) {
        const co = Math.cos(angle), si = Math.sin(angle), frequency = TAU / pitch;
        const k = focus * 5;
        // Normalise peak displacement, so Focus narrows the flow instead of merely
        // making every deformation disappear as its envelope becomes narrower.
        const z = k ? 2 * k / (1 + Math.sqrt(1 + 4 * k * k)) : 0;
        const gain = 1 / (Math.sqrt(1 - z * z) * Math.exp(k * (z - 1)));
        function apply(point, sign = 1) {
          const y = -(point.x - cx) * si + (point.y - cy) * co;
          const t = y * frequency + phase;
          const d = sign * amplitude * gain * Math.sin(t) * Math.exp(k * (Math.cos(t) - 1));
          return { x: point.x + co * d, y: point.y + si * d };
        }
        function bound(point) {
          const y = add(scale(shift(point.x, -cx), -si), scale(shift(point.y, -cy), co));
          const t = shift(scale(y, frequency), phase);
          const trig = sinCos(t);
          const displacement = scale(multiply(trig.sin, exp(scale(shift(trig.cos, -1), k))), amplitude * gain);
          return { x: add(point.x, scale(displacement, co)), y: add(point.y, scale(displacement, si)) };
        }
        function boxDerivative(box) {
          const x = iadd(box[0], [-cx, -cx]), y = iadd(box[1], [-cy, -cy]);
          const t = iadd(iscale(iadd(iscale(x, -si), iscale(y, co)), frequency), [phase, phase]);
          const sine = isin(t), cosine = icos(t), envelope = iexp(iscale(iadd(cosine, [-1, -1]), k));
          const displacement = iscale(imul(sine, envelope), amplitude * gain);
          const slope = iscale(imul(envelope, iadd(cosine, iscale(isquare(sine), -k))), amplitude * gain * frequency);
          const slopeBound = imax(slope);
          return { norm: (Math.hypot(2, slopeBound) + slopeBound) / 2,
            box: [iadd(box[0], iscale(displacement, co)), iadd(box[1], iscale(displacement, si))],
            matrix: [iadd([1, 1], iscale(slope, -co * si)), iscale(slope, -si * si),
              iscale(slope, co * co), iadd([1, 1], iscale(slope, si * co))] };
        }
        return { apply, bound, boxDerivative, kind: 'shear', amplitude, pitch };
      }

      function twist(cx, cy, radius, strength) {
        const falloff = -.5 / (radius * radius);
        function apply(point, sign = 1) {
          const x = point.x - cx, y = point.y - cy;
          const angle = sign * strength * Math.exp((x * x + y * y) * falloff);
          const co = Math.cos(angle), si = Math.sin(angle);
          return { x: cx + x * co - y * si, y: cy + x * si + y * co };
        }
        function bound(point) {
          const x = shift(point.x, -cx), y = shift(point.y, -cy);
          const angle = scale(exp(scale(add(square(x), square(y)), falloff)), strength);
          const trig = sinCos(angle), co = trig.cos, si = trig.sin;
          return { x: shift(add(multiply(x, co), scale(multiply(y, si), -1)), cx),
            y: shift(add(multiply(x, si), multiply(y, co)), cy) };
        }
        function boxDerivative(box) {
          const x = iadd(box[0], [-cx, -cx]), y = iadd(box[1], [-cy, -cy]);
          const radius2 = iadd(isquare(x), isquare(y));
          const angle = iscale(iexp(iscale(radius2, falloff)), strength);
          const co = icos(angle), si = isin(angle);
          const tx = iadd(imul(x, co), ineg(imul(y, si))), ty = iadd(imul(x, si), imul(y, co));
          const gx = iscale(imul(x, angle), 2 * falloff), gy = iscale(imul(y, angle), 2 * falloff);
          // In polar coordinates this derivative is a shear of r*phi'(r), between
          // orthogonal frames. q*exp(-q/2) peaks at q=2; retain this independent cap
          // when the rectangular interval Jacobian becomes excessively wide.
          const q0 = Math.max(0, radius2[0] / (radius * radius)), q1 = Math.max(0, radius2[1] / (radius * radius));
          const profile = q => Number.isFinite(q) ? q * Math.exp(-q / 2) : 0;
          const shearBound = Math.abs(strength) * Math.max(profile(q0), profile(q1), q0 <= 2 && q1 >= 2 ? 2 / Math.E : 0) * (1 + 32 * Number.EPSILON);
          return { norm: (Math.hypot(2, shearBound) + shearBound) / 2,
            box: [iadd(tx, [cx, cx]), iadd(ty, [cy, cy])],
            matrix: [iadd(co, ineg(imul(ty, gx))), iadd(si, imul(tx, gx)),
              ineg(iadd(si, imul(ty, gy))), iadd(co, imul(tx, gy))] };
        }
        return { apply, bound, boxDerivative, kind: 'twist', radius, strength };
      }

      // Conjugate a circular twist by an invertible area-neutral local frame. This
      // remains a planar bijection with an explicit inverse and determinant one;
      // anisotropy is in the field, not an extra scale applied to the final glyph.
      function ellipticTwist(cx, cy, radius, strength, angle, stretch) {
        const co = Math.cos(angle), si = Math.sin(angle), q = stretch;
        const m = [co * q, si * q, -si / q, co / q];
        const inv = [co / q, -si * q, si / q, co * q];
        const circular = twist(0, 0, radius, strength);
        const linear = (matrix, x, y) => [matrix[0] * x + matrix[2] * y, matrix[1] * x + matrix[3] * y];
        const range = (matrix, box) => [iadd(iscale(box[0], matrix[0]), iscale(box[1], matrix[2])),
          iadd(iscale(box[0], matrix[1]), iscale(box[1], matrix[3]))];
        const jets = (matrix, point) => ({ x: add(scale(point.x, matrix[0]), scale(point.y, matrix[2])),
          y: add(scale(point.x, matrix[1]), scale(point.y, matrix[3])) });
        function apply(point, sign = 1) {
          const [x, y] = linear(inv, point.x - cx, point.y - cy);
          const moved = circular.apply({ x, y }, sign), [dx, dy] = linear(m, moved.x, moved.y);
          return { x: cx + dx, y: cy + dy };
        }
        function bound(point) {
          const local = jets(inv, { x: shift(point.x, -cx), y: shift(point.y, -cy) });
          const mapped = jets(m, circular.bound(local));
          return { x: shift(mapped.x, cx), y: shift(mapped.y, cy) };
        }
        function boxDerivative(box) {
          const localBox = range(inv, [iadd(box[0], [-cx, -cx]), iadd(box[1], [-cy, -cy])]);
          const local = circular.boxDerivative(localBox), j = local.matrix;
          const right = [iadd(iscale(j[0], inv[0]), iscale(j[2], inv[1])),
            iadd(iscale(j[1], inv[0]), iscale(j[3], inv[1])),
            iadd(iscale(j[0], inv[2]), iscale(j[2], inv[3])),
            iadd(iscale(j[1], inv[2]), iscale(j[3], inv[3]))];
          const matrix = [iadd(iscale(right[0], m[0]), iscale(right[1], m[2])),
            iadd(iscale(right[0], m[1]), iscale(right[1], m[3])),
            iadd(iscale(right[2], m[0]), iscale(right[3], m[2])),
            iadd(iscale(right[2], m[1]), iscale(right[3], m[3]))];
          const output = range(m, local.box);
          return { norm: local.norm * Math.max(q, 1 / q) ** 2, matrix,
            box: [iadd(output[0], [cx, cx]), iadd(output[1], [cy, cy])] };
        }
        return { apply, bound, boxDerivative, kind: 'elliptic-twist' };
      }

      function createMarblingMap(glyph, input, phase = 0) {
        const settings = normalizeMarblingSettings(input);
        const phase01 = Number.isFinite(phase) ? ((phase % 1) + 1) % 1 : 0;
        const time = phase01 * TAU, wave = Math.sin(time), breath = 1 - settings.motion * .22 * (1 - Math.cos(time));
        const angle = settings.angle * Math.PI / 180 + settings.motion * .25 * wave;
        const amount = settings.amount * breath;
        const pitch = settings.pitch, focus = settings.focus, circulation = settings.circulation;
        const amplitude = amount * pitch * .52, offset = settings.motion * wave * .9;
        const { cx, cy } = glyph;
        const operations = [];
        // Later tools act at transported anatomy sites. Eddy's footprint measures
        // surrounding ink, not just a possibly tiny empty counter; Plume retains
        // its original circular field and stage sequence.
        const anatomyAt = index => {
          let point = glyph.anchors[index];
          for (const operation of operations) point = operation.apply(point);
          return [point.x, point.y];
        };
        if (settings.amount !== 0 && glyph.rings.length) {
          if (settings.mode === 'rake') {
            operations.push(shear(cx, cy, angle, amplitude, pitch, focus, offset));
            operations.push(shear(cx, cy, angle + Math.PI / 2, amplitude * .22 * circulation, pitch * 1.7, focus * .3, 1.1 - offset));
            operations.push(shear(cx, cy, angle, -amplitude * .38, pitch * 1.12, focus, 1.7 + offset));
          } else if (settings.mode === 'eddy') {
            // Split the first tool around the second: a single final full vortex
            // otherwise overwhelms the two source sites with one common envelope.
            // Total signed strengths are unchanged. Each revisit follows its source
            // anchor through ALL previous tools; it is not a frozen canvas centre.
            for (const [i, fraction] of [[0, .5], [1, 1], [0, .5]]) {
              const site = glyph.eddySites[i], centre = anatomyAt(i);
              operations.push(ellipticTwist(...centre, site.radius * pitch / 118 * (1 - focus * .45),
                amount * circulation * (i ? -2 : 2.5) * fraction, site.angle + angle, site.stretch));
            }
            operations.push(shear(cx, cy, angle, amplitude * .18, pitch * 1.8, focus * .25, offset));
          } else {
            operations.push(shear(cx, cy, angle + Math.PI / 2, amplitude * 1.1, pitch, focus, .6 + offset));
            operations.push(twist(...anatomyAt(0), pitch * .68, amount * circulation * 1.4));
            operations.push(shear(cx, cy, angle, -amplitude * .8, pitch * 1.45, focus * .55, .8 - offset));
            operations.push(twist(...anatomyAt(1), pitch * .4, -amount * circulation * .7));
          }
        }
        function map(point) {
          let result = { x: point.x, y: point.y };
          for (const operation of operations) result = operation.apply(result);
          return result;
        }
        function inverse(point) {
          let result = { x: point.x, y: point.y };
          for (let i = operations.length - 1; i >= 0; i--) result = operations[i].apply(result, -1);
          return result;
        }
        function chordErrorBound(a, b) {
          if (!operations.length || a.x === b.x && a.y === b.y) return 0;
          // Local t always ranges 0..1 on THIS source segment. Composition of second
          // derivative interval jets bounds its mapped chord deviation by M / 8.
          let point = { x: jet(a.x - cx, b.x - cx), y: jet(a.y - cy, b.y - cy) };
          point = { x: shift(point.x, cx), y: shift(point.y, cy) };
          for (const operation of operations) point = operation.bound(point);
          return Math.hypot(imax(point.x.dd), imax(point.y.dd)) / 8;
        }
        function derivativeBoundForBox(box) {
          if (!operations.length) return 1;
          let range = [[box[0], box[2]], [box[1], box[3]]];
          let jacobian = [[1, 1], [0, 0], [0, 0], [1, 1]];
          let normProduct = 1;
          for (const operation of operations) {
            const local = operation.boxDerivative(range), m = local.matrix, j = jacobian;
            normProduct *= local.norm;
            jacobian = [iadd(imul(m[0], j[0]), imul(m[2], j[1])),
              iadd(imul(m[1], j[0]), imul(m[3], j[1])),
              iadd(imul(m[0], j[2]), imul(m[2], j[3])),
              iadd(imul(m[1], j[2]), imul(m[3], j[3]))];
            range = local.box;
          }
          // |Jv| <= abs(J) abs(v): the spectral norm of the entrywise upper
          // magnitudes bounds EVERY derivative in the propagated source box.
          const [a, b, c, d] = jacobian.map(imax), magnitude = Math.max(a, b, c, d);
          if (!Number.isFinite(magnitude)) return normProduct;
          if (magnitude === 0) return 0;
          const aa = (a / magnitude) ** 2 + (b / magnitude) ** 2;
          const cc = (c / magnitude) ** 2 + (d / magnitude) ** 2;
          const ac = (a / magnitude) * (c / magnitude) + (b / magnitude) * (d / magnitude);
          return Math.min(normProduct, magnitude * Math.sqrt((aa + cc + Math.hypot(aa - cc, 2 * ac)) / 2));
        }
        return Object.freeze({ map, inverse, chordErrorBound, derivativeBoundForBox, settings,
          identity: operations.length === 0, operations: operations.map(({ kind }) => kind), phase: phase01 });
      }

      function deformMarblingGlyph(glyph, input, phase = 0, options = {}) {
        const transform = createMarblingMap(glyph, input, phase);
        const tolerance = finite(options.tolerance, .08, .00001, 10);
        const maxPoints = Math.floor(finite(options.maxPoints, 131072, 3, 1000000));
        const maxDepth = Math.floor(finite(options.maxDepth, 22, 0, 28));
        let pointCount = 0, subdivisions = 0, maxErrorBound = 0;
        const fail = () => { throw new RangeError('Marbling geometry budget exceeded; no truncated output returned'); };
        const rings = glyph.rings.map(ring => {
          const points = [], material = [];
          function edge(a, b, mappedA, u0, u1, depth) {
            const error = transform.chordErrorBound(a, b);
            if (!(error <= tolerance)) {
              if (depth >= maxDepth || pointCount >= maxPoints) fail();
              const mid = { x: a.x + (b.x - a.x) / 2, y: a.y + (b.y - a.y) / 2 }, um = (u0 + u1) / 2;
              subdivisions++;
              edge(a, mid, mappedA, u0, um, depth + 1);
              edge(mid, b, transform.map(mid), um, u1, depth + 1);
            } else {
              if (++pointCount > maxPoints) fail();
              if (!Number.isFinite(mappedA.x) || !Number.isFinite(mappedA.y)) throw new RangeError('Marbling: non-finite transformed geometry');
              points.push(mappedA); material.push(u0); maxErrorBound = Math.max(maxErrorBound, error);
            }
          }
          for (let i = 0; i < ring.points.length; i++) {
            const a = ring.points[i], b = ring.points[(i + 1) % ring.points.length];
            edge(a, b, transform.map(a), i, i + 1, 0);
          }
          return { points, material, area: marblingSignedArea(points), sourceArea: ring.area };
        });
        return { rings, pointCount, subdivisions, maxErrorBound, tolerance, identity: transform.identity };
      }
      return {MARBLING_MODES,MARBLING_PRESETS,normalizeMarblingSettings,marblingSignedArea,prepareMarblingGlyph,createMarblingMap,deformMarblingGlyph};
      })();
      modules[2] = (function () {
      const { createMarblingMap } = modules[1];

      // Retains every original point. Nodes only APPROXIMATE an arc when the source
      // error transported through the current map, plus its chord curvature, fits
      // the requested destination error. No fixed thinning or smoothing of the font.
      // The same source hierarchy as synchronous compilation, suspended within a
      // node's point scan (not merely between whole glyphs). The glyph must already
      // be an immutable prepareMarblingGlyph result; capture/ink analysis is separate.
      function createMarblingCompileTask(glyph) {
        if (!glyph?.rings || !Object.isFrozen(glyph)) throw new TypeError('An immutable prepared Marbling glyph is required');
        const progress = { nodeCount: 0, visitedPoints: 0, workCount: 0 };
        let source = glyph, state = 'working', result = null, error = null;
        let trees = [], roots = [], stack = [], ringIndex = 0, rootIndex = 0;
        function push(start, end) {
          const points = source.rings[ringIndex].points, n = points.length;
          const a = points[start % n], b = points[end % n], dx = b.x - a.x, dy = b.y - a.y;
          stack.push({ start, end, a, b, dx, dy, length2: dx * dx + dy * dy, index: start,
            box: [Infinity, Infinity, -Infinity, -Infinity], error2: 0, stage: 0, left: null, right: null });
        }
        function nextRoot() {
          if (ringIndex >= source.rings.length) {
            result = Object.freeze({ glyph: source, trees: Object.freeze(trees), nodeCount: progress.nodeCount });
            source = null; state = 'complete'; trees = []; return;
          }
          const count = source.rings[ringIndex].points.length, divisions = Math.min(4, count);
          push(Math.floor(rootIndex * count / divisions), Math.floor((rootIndex + 1) * count / divisions));
        }
        function release() { source = null; trees = []; roots = []; stack = []; }
        nextRoot();
        return Object.freeze({
          get status() { return state; }, get result() { return result; }, get error() { return error; },
          get progress() { return { ...progress, state }; },
          cancel(reason = abortError()) {
            if (state !== 'working') return false;
            release(); state = 'cancelled'; error = reason; return true;
          },
          step({ maxWork = 2048, maxMs = 4, now = () => performance.now() } = {}) {
            if (state !== 'working') return state;
            const budget = Math.floor(limit(maxWork, 2048, 1, 1e7)), duration = limit(maxMs, Infinity, 0, 60000);
            const started = Number.isFinite(duration) ? now() : 0;
            try {
              let work = 0;
              while (state === 'working' && work < budget) {
                const node = stack[stack.length - 1];
                if (node.stage === 0) {
                  const points = source.rings[ringIndex].points, count = points.length;
                  const visits = Math.min(budget - work, node.end - node.index + 1,
                    Number.isFinite(duration) ? duration === 0 ? 1 : 32 - work % 32 : Infinity);
                  const { a, dx, dy, length2, box } = node;
                  let error2 = node.error2;
                  for (let i = 0; i < visits; i++) {
                    const p = points[node.index++ % count];
                    box[0] = Math.min(box[0], p.x); box[1] = Math.min(box[1], p.y);
                    box[2] = Math.max(box[2], p.x); box[3] = Math.max(box[3], p.y);
                    const t = length2 ? Math.max(0, Math.min(1, ((p.x - a.x) * dx + (p.y - a.y) * dy) / length2)) : 0;
                    error2 = Math.max(error2, (p.x - a.x - t * dx) ** 2 + (p.y - a.y - t * dy) ** 2);
                  }
                  node.error2 = error2; work += visits; progress.workCount += visits; progress.visitedPoints += visits;
                  if (node.index > node.end) { node.stage = 1; progress.nodeCount++; }
                  // At most 32 visits between clock checks. This is cooperative work,
                  // not a hard wall-time deadline or a guarantee about GC pauses.
                  if (Number.isFinite(duration) && (duration === 0 || work % 32 === 0) && now() - started >= duration) break;
                } else if (node.stage === 1 && node.end - node.start > 1) {
                  node.stage = 2; push(node.start, Math.floor((node.start + node.end) / 2));
                } else if (node.stage === 2) {
                  node.stage = 3; push(Math.floor((node.start + node.end) / 2), node.end);
                } else {
                  const data = Object.freeze({ start: node.start, end: node.end, a: node.a, b: node.b,
                    box: Object.freeze(node.box), sourceError: Math.sqrt(node.error2), left: node.left, right: node.right });
                  stack.pop();
                  const parent = stack[stack.length - 1];
                  if (parent) { if (parent.stage === 2) parent.left = data; else parent.right = data; }
                  else {
                    roots.push(data); rootIndex++;
                    if (rootIndex === Math.min(4, source.rings[ringIndex].points.length)) {
                      trees.push(Object.freeze(roots)); roots = []; rootIndex = 0; ringIndex++;
                    }
                    nextRoot();
                  }
                }
              }
            } catch (failure) { release(); state = 'error'; error = failure; throw failure; }
            return state;
          },
        });
      }

      function compileMarblingGlyph(glyph) {
        // Keep the original synchronous fast path for callers that intentionally
        // perform offline work. The resumable path is independently parity-tested.
        let nodeCount = 0;
        const trees = glyph.rings.map(ring => {
          const source = ring.points, count = source.length;
          function build(start, end) {
            const a = source[start % count], b = source[end % count];
            const dx = b.x - a.x, dy = b.y - a.y, length2 = dx * dx + dy * dy;
            const box = [Infinity, Infinity, -Infinity, -Infinity];
            let error2 = 0;
            for (let i = start; i <= end; i++) {
              const p = source[i % count];
              box[0] = Math.min(box[0], p.x); box[1] = Math.min(box[1], p.y);
              box[2] = Math.max(box[2], p.x); box[3] = Math.max(box[3], p.y);
              const t = length2 ? Math.max(0, Math.min(1, ((p.x - a.x) * dx + (p.y - a.y) * dy) / length2)) : 0;
              error2 = Math.max(error2, (p.x - a.x - t * dx) ** 2 + (p.y - a.y - t * dy) ** 2);
            }
            const middle = Math.floor((start + end) / 2); nodeCount++;
            return Object.freeze({ start, end, a, b, box: Object.freeze(box), sourceError: Math.sqrt(error2),
              left: end - start > 1 ? build(start, middle) : null, right: end - start > 1 ? build(middle, end) : null });
          }
          const roots = [], divisions = Math.min(4, count);
          for (let i = 0; i < divisions; i++) roots.push(build(Math.floor(i * count / divisions), Math.floor((i + 1) * count / divisions)));
          return Object.freeze(roots);
        });
        return Object.freeze({ glyph, trees: Object.freeze(trees), nodeCount });
      }

      const limit = (v, fallback, min, max) => Number.isFinite(v) ? Math.max(min, Math.min(max, v)) : fallback;
      const abortError = () => Object.assign(new Error('Marbling computation cancelled'), { name: 'AbortError' });

      function createMarblingLodTask(compiled, settings, phase = 0, options = {}) {
        const tolerance = limit(options.tolerance, .08, .00001, 10);
        const maxPoints = Math.floor(limit(options.maxPoints, 262144, 3, 1000000));
        const maxDepth = Math.floor(limit(options.maxDepth, 22, 0, 28));
        let source = compiled, transform = createMarblingMap(compiled.glyph, settings, phase);
        let state = 'working', result = null, error = null, ringIndex = 0;
        let points = [], spans = [], rings = [], stack = [], origin = null, previous = null, twiceArea = 0;
        let pointCount = 0, visitedNodes = 0, mappedPoints = 0, maxErrorBound = 0, workCount = 0;
        const identity = transform.identity;
        function pushRing() {
          if (ringIndex >= source.glyph.rings.length) {
            result = Object.freeze({ rings: Object.freeze(rings), pointCount, visitedNodes, mappedPoints, maxErrorBound, tolerance, identity });
            state = 'complete'; source = null; transform = null; stack = [];
            return;
          }
          if (identity) {
            const ring = source.glyph.rings[ringIndex];
            for (let i = ring.points.length - 1; i >= 0; i--) stack.push({ native: true, a: ring.points[i], start: i, end: i + 1 });
          } else for (let i = source.trees[ringIndex].length - 1; i >= 0; i--) stack.push(source.trees[ringIndex][i]);
        }
        function release() {
          source = null; transform = null; points = []; spans = []; rings = []; stack = []; origin = null; previous = null;
        }
        function fail(message) { throw new RangeError(`Marbling ${message}; no truncated output returned`); }
        function emit(a, start, end, bound) {
          if (++pointCount > maxPoints) fail('geometry budget exceeded');
          mappedPoints++;
          const point = Object.freeze(transform.map(a));
          if (!Number.isFinite(point.x) || !Number.isFinite(point.y)) fail('non-finite transformed geometry');
          if (!origin) origin = point;
          if (previous) twiceArea += (previous.x - origin.x) * (point.y - origin.y) - (point.x - origin.x) * (previous.y - origin.y);
          previous = point;
          points.push(point); spans.push(Object.freeze([start, end])); maxErrorBound = Math.max(maxErrorBound, bound);
        }
        function advance(node) {
          visitedNodes++;
          if (node.native) { emit(node.a, node.start, node.end, 0); return; }
          if (node.left) {
            const sourceBound = node.sourceError === 0 ? 0 : node.sourceError * transform.derivativeBoundForBox(node.box);
            // If the original arc alone cannot fit, do not spend a Hessian pass
            // testing its chord. Descend without compromising the source error.
            if (sourceBound <= tolerance) {
              const bound = sourceBound + transform.chordErrorBound(node.a, node.b);
              if (bound <= tolerance) { emit(node.a, node.start, node.end, bound); return; }
            }
            stack.push(node.right, node.left);
            return;
          }
          const bound = transform.chordErrorBound(node.a, node.b);
          if (bound <= tolerance) { emit(node.a, node.start, node.end, bound); return; }
          const depth = node.depth || 0;
          if (depth >= maxDepth || pointCount >= maxPoints) fail('geometry budget exceeded');
          const middle = { x: node.a.x + (node.b.x - node.a.x) / 2, y: node.a.y + (node.b.y - node.a.y) / 2 };
          const u = (node.start + node.end) / 2;
          stack.push({ a: middle, b: node.b, start: u, end: node.end, depth: depth + 1 },
            { a: node.a, b: middle, start: node.start, end: u, depth: depth + 1 });
        }
        pushRing();
        return Object.freeze({
          get status() { return state; },
          get error() { return error; },
          get result() { return result; },
          get progress() { return { state, pointCount, visitedNodes, workCount, ringIndex, pendingNodes: stack.length }; },
          cancel(reason = abortError()) {
            if (state !== 'working') return false;
            error = reason; state = 'cancelled'; release(); return true;
          },
          step({ maxWork = 128, maxMs = Infinity, now = () => performance.now() } = {}) {
            if (state !== 'working') return state;
            const budget = Math.floor(limit(maxWork, 128, 1, 1e7));
            const duration = limit(maxMs, Infinity, 0, 60000), started = now();
            try {
              for (let work = 0; work < budget && state === 'working'; work++) {
                const node = stack.pop(); workCount++; advance(node);
                if (!stack.length) {
                  rings.push(Object.freeze({ points: Object.freeze(points), spans: Object.freeze(spans),
                    sourceArea: source.glyph.rings[ringIndex].area, area: twiceArea / 2 }));
                  points = []; spans = []; origin = null; previous = null; twiceArea = 0; ringIndex++; pushRing();
                }
                if (now() - started >= duration) break;
              }
            } catch (failure) { error = failure; state = 'error'; release(); throw failure; }
            return state;
          },
        });
      }

      function renderMarblingLod(compiled, settings, phase = 0, options = {}) {
        const task = createMarblingLodTask(compiled, settings, phase, options);
        while (task.status === 'working') task.step({ maxWork: 65536 });
        return task.result;
      }

      // Genuine task yielding, not a Promise.resolve microtask loop. The scheduler
      // API is optional; a timer works on runtimes/Safari without scheduler.yield.
      function yieldMarblingWork() {
        return globalThis.scheduler?.yield ? globalThis.scheduler.yield() : new Promise(resolve => setTimeout(resolve, 0));
      }

      async function runMarblingLodTask(task, { signal, yieldWork = yieldMarblingWork, maxWork = 96, maxMs = 4 } = {}) {
        let rejectAbort;
        const aborted = new Promise((_, reject) => { rejectAbort = reject; });
        // A rejection may occur before the first yield; attach a handler immediately.
        aborted.catch(() => {});
        const onAbort = () => { const reason = signal.reason === undefined ? abortError() : signal.reason; task.cancel(reason); rejectAbort(reason); };
        if (signal?.aborted) { task.cancel(signal.reason); throw signal.reason; }
        signal?.addEventListener('abort', onAbort, { once: true });
        try {
          while (task.status === 'working') {
            task.step({ maxWork, maxMs });
            if (task.status === 'working') await Promise.race([Promise.resolve().then(yieldWork), aborted]);
          }
          if (signal?.aborted) throw signal.reason;
          if (task.status !== 'complete') throw task.error;
          return task.result;
        } catch (failure) { task.cancel(failure); throw failure; }
        finally { signal?.removeEventListener('abort', onAbort); }
      }

      function marblingPixelTolerance(matrix, pixelError = .2) {
        const { a, b, c, d } = matrix;
        if (![a, b, c, d, pixelError].every(Number.isFinite) || pixelError <= 0) throw new TypeError('Invalid Marbling output transform');
        const scale = Math.max(Math.abs(a), Math.abs(b), Math.abs(c), Math.abs(d));
        if (scale === 0) return 10;
        const an = a / scale, bn = b / scale, cn = c / scale, dn = d / scale;
        const aa = an * an + bn * bn, cc = cn * cn + dn * dn, ac = an * cn + bn * dn;
        const sigma = scale * Math.sqrt((aa + cc + Math.hypot(aa - cc, 2 * ac)) / 2);
        const tolerance = pixelError / sigma;
        if (tolerance < .00001) throw new RangeError('Output scale exceeds Marbling precision budget');
        return Math.min(10, tolerance);
      }
      return {createMarblingCompileTask,compileMarblingGlyph,createMarblingLodTask,renderMarblingLod,yieldMarblingWork,runMarblingLodTask,marblingPixelTolerance};
      })();
      modules[3] = (function () {
      const { createMarblingCompileTask } = modules[2];

      // Active source leases. A key identifies font + grapheme + capture contract;
      // revision identifies immutable source content. Colours, poses, effect values,
      // animation time and destination precision do not belong in this key.
      // load receives the remaining source-point allowance and must synchronously
      // return a prepareMarblingGlyph result. Capture/ink analysis is NOT time-sliced.
      function createMarblingSourcePool({ pointLimit = 262144, nodeLimit = 524288,
        taskFactory = createMarblingCompileTask } = {}) {
        for (const n of [pointLimit, nodeLimit]) if (!Number.isInteger(n) || n < 1) throw new RangeError('Invalid Marbling source limit');
        let entries = new Map(), paused = false, cursor = 0, loads = 0, builds = 0;
        function descriptor(request, requireLoad = true) {
          if (typeof request?.key !== 'string' || !request.key || typeof request.revision !== 'string' || !request.revision ||
            requireLoad && typeof request.load !== 'function') throw new TypeError('Marbling source needs a key, revision and loader');
          return { id: JSON.stringify([request.key, request.revision]), key: request.key, revision: request.revision, load: request.load };
        }
        function usage() {
          let points = 0, nodes = 0;
          for (const entry of entries.values()) { points += entry.points; nodes += entry.nodes; }
          return { points, nodes };
        }
        function retire(entry) {
          entry.task?.cancel(); entry.task = null; entry.data = null; entry.load = null;
          entry.points = 0; entry.nodes = 0; entry.error = null;
        }
        function ticket(entry) {
          const live = () => entries.get(entry.id) === entry;
          return Object.freeze({
            get status() { return !live() ? 'missing' : entry.data ? 'ready' : entry.error ? 'error' : 'pending'; },
            get result() { return live() ? entry.data : null; },
            get error() { return live() ? entry.error : null; },
          });
        }
        function sync(requests) {
          // A bad edit must not evict the last valid source set. Loader closures are
          // captured once per revision, not replaced by per-frame mutable settings.
          const descriptions = requests.map(r => descriptor(r)), next = new Map();
          for (const d of descriptions) if (!next.has(d.id)) next.set(d.id, entries.get(d.id) ||
            { ...d, data: null, task: null, error: null, points: 0, nodes: 0 });
          for (const [id, entry] of entries) if (!next.has(id)) retire(entry);
          entries = next;
          return descriptions.map(d => ticket(entries.get(d.id)));
        }
        function advance(options = {}) {
          if (paused) return false;
          const pending = [...entries.values()].filter(e => !e.data && !e.error);
          if (!pending.length) return false;
          const entry = pending[cursor++ % pending.length];
          try {
            if (!entry.task) {
              const used = usage();
              loads++;
              const glyph = entry.load({ maxPoints: pointLimit - used.points });
              if (!Object.isFrozen(glyph) || !Array.isArray(glyph?.rings) || !Object.isFrozen(glyph.rings) ||
                !Number.isInteger(glyph.pointCount) || glyph.pointCount < 0) throw new TypeError('Invalid immutable Marbling source');
              let points = 0, nodes = 0;
              for (const ring of glyph.rings) {
                if (!Object.isFrozen(ring) || !Array.isArray(ring.points) || !Object.isFrozen(ring.points) || ring.points.length < 3)
                  throw new TypeError('Invalid immutable Marbling source ring');
                points += ring.points.length;
                nodes += 2 * ring.points.length - Math.min(4, ring.points.length);
              }
              if (points !== glyph.pointCount) throw new TypeError('Invalid Marbling source point count');
              if (used.points + points > pointLimit || used.nodes + nodes > nodeLimit) throw new RangeError('Marbling active source budget exceeded');
              // Reserve the full hierarchy before any partial allocation. Active
              // glyphs are never evicted to silently lower quality or make room.
              entry.points = points; entry.nodes = nodes;
              entry.task = taskFactory(glyph); builds++;
            } else entry.task.step(options);
            if (entry.task.status === 'complete') {
              if (entry.task.result.nodeCount !== entry.nodes) throw new Error('Marbling source hierarchy count mismatch');
              entry.data = entry.task.result; entry.task = null; entry.load = null;
            } else if (entry.task.status !== 'working') throw entry.task.error || new Error('Marbling source compilation failed');
          } catch (failure) {
            entry.task?.cancel(failure); entry.task = null; entry.data = null;
            entry.points = 0; entry.nodes = 0; entry.error = failure;
          }
          return true;
        }
        function state() {
          const all = [...entries.values()];
          return { total: all.length, ready: all.filter(e => e.data).length, pending: all.some(e => !e.data && !e.error),
            errors: all.filter(e => e.error).length, paused, loads, builds, usage: usage(), pointLimit, nodeLimit };
        }
        return Object.freeze({ sync, advance, state,
          // Read-only export/preflight inspection cannot prune the live working set.
          peek(request) { const { id } = descriptor(request, false); return entries.has(id) ? ticket(entries.get(id)) : null; },
          retry() { for (const e of entries.values()) e.error = null; paused = false; },
          setPaused(value) { paused = !!value; },
          reset() { for (const e of entries.values()) retire(e); entries.clear(); paused = false; cursor = 0; },
        });
      }
      return {createMarblingSourcePool};
      })();
      return Object.assign({}, ...modules);
      })();

var marblingPool = MarblingType.createMarblingSourcePool();

var marblingFontRevision = 0;

var marblingTimer = null;

var marblingRenderError = '';

var marblingCache = new Map();

var marblingCachePoints = 0;

function marblingClearGeometry() { marblingCache.clear(); marblingCachePoints = 0; }

function marblingPhase(phase) {
        var value = Number.isFinite(phase) ? phase : compositionState.enabled ? compositionState.phase : 0;
        return ((value % 1) + 1) % 1;
      }

function marblingGlyphSettings(g) {
        var profile = g.surface || {}, settings = {};
        ['amount', 'pitch', 'focus', 'circulation', 'angle', 'motion'].forEach(function (field) {
          var key = 'marbling' + field[0].toUpperCase() + field.slice(1);
          settings[field] = Number.isFinite(profile[key]) ? profile[key] : params[key];
        });
        settings.mode = MarblingType.MARBLING_MODES.indexOf(profile.marblingMode) >= 0 ? profile.marblingMode : params.marblingMode;
        return MarblingType.normalizeMarblingSettings(settings);
      }

function marblingSource(ch,g){
    var sourceSize=globalThis.TypeDeformerDraftMode?64:768;
    var spec=g&&g.fontAxes?glyphFontSpec(g,sourceSize):null,font=params.fontWeight+' '+sourceSize+'px '+params.fontFamily;
    return {key:(spec?spec.key:font)+'\u0000'+ch,revision:String(marblingFontRevision),load:marblingGlyphData.bind(null,ch,{font:font,glyph:g&&g.fontAxes?g:null})};
  }

function marblingGlyphData(ch,fontSpec,allowance){
    var sourceSize=globalThis.TypeDeformerDraftMode?64:768,unit=sourceSize/192;
    var canvas=document.createElement('canvas'),ctx=canvas.getContext('2d',{willReadFrequently:true});
    if(!ctx)throw new Error('Marbling Typeの字形Canvasを作成できません。');
    var font=fontSpec.glyph?glyphFontSpec(fontSpec.glyph,sourceSize).font:fontSpec.font;
    try{ctx.font=font;var measure=ctx.measureText(ch),left=Math.ceil(Math.max(0,measure.actualBoundingBoxLeft||0))+16,ascent=Math.ceil(Math.max(1,measure.actualBoundingBoxAscent||sourceSize))+16,right=Math.ceil(Math.max(measure.width,measure.actualBoundingBoxRight||0))+16,descent=Math.ceil(Math.max(0,measure.actualBoundingBoxDescent||0))+16;
      if(left+right>4096||ascent+descent>4096)throw new RangeError('Marbling Typeの字形ソースは4096pxが上限です。');
      canvas.width=Math.max(8,left+right);canvas.height=Math.max(8,ascent+descent);ctx.font=font;ctx.textBaseline='alphabetic';ctx.fillStyle='#fff';ctx.fillText(ch,left,ascent);
      var traced=spectralTraceContours(ctx.getImageData(0,0,canvas.width,canvas.height).data,canvas.width,canvas.height),count=0,contours=traced.map(function(ring){count+=ring.points.length;return {points:ring.points.map(function(p){return {x:(p.x-left)/unit,y:(p.y-ascent)/unit};})};});
      if(count>allowance.maxPoints)throw new RangeError('Marbling Typeの字形メモリ上限です。適用する文字種を減らしてください。');
      ctx.textBaseline='middle';var middle=ctx.measureText(ch),middleOffset=Number.isFinite(middle.actualBoundingBoxAscent)&&Number.isFinite(measure.actualBoundingBoxAscent)?(measure.actualBoundingBoxAscent-middle.actualBoundingBoxAscent)/unit:192*.35;
      return Object.freeze(Object.assign({},MarblingType.prepareMarblingGlyph(contours),{nativeMetrics:Object.freeze({advance:measure.width/unit,middleOffset:middleOffset,ascent:Math.max(1,measure.actualBoundingBoxAscent||sourceSize)/unit,descent:Math.max(0,measure.actualBoundingBoxDescent||0)/unit})}));
    }finally{canvas.width=0;canvas.height=0;}
  }

function marblingRequests(glyphs) {
        var requests = new Map(); if (surfaceOutputOpacity('marblingType') <= .002) return [];
        glyphs.forEach(function (g) {
          if (surfaceGlyphStrength(g, 'marblingType') <= .002 || !String(g.ch || '').trim() || g.opacity === 0 || marblingGlyphSettings(g).amount === 0) return;
          var request = marblingSource(g.ch,g); if (!requests.has(request.key)) requests.set(request.key, request);
        });
        return Array.from(requests.values());
      }

function marblingUpdateStatus(){return marblingPool.state();}

function marblingScheduleWork(){}

function marblingPrepare(glyphs) {
        if(globalThis.TypeDeformerRenderJobs&&typeof Worker!=='undefined'&&!globalThis.TypeDeformerWorkerRuntime)return;
        var requests = marblingRequests(glyphs); marblingPool.sync(requests);
        var active = new Set(requests.map(function (r) { return r.key; }));
        marblingCache.forEach(function (entry, key) {
          if (!active.has(entry.sourceKey)) { marblingCachePoints -= entry.shape.pointCount; marblingCache.delete(key); }
        });
        if (!requests.length) {
          marblingRenderError = ''; marblingClearGeometry(); marblingPool.setPaused(false);
          if (marblingTimer !== null) cancelAnimationFrame(marblingTimer); marblingTimer = null;
        }
        marblingScheduleWork();
      }

function marblingAssertReady(glyphs) {
        if(globalThis.TypeDeformerRenderJobs&&typeof Worker!=='undefined'&&!globalThis.TypeDeformerWorkerRuntime)return;
        marblingRequests(glyphs).forEach(function (request) {
          var ticket = marblingPool.peek(request); if (ticket && ticket.status === 'ready') return;
          var error = new Error(ticket && ticket.error ? ticket.error.message : 'Marbling Typeの字形を準備中です。キャンバスの準備完了を待ってください。');
          error.code = ticket && ticket.status === 'error' ? 'MARBLING_ERROR' : 'MARBLING_PENDING'; throw error;
        });
      }

function marblingShape(compiled, settings, phase, tolerance, sourceKey) {
        // Keep destination LODs independent: a high-resolution export must not
        // replace the preview's tessellation and change its paused pixels.
        var key = JSON.stringify([marblingFontRevision, sourceKey, settings, settings.motion ? marblingPhase(phase) : 0, tolerance]);
        var cached = marblingCache.get(key);
        if (cached && cached.compiled === compiled && cached.shape.tolerance <= tolerance) {
          marblingCache.delete(key); marblingCache.set(key, cached); return cached.shape;
        }
        var shape = MarblingType.renderMarblingLod(compiled, settings, phase, { tolerance: tolerance, maxPoints: 262144 });
        if (cached) { marblingCachePoints -= cached.shape.pointCount; marblingCache.delete(key); }
        while (marblingCache.size && marblingCachePoints + shape.pointCount > 262144) {
          var oldest = marblingCache.keys().next().value; marblingCachePoints -= marblingCache.get(oldest).shape.pointCount; marblingCache.delete(oldest);
        }
        marblingCache.set(key, { compiled: compiled, shape: shape, sourceKey: sourceKey }); marblingCachePoints += shape.pointCount;
        return shape;
      }

function marblingEffectPad(glyphs, phase) {
        var extra = 0; if (surfaceOutputOpacity('marblingType') <= .002) return 0;
        glyphs.forEach(function (g) {
          var settings = marblingGlyphSettings(g);
          if (surfaceGlyphStrength(g, 'marblingType') <= .002 || !String(g.ch || '').trim() || g.opacity === 0 || settings.amount === 0) return;
          var request = marblingSource(g.ch,g), ticket = marblingPool.peek(request); if (!ticket || ticket.status !== 'ready') return;
          try {
            var compiled = ticket.result, fm = compiled.glyph.nativeMetrics;
            var shape = marblingShape(compiled, settings, marblingPhase(phase), .08, request.key), extent = 0;
            shape.rings.forEach(function (ring) { ring.points.forEach(function (p) {
              extent = Math.max(extent, -p.x, p.x - fm.advance, -p.y - fm.ascent, p.y - fm.descent);
            }); });
            var stretch = Math.max(Math.abs(g.scaleX == null ? 1 : g.scaleX), Math.abs(g.scaleY == null ? 1 : g.scaleY));
            var shear = 1 + Math.abs(Math.tan((g.skewX || 0) * Math.PI / 180)) + Math.abs(Math.tan((g.skewY || 0) * Math.PI / 180));
            extra = Math.max(extra, (extent + .08) * params.fontSize / 192 * stretch * shear * Math.SQRT2);
          } catch (error) { marblingRenderError = error.message; marblingUpdateStatus(); }
        });
        return extra;
      }

function marblingTraceRings(ctx, rings) {
        rings.forEach(function (ring) {
          var points = ring.points; if (!points.length) return;
          ctx.moveTo(points[0].x, points[0].y);
          for (var i = 1; i < points.length; i++) ctx.lineTo(points[i].x, points[i].y); ctx.closePath();
        });
      }

function renderMarblingTypeDirect(ctx, glyphs, pixelScale, L, fm, livePreview) {
        if (surfaceOutputOpacity('marblingType') <= .002) return;
        var color = surfaceEffectColor('marblingType'), phase = marblingPhase(), groups = new Map(), items = [], usedPoints = 0;
        marblingRenderError = '';
        glyphs.forEach(function (g) {
          var strength = surfaceGlyphStrength(g, 'marblingType');
          if (strength <= .002 || !String(g.ch || '').trim() || g.opacity === 0) return;
          var settings = marblingGlyphSettings(g), entry = { g: g, strength: strength, native: settings.amount === 0, group: null, error: null };
          items.push(entry); if (entry.native) return;
          var source = marblingSource(g.ch,g), ticket = marblingPool.peek(source);
          if (!ticket || ticket.status !== 'ready') {
            entry.error = Object.assign(new Error(ticket && ticket.error ? ticket.error.message : 'Marbling Typeの字形を準備中です。'),
              { code: ticket && ticket.status === 'error' ? 'MARBLING_ERROR' : 'MARBLING_PENDING' });
            if (!livePreview) throw entry.error; return;
          }
          ctx.save();
          try {
            spectralGlyphFrame(ctx, g, pixelScale, L, fm, ticket.result.glyph.nativeMetrics);
            var tolerance = MarblingType.marblingPixelTolerance(globalThis.TypeDeformerRenderContext?TypeDeformerRenderContext.physical(ctx.canvas).getContext('2d').getTransform():ctx.getTransform(), .2), key = JSON.stringify([source.key, settings]);
            var group = groups.get(key);
            if (!group) { group = { compiled: ticket.result, sourceKey: source.key, settings: settings, tolerance: tolerance, generated: false, shape: null, error: null, remaining: 0 }; groups.set(key, group); }
            group.tolerance = Math.min(group.tolerance, tolerance); group.remaining++; entry.group = group;
          } catch (error) { entry.error = error; if (!livePreview) throw error; }
          finally { ctx.restore(); }
        });
        items.forEach(function (entry) {
          var group = entry.group;
          if (group && !group.generated) {
            group.generated = true;
            try {
              group.shape = marblingShape(group.compiled, group.settings, phase, group.tolerance, group.sourceKey);
              usedPoints += group.shape.pointCount;
              if (usedPoints > 262144) throw new RangeError('Marbling Typeの1フレームの輪郭上限です。文字種・設定の種類を減らしてください。');
              if (typeof Path2D === 'function') { group.path = new Path2D(); marblingTraceRings(group.path, group.shape.rings); }
            } catch (error) { group.shape = null; group.error = error; }
          }
          var error = entry.error || (group && group.error); if (error && !livePreview) throw error;
          if (error && error.code !== 'MARBLING_PENDING' && !marblingRenderError) marblingRenderError = error.message;
          if (entry.native || !group || error) drawSurfaceGlyph(ctx, entry.g, pixelScale, L, fm, entry.strength, color);
          else {
            ctx.save();
            try {
              spectralGlyphFrame(ctx, entry.g, pixelScale, L, fm, group.compiled.glyph.nativeMetrics);
              ctx.globalAlpha = entry.strength * (entry.g.opacity == null ? 1 : entry.g.opacity); ctx.fillStyle = color;
              if (group.path) ctx.fill(group.path); else { ctx.beginPath(); marblingTraceRings(ctx, group.shape.rings); ctx.fill(); }
            } finally { ctx.restore(); }
          }
          if (group && --group.remaining === 0) { group.shape = null; group.path = null; }
        });
        marblingUpdateStatus();
      }

function renderAsemicDuctus(targetCtx, glyphs, width, height, pixelScale, L, fm, coverBase) {
        var defaultGrammar = surfaceChoice(glyphs, 'asemicDuctus', 'asemicGrammar', params.asemicGrammar, BATCH_PARAM_OPTIONS.asemicGrammar);
        var mask = buildSurfaceMask(glyphs, 'asemicDuctus', width, height, pixelScale, L, fm);
        compositeSurfaceSource(targetCtx, mask, 'asemicDuctus', coverBase);
        var source;
        try { source = mask.getContext('2d', { willReadFrequently: true }).getImageData(0, 0, width, height); }
        catch (error) { return; }
        var field = surfaceBoundaryDistance(source.data, width, height);
        if (!field.bounds) return;
        var metas = surfaceGlyphAnatomyMetas(glyphs, 'asemicDuctus', pixelScale, L);
        if (!metas.length) return;
        var topology = surfaceVoidTopology(field, width, height);
        var phase = compositionState.enabled ? (((compositionState.phase % 1) + 1) % 1) * Math.PI * 2 : 0;
        var work = surfaceScratch('asemic-ductus-v37-body', width, height, false);
        // Finish the opaque writing and its cuts in an isolated glyph group.
        // One reused layer, not an allocation or full-frame copy per character.
        var glyphWork = surfaceScratch('asemic-ductus-v37-glyph', width, height, false);
        var ctx = glyphWork.ctx;
        ctx.fillStyle = '#fff';
        ctx.strokeStyle = '#fff';
        for (var metaIndex = 0; metaIndex < metas.length; metaIndex++) {
          var meta = metas[metaIndex];
          var glyphText = String(meta.glyph.ch || '');
          // Whitespace carries rhythm and advance width but has no writing
          // body. Never synthesize a fallback mark into an intentional gap.
          if (!glyphText.trim()) continue;
          var codepoint = glyphText.codePointAt ? (glyphText.codePointAt(0) || 0) : (glyphText.charCodeAt(0) || 0);
          var seed = codepoint + params.seed * 131 + meta.index * 977;
          var grammar = asemicMetaChoice(meta, 'asemicGrammar', defaultGrammar, BATCH_PARAM_OPTIONS.asemicGrammar);
          var memory = asemicMetaParam(meta, 'asemicMemory', params.asemicMemory, 0, 1, false);
          var gestures = asemicMetaParam(meta, 'asemicGestures', params.asemicGestures, 2, 16, true);
          if (metas.length > 96) gestures = Math.min(gestures, 6);
          var weight = asemicMetaParam(meta, 'asemicWeight', params.asemicWeight, 0.4, 64, false) * pixelScale * L.s;
          var flow = asemicMetaParam(meta, 'asemicFlow', params.asemicFlow, -4, 4, false);
          var contrast = asemicMetaParam(meta, 'asemicContrast', params.asemicContrast, 0, 6, false);
          var flourish = asemicMetaParam(meta, 'asemicFlourish', params.asemicFlourish, 0, 480, false) * pixelScale * L.s;
          var counterMemory = asemicMetaParam(meta, 'asemicCounter', params.asemicCounter, 0, 1.5, false);
          var counters = asemicCounterComponents(topology, metas, meta);
          var anchors = asemicBuildAnchors(field, width, height, metas, meta, grammar,
            memory, gestures, flow, flourish, phase, seed);
          if (anchors.length < 2) continue;
          var inkBounds = asemicBodyBounds(anchors, counters, meta, weight, contrast, flow, grammar, width, height);
          if (!inkBounds) continue;
          ctx.clearRect(inkBounds.x, inkBounds.y, inkBounds.width, inkBounds.height);
          ctx.save();
          ctx.globalAlpha = 1;
          if (grammar === 'chamber') {
            asemicRenderChamber(ctx, anchors, counters, meta, weight, contrast, flow, phase, seed);
          } else if (grammar === 'polyphonic') {
            var voiceGap = weight * (1.08 + contrast * 0.055);
            asemicRenderPath(ctx, asemicOffsetPath(anchors, -voiceGap), weight, contrast, flow,
              phase, seed + 7, grammar, false, 0.58);
            asemicRenderPath(ctx, anchors, weight, contrast, flow, phase, seed + 11, grammar, false, 0.72);
            asemicRenderPath(ctx, asemicOffsetPath(anchors, voiceGap), weight, contrast, flow,
              phase, seed + 13, grammar, false, 0.58);
            ctx.lineWidth = Math.max(0.5, weight * 0.34);
            ctx.lineCap = 'round';
            for (var bridge = 1; bridge + 1 < anchors.length; bridge += 3) {
              var bridgeA = asemicOffsetPath([anchors[Math.max(0, bridge - 1)], anchors[bridge], anchors[Math.min(anchors.length - 1, bridge + 1)]], -voiceGap)[1];
              var bridgeB = asemicOffsetPath([anchors[Math.max(0, bridge - 1)], anchors[bridge], anchors[Math.min(anchors.length - 1, bridge + 1)]], voiceGap)[1];
              ctx.beginPath(); ctx.moveTo(bridgeA.x, bridgeA.y); ctx.lineTo(bridgeB.x, bridgeB.y); ctx.stroke();
            }
          } else {
            asemicRenderPath(ctx, anchors, weight, contrast, flow, phase, seed, grammar, false, 1);
          }
          asemicCutCounters(ctx, counters, meta, counterMemory, weight, grammar, flow, seed);
          ctx.restore();
          work.ctx.save();
          work.ctx.globalAlpha = meta.strength;
          work.ctx.drawImage(glyphWork.canvas, inkBounds.x, inkBounds.y, inkBounds.width, inkBounds.height,
            inkBounds.x, inkBounds.y, inkBounds.width, inkBounds.height);
          work.ctx.restore();
        }
        paintSurfaceMask(targetCtx, work.canvas, surfaceEffectColor('asemicDuctus'), 1, 'asemic-ductus-v37-color');
      }

function renderSinewTorqueLegacy(targetCtx, glyphs, width, height, pixelScale, L, fm, coverBase) {
        var pullAggregate = surfaceAggregate(glyphs, 'sinewTorque', 'sinewPull', params.sinewPull);
        if (!pullAggregate.weight) return;
        var globalStrength = pullAggregate.strength;
        var pull = pullAggregate.value * pixelScale * L.s * globalStrength;
        var torque = surfaceAggregate(glyphs, 'sinewTorque', 'sinewTorqueAmount', params.sinewTorque).value * Math.PI / 180 * globalStrength;
        var tension = surfaceAggregate(glyphs, 'sinewTorque', 'sinewTension', params.sinewTension).value;
        var waistAmount = surfaceAggregate(glyphs, 'sinewTorque', 'sinewWaist', params.sinewWaist).value * globalStrength;
        var axis = surfaceAggregate(glyphs, 'sinewTorque', 'sinewAxis', params.sinewAxis).value * Math.PI / 180;
        var mask = buildSurfaceMask(glyphs, 'sinewTorque', width, height, pixelScale, L, fm);
        compositeSurfaceSource(targetCtx, mask, 'sinewTorque', coverBase);
        var sourceCtx = mask.getContext('2d', { willReadFrequently: true });
        var source;
        try { source = sourceCtx.getImageData(0, 0, width, height); }
        catch (error) { return; }
        var field = surfaceBoundaryDistance(source.data, width, height);
        if (!field.bounds) return;

        var centerX = (field.bounds[0] + field.bounds[2]) * 0.5;
        var centerY = (field.bounds[1] + field.bounds[3]) * 0.5;
        var cosAxis = Math.cos(axis), sinAxis = Math.sin(axis);
        var corners = [
          [field.bounds[0], field.bounds[1]], [field.bounds[2], field.bounds[1]],
          [field.bounds[2], field.bounds[3]], [field.bounds[0], field.bounds[3]]
        ];
        var halfU = 2, halfV = 2;
        for (var cornerIndex = 0; cornerIndex < corners.length; cornerIndex++) {
          var cornerDx = corners[cornerIndex][0] - centerX;
          var cornerDy = corners[cornerIndex][1] - centerY;
          halfU = Math.max(halfU, Math.abs(cornerDx * cosAxis + cornerDy * sinAxis));
          halfV = Math.max(halfV, Math.abs(-cornerDx * sinAxis + cornerDy * cosAxis));
        }
        // The inverse deformation decides whether a destination pixel belongs
        // to the body. A predicted reach rectangle is not a valid clipping
        // boundary: extreme pull/torque can fold real output beyond it.
        var minX = 0, maxX = width - 1, minY = 0, maxY = height - 1;
        var warped = surfaceScratch('sinew-torque-mask', width, height, true);
        var output = warped.ctx.createImageData(width, height);
        var exponent = 0.45 + (globalThis.TypeDeformerParameters ? globalThis.TypeDeformerParameters.normalize('sinewTension', tension, 0, 0.1, 8) : Math.max(0.1, Math.min(8, tension))) * 0.32;

        function sourceAlphaAt(x, y) {
          if (x < 0 || y < 0 || x >= width - 1 || y >= height - 1) return 0;
          var x0 = Math.floor(x), y0 = Math.floor(y);
          var tx = x - x0, ty = y - y0;
          var i00 = (y0 * width + x0) * 4 + 3;
          var i10 = i00 + 4;
          var i01 = i00 + width * 4;
          var i11 = i01 + 4;
          var top = source.data[i00] * (1 - tx) + source.data[i10] * tx;
          var bottom = source.data[i01] * (1 - tx) + source.data[i11] * tx;
          return top * (1 - ty) + bottom * ty;
        }

        for (var y = minY; y <= maxY; y++) for (var x = minX; x <= maxX; x++) {
          var dx = x - centerX, dy = y - centerY;
          var destinationU = dx * cosAxis + dy * sinAxis;
          var destinationV = -dx * sinAxis + dy * cosAxis;
          var sourceU = destinationU, sourceV = destinationV;
          var q = Math.max(-1.5, Math.min(1.5, sourceV / halfV));
          for (var solve = 0; solve < 4; solve++) {
            var flex = (q < 0 ? -1 : 1) * Math.pow(Math.abs(q), exponent);
            var localAngle = torque * flex * 0.5;
            var bend = pull * (q * 0.35 + Math.sin(q * Math.PI * 0.5) * 0.65);
            var shiftedU = destinationU - bend;
            var cosLocal = Math.cos(localAngle), sinLocal = Math.sin(localAngle);
            var scaledU = shiftedU * cosLocal + destinationV * sinLocal;
            sourceV = -shiftedU * sinLocal + destinationV * cosLocal;
            q = Math.max(-1.5, Math.min(1.5, sourceV / halfV));
            var centerPressure = Math.max(0, 1 - Math.min(1, Math.abs(q)));
            var waistScale = Math.max(0.08, Math.min(5, Math.exp(waistAmount * centerPressure * 0.42)));
            sourceU = scaledU / waistScale;
          }
          var sourceX = centerX + sourceU * cosAxis - sourceV * sinAxis;
          var sourceY = centerY + sourceU * sinAxis + sourceV * cosAxis;
          var alpha = sourceAlphaAt(sourceX, sourceY);
          if (alpha < 0.5) continue;
          var outputIndex = (y * width + x) * 4;
          output.data[outputIndex] = 255;
          output.data[outputIndex + 1] = 255;
          output.data[outputIndex + 2] = 255;
          output.data[outputIndex + 3] = Math.max(0, Math.min(255, Math.round(alpha)));
        }

        // Output pixels sample the fixed geometry and original glyph raster.
        if(globalThis.TypeDeformerRenderContext){var RC=globalThis.TypeDeformerRenderContext;function outputSourceAlphaAt(x,y){return RC.rasterSample(source.data,width,height,x,y,3);}RC.field(output,function(px,py,rgba){var x=px-.5,y=py-.5;if(y<(minY)||!(y <= maxY))return;if(x<(minX)||!(x <= maxX))return;
          var dx = x - centerX, dy = y - centerY;
          var destinationU = dx * cosAxis + dy * sinAxis;
          var destinationV = -dx * sinAxis + dy * cosAxis;
          var sourceU = destinationU, sourceV = destinationV;
          var q = Math.max(-1.5, Math.min(1.5, sourceV / halfV));
          for (var solve = 0; solve < 4; solve++) {
            var flex = (q < 0 ? -1 : 1) * Math.pow(Math.abs(q), exponent);
            var localAngle = torque * flex * 0.5;
            var bend = pull * (q * 0.35 + Math.sin(q * Math.PI * 0.5) * 0.65);
            var shiftedU = destinationU - bend;
            var cosLocal = Math.cos(localAngle), sinLocal = Math.sin(localAngle);
            var scaledU = shiftedU * cosLocal + destinationV * sinLocal;
            sourceV = -shiftedU * sinLocal + destinationV * cosLocal;
            q = Math.max(-1.5, Math.min(1.5, sourceV / halfV));
            var centerPressure = Math.max(0, 1 - Math.min(1, Math.abs(q)));
            var waistScale = Math.max(0.08, Math.min(5, Math.exp(waistAmount * centerPressure * 0.42)));
            sourceU = scaledU / waistScale;
          }
          var sourceX = centerX + sourceU * cosAxis - sourceV * sinAxis;
          var sourceY = centerY + sourceU * sinAxis + sourceV * cosAxis;
          var alpha = outputSourceAlphaAt(sourceX, sourceY);
          if (alpha < 0.5) return;
          var outputIndex = (y * width + x) * 4;
          rgba[0] = 255;
          rgba[1] = 255;
          rgba[2] = 255;
          rgba[3] = Math.max(0, Math.min(255, Math.round(alpha)));

        });}
        warped.ctx.putImageData(output, 0, 0);
        paintSurfaceMask(targetCtx, warped.canvas, surfaceEffectColor('sinewTorque'), 1, 'sinew-torque-color');
      }

function renderSinewTorque(targetCtx, glyphs, width, height, pixelScale, L, fm, coverBase) {
        var system = surfaceChoice(glyphs, 'sinewTorque', 'sinewSystem', params.sinewSystem,
          BATCH_PARAM_OPTIONS.sinewSystem);
        if (system === 'legacy') {
          renderSinewTorqueLegacy(targetCtx, glyphs, width, height, pixelScale, L, fm, coverBase);
          return;
        }
        // Each typographic line becomes one continuous tissue body. Treating
        // every line as one global muscle made a lower line fold through the
        // line above it; per-line fields keep readable baselines while still
        // coupling all glyphs within the line through one pennation system.
        var lineGroups = Object.create(null);
        var lineOrder = [];
        for (var glyphIndex = 0; glyphIndex < glyphs.length; glyphIndex++) {
          var glyph = glyphs[glyphIndex];
          var lineKey = glyph.line != null ? String(glyph.line)
            : String(Math.round(((glyph.by != null ? glyph.by : glyph.y) || 0) / Math.max(1, fm.ascent + fm.descent)));
          if (!lineGroups[lineKey]) {
            lineGroups[lineKey] = [];
            lineOrder.push(lineKey);
          }
          lineGroups[lineKey].push(glyph);
        }
        for (var lineIndex = 0; lineIndex < lineOrder.length; lineIndex++) {
          renderSinewTorqueField(targetCtx, lineGroups[lineOrder[lineIndex]], width, height,
            pixelScale, L, fm, coverBase, system);
        }
      }

function renderSinewTorqueField(targetCtx, glyphs, width, height, pixelScale, L, fm, coverBase, system) {
        var pullAggregate = surfaceAggregate(glyphs, 'sinewTorque', 'sinewPull', params.sinewPull);
        if (!pullAggregate.weight) return;
        var globalStrength = pullAggregate.strength;
        var pull = pullAggregate.value * pixelScale * L.s * globalStrength;
        var torque = surfaceAggregate(glyphs, 'sinewTorque', 'sinewTorqueAmount', params.sinewTorque).value
          * Math.PI / 180 * globalStrength;
        var tension = (globalThis.TypeDeformerParameters ? globalThis.TypeDeformerParameters.normalize('sinewTension', surfaceAggregate(glyphs, 'sinewTorque', 'sinewTension', params.sinewTension).value, 0, 0.1, 8) : Math.max(0.1, Math.min(8,
          surfaceAggregate(glyphs, 'sinewTorque', 'sinewTension', params.sinewTension).value)));
        var waistAmount = surfaceAggregate(glyphs, 'sinewTorque', 'sinewWaist', params.sinewWaist).value
          * globalStrength;
        var axis = surfaceAggregate(glyphs, 'sinewTorque', 'sinewAxis', params.sinewAxis).value
          * Math.PI / 180;
        var mask = buildSurfaceMask(glyphs, 'sinewTorque', width, height, pixelScale, L, fm);
        compositeSurfaceSource(targetCtx, mask, 'sinewTorque', coverBase);
        // Fascia joins neighbouring glyph bodies before deformation. These
        // short internal bridges turn a row into one muscle architecture while
        // retaining the original counters and most inter-letter air.
        var tissueMask = surfaceScratch('sinew-connected-source', width, height, false);
        tissueMask.ctx.drawImage(mask, 0, 0);
        var orderedGlyphs = glyphs.slice().sort(function (a, b) {
          var ax = a.x != null ? a.x : a.bx;
          var bx = b.x != null ? b.x : b.bx;
          return ax - bx;
        });
        tissueMask.ctx.strokeStyle = '#fff';
        tissueMask.ctx.lineCap = 'round';
        tissueMask.ctx.lineJoin = 'round';
        tissueMask.ctx.lineWidth = Math.max(1.2,
          (fm.ascent + fm.descent) * pixelScale * L.s * (0.026 + tension * 0.004));
        for (var bridgeIndex = 0; bridgeIndex < orderedGlyphs.length - 1; bridgeIndex++) {
          var bridgeLevel = 0.2 + hash(bridgeIndex, params.seed % 97, 2251) * 0.6;
          var bridgeCounterLevel = Math.max(0.14, Math.min(0.86, bridgeLevel
            + (hash(bridgeIndex, params.seed % 89, 2257) - 0.5) * 0.22));
          var bridgeA = surfaceGlyphNode(orderedGlyphs[bridgeIndex], 'sinewTorque', pixelScale, L,
            0, 0, 0, bridgeIndex, 0.8, bridgeLevel, 1);
          var bridgeB = surfaceGlyphNode(orderedGlyphs[bridgeIndex + 1], 'sinewTorque', pixelScale, L,
            0, 0, 0, bridgeIndex + 1, 0.2, bridgeCounterLevel, 1);
          var bridgeDx = bridgeB.x - bridgeA.x;
          var bridgeDy = bridgeB.y - bridgeA.y;
          var bridgeLength = Math.sqrt(bridgeDx * bridgeDx + bridgeDy * bridgeDy);
          var bridgeLimit = Math.max(20, (fm.ascent + fm.descent) * pixelScale * L.s * 1.18);
          if (bridgeLength > bridgeLimit) continue;
          var bridgeLift = (hash(bridgeIndex, params.seed % 97, 2267) - 0.5)
            * tissueMask.ctx.lineWidth * (system === 'braid' ? 3.4 : 2.2);
          tissueMask.ctx.beginPath();
          tissueMask.ctx.moveTo(bridgeA.x, bridgeA.y);
          tissueMask.ctx.quadraticCurveTo((bridgeA.x + bridgeB.x) * 0.5,
            (bridgeA.y + bridgeB.y) * 0.5 + bridgeLift, bridgeB.x, bridgeB.y);
          tissueMask.ctx.stroke();
        }
        var sourceCtx = tissueMask.ctx;
        var source;
        try { source = sourceCtx.getImageData(0, 0, width, height); }
        catch (error) { return; }
        var field = surfaceBoundaryDistance(source.data, width, height);
        if (!field.bounds) return;

        var centerX = (field.bounds[0] + field.bounds[2]) * 0.5;
        var centerY = (field.bounds[1] + field.bounds[3]) * 0.5;
        var cosAxis = Math.cos(axis), sinAxis = Math.sin(axis);
        var corners = [
          [field.bounds[0], field.bounds[1]], [field.bounds[2], field.bounds[1]],
          [field.bounds[2], field.bounds[3]], [field.bounds[0], field.bounds[3]]
        ];
        var halfU = 2, halfV = 2;
        for (var cornerIndex = 0; cornerIndex < corners.length; cornerIndex++) {
          var cornerDx = corners[cornerIndex][0] - centerX;
          var cornerDy = corners[cornerIndex][1] - centerY;
          halfU = Math.max(halfU, Math.abs(cornerDx * cosAxis + cornerDy * sinAxis));
          halfV = Math.max(halfV, Math.abs(-cornerDx * sinAxis + cornerDy * cosAxis));
        }
        var phase = compositionState.enabled
          ? (((compositionState.phase % 1) + 1) % 1) * Math.PI * 2 : 0;
        var exponent = 0.45 + tension * 0.32;

        function deformationAt(u, v, family) {
          var q = Math.max(-1.6, Math.min(1.6, v / halfV));
          var centerPressure = Math.max(0, 1 - Math.min(1, Math.abs(q)));
          var flex = (q < 0 ? -1 : 1) * Math.pow(Math.abs(q), exponent);
          var angle = 0, bend = 0, scaleU = 1, scaleV = 1, transverse = 0;
          if (system === 'torsion') {
            angle = torque * (flex * 0.68 + Math.sin(q * Math.PI) * 0.32);
            bend = pull * (q * 0.3 + Math.sin(q * Math.PI * 0.5) * 0.7);
            scaleU = Math.exp(waistAmount * centerPressure * 0.36);
          } else if (system === 'fascicle') {
            var pennation = Math.sin(q * Math.PI * (1.15 + tension * 0.05) + phase * 0.16);
            angle = torque * (0.13 + tension * 0.018) * pennation;
            bend = pull * (q * 0.18 + Math.sin(q * Math.PI * 0.7) * 0.82);
            scaleU = Math.exp(waistAmount * centerPressure * 0.28);
            transverse = Math.sin((u / halfU) * Math.PI * (1.2 + tension * 0.16)
              + q * 2.2 + phase * 0.2 + (family || 0) * Math.PI) * halfV * 0.035 * tension;
          } else if (system === 'compression') {
            scaleU = Math.exp(waistAmount * centerPressure * 0.52)
              * (1 + Math.max(0, tension - 1) * centerPressure * 0.055);
            scaleV = Math.max(0.24, 1 - Math.min(0.62, Math.abs(pull) / Math.max(20, halfV * 3.2))
              * centerPressure);
            bend = pull * 0.16 * Math.sin(q * Math.PI);
            angle = torque * 0.08 * Math.sin(q * Math.PI * 2);
          } else {
            var braidSign = family ? -1 : 1;
            angle = torque * braidSign * (0.19 + tension * 0.025)
              * Math.sin(q * Math.PI * (2.15 + tension * 0.06) + phase * 0.18 + (family || 0) * Math.PI);
            bend = pull * (0.26 * q + 0.74 * Math.sin(q * Math.PI * 1.35 + (family || 0) * Math.PI));
            scaleU = Math.exp(waistAmount * centerPressure * 0.22);
            transverse = braidSign * Math.sin((u / halfU) * Math.PI * 1.7 + q * Math.PI * 2.4 + phase * 0.25)
              * halfV * (0.018 + tension * 0.006);
          }
          var scaledU = u * scaleU + bend;
          var scaledV = v * scaleV + transverse;
          var cosLocal = Math.cos(angle), sinLocal = Math.sin(angle);
          return {
            u: scaledU * cosLocal - scaledV * sinLocal,
            v: scaledU * sinLocal + scaledV * cosLocal
          };
        }

        function sourceAlphaAt(x, y) {
          if (x < 0 || y < 0 || x >= width - 1 || y >= height - 1) return 0;
          var x0 = Math.floor(x), y0 = Math.floor(y);
          var tx = x - x0, ty = y - y0;
          var i00 = (y0 * width + x0) * 4 + 3;
          var i10 = i00 + 4;
          var i01 = i00 + width * 4;
          var i11 = i01 + 4;
          var top = source.data[i00] * (1 - tx) + source.data[i10] * tx;
          var bottom = source.data[i01] * (1 - tx) + source.data[i11] * tx;
          return top * (1 - ty) + bottom * ty;
        }

        // Never turn a performance estimate into an artwork boundary. Scan the
        // complete analysis surface; the shared render envelope grows it when
        // real pixels reach an edge.
        var minX = 0, maxX = width - 1, minY = 0, maxY = height - 1;
        var warped = surfaceScratch('sinew-tissue-mask', width, height, true);
        var output = warped.ctx.createImageData(width, height);
        for (var y = minY; y <= maxY; y++) for (var x = minX; x <= maxX; x++) {
          var dx = x - centerX, dy = y - centerY;
          var destinationU = dx * cosAxis + dy * sinAxis;
          var destinationV = -dx * sinAxis + dy * cosAxis;
          var sourceU = destinationU, sourceV = destinationV;
          // Invert the nonlinear tissue field by fixed-point correction. This
          // keeps the contour continuous even under extreme pull/torque.
          for (var solve = 0; solve < 7; solve++) {
            var mapped = deformationAt(sourceU, sourceV, 0);
            sourceU += (destinationU - mapped.u) * 0.66;
            sourceV += (destinationV - mapped.v) * 0.66;
          }
          var sourceX = centerX + sourceU * cosAxis - sourceV * sinAxis;
          var sourceY = centerY + sourceU * sinAxis + sourceV * cosAxis;
          var alpha = sourceAlphaAt(sourceX, sourceY);
          if (alpha < 0.5) continue;
          var outputIndex = (y * width + x) * 4;
          output.data[outputIndex] = output.data[outputIndex + 1] = output.data[outputIndex + 2] = 255;
          output.data[outputIndex + 3] = Math.max(0, Math.min(255, Math.round(alpha)));
        }

        // Output pixels sample the fixed geometry and original glyph raster.
        if(globalThis.TypeDeformerRenderContext){var RC=globalThis.TypeDeformerRenderContext;function outputSourceAlphaAt(x,y){return RC.rasterSample(source.data,width,height,x,y,3);}RC.field(output,function(px,py,rgba){var x=px-.5,y=py-.5;if(y<(minY)||!(y <= maxY))return;if(x<(minX)||!(x <= maxX))return;
          var dx = x - centerX, dy = y - centerY;
          var destinationU = dx * cosAxis + dy * sinAxis;
          var destinationV = -dx * sinAxis + dy * cosAxis;
          var sourceU = destinationU, sourceV = destinationV;
          // Invert the nonlinear tissue field by fixed-point correction. This
          // keeps the contour continuous even under extreme pull/torque.
          for (var solve = 0; solve < 7; solve++) {
            var mapped = deformationAt(sourceU, sourceV, 0);
            sourceU += (destinationU - mapped.u) * 0.66;
            sourceV += (destinationV - mapped.v) * 0.66;
          }
          var sourceX = centerX + sourceU * cosAxis - sourceV * sinAxis;
          var sourceY = centerY + sourceU * sinAxis + sourceV * cosAxis;
          var alpha = outputSourceAlphaAt(sourceX, sourceY);
          if (alpha < 0.5) return;
          var outputIndex = (y * width + x) * 4;
          rgba[0] = rgba[1] = rgba[2] = 255;
          rgba[3] = Math.max(0, Math.min(255, Math.round(alpha)));

        });}
        warped.ctx.putImageData(output, 0, 0);

        var fibers = surfaceScratch('sinew-fascicle-grooves', width, height, false);
        var fiberCtx = fibers.ctx;
        fiberCtx.strokeStyle = '#fff';
        fiberCtx.lineCap = 'round';
        fiberCtx.lineJoin = 'round';
        var fiberCount = Math.min(86, Math.max(10, Math.round(13 + tension * 7 + Math.abs(waistAmount) * 3)));
        var sampleCount = 32;
        function worldPoint(localU, localV, family) {
          var deformed = deformationAt(localU, localV, family);
          return {
            x: centerX + deformed.u * cosAxis - deformed.v * sinAxis,
            y: centerY + deformed.u * sinAxis + deformed.v * cosAxis
          };
        }
        function strokeFiber(localV, family, alphaScale, widthScale) {
          fiberCtx.globalAlpha = alphaScale;
          fiberCtx.lineWidth = Math.max(0.38, pixelScale * L.s * widthScale);
          fiberCtx.beginPath();
          for (var sample = 0; sample <= sampleCount; sample++) {
            var u = -halfU * 1.15 + halfU * 2.3 * sample / sampleCount;
            var ripple = Math.sin(sample / sampleCount * Math.PI * (1.8 + tension * 0.08)
              + localV / halfV * 2.3 + phase * 0.18 + family * Math.PI) * halfV * 0.018 * tension;
            var point = worldPoint(u, localV + ripple, family);
            if (sample === 0) fiberCtx.moveTo(point.x, point.y);
            else fiberCtx.lineTo(point.x, point.y);
          }
          fiberCtx.stroke();
        }
        for (var fiberIndex = 0; fiberIndex < fiberCount; fiberIndex++) {
          var localV = -halfV + halfV * 2 * (fiberIndex + 0.5) / fiberCount;
          var centerBias = Math.max(0, 1 - Math.abs(localV / halfV));
          strokeFiber(localV, 0, (0.16 + centerBias * 0.38) * globalStrength,
            0.58 + tension * 0.055);
          if (system === 'braid') {
            strokeFiber(localV + halfV / fiberCount * 0.48, 1,
              (0.11 + centerBias * 0.31) * globalStrength, 0.46 + tension * 0.045);
          }
        }

        if (system === 'compression') {
          var ringCount = Math.min(28, Math.max(5, Math.round(5 + tension * 2.5)));
          for (var ringIndex = 0; ringIndex < ringCount; ringIndex++) {
            var ringU = -halfU + halfU * 2 * (ringIndex + 0.5) / ringCount;
            fiberCtx.globalAlpha = globalStrength * (0.12 + 0.24 * Math.sin((ringIndex + 1) / (ringCount + 1) * Math.PI));
            fiberCtx.lineWidth = Math.max(0.4, pixelScale * L.s * (0.42 + tension * 0.035));
            fiberCtx.beginPath();
            for (var ringSample = 0; ringSample <= 24; ringSample++) {
              var ringV = -halfV * 1.05 + halfV * 2.1 * ringSample / 24;
              var ringPoint = worldPoint(ringU + Math.sin(ringV / halfV * Math.PI) * halfU * 0.025 * tension,
                ringV, 0);
              if (ringSample === 0) fiberCtx.moveTo(ringPoint.x, ringPoint.y);
              else fiberCtx.lineTo(ringPoint.x, ringPoint.y);
            }
            fiberCtx.stroke();
          }
        }
        fiberCtx.globalAlpha = 1;
        fiberCtx.globalCompositeOperation = 'destination-in';
        fiberCtx.drawImage(warped.canvas, 0, 0);
        fiberCtx.globalCompositeOperation = 'source-over';
        warped.ctx.save();
        warped.ctx.globalCompositeOperation = 'destination-out';
        warped.ctx.globalAlpha = system === 'fascicle' ? 0.34 : system === 'braid' ? 0.4 : 0.28;
        warped.ctx.drawImage(fibers.canvas, 0, 0);
        warped.ctx.restore();

        // A longitudinal aponeurosis makes the field legible as one anatomical
        // body, not a texture placed over independent glyphs.
        var seam = surfaceScratch('sinew-aponeurosis', width, height, false);
        seam.ctx.strokeStyle = '#fff';
        seam.ctx.lineCap = 'round';
        seam.ctx.lineJoin = 'round';
        seam.ctx.globalAlpha = Math.min(0.64, 0.24 + tension * 0.045) * globalStrength;
        seam.ctx.lineWidth = Math.max(0.7, pixelScale * L.s * (0.9 + tension * 0.18));
        seam.ctx.beginPath();
        for (var seamSample = 0; seamSample <= 48; seamSample++) {
          var seamU = -halfU * 1.05 + halfU * 2.1 * seamSample / 48;
          var seamV = Math.sin(seamU / halfU * Math.PI * 1.35 + phase * 0.12)
            * halfV * 0.036 * tension;
          var seamPoint = worldPoint(seamU, seamV, 0);
          if (seamSample === 0) seam.ctx.moveTo(seamPoint.x, seamPoint.y);
          else seam.ctx.lineTo(seamPoint.x, seamPoint.y);
        }
        seam.ctx.stroke();
        seam.ctx.globalCompositeOperation = 'destination-in';
        seam.ctx.drawImage(warped.canvas, 0, 0);
        seam.ctx.globalCompositeOperation = 'source-over';
        warped.ctx.save();
        warped.ctx.globalCompositeOperation = 'destination-out';
        warped.ctx.globalAlpha = 0.24;
        warped.ctx.drawImage(seam.canvas, 0, 0);
        warped.ctx.restore();
        paintSurfaceMask(targetCtx, warped.canvas, surfaceEffectColor('sinewTorque'), 1,
          'sinew-tissue-color');
      }

function surfaceCanonicalRasterPlan(glyphs){
    var bounds=contentBounds(glyphs,Math.max(64,differentialEffectPad(glyphs),marblingEffectPad(glyphs,0)));
    if(globalThis.TypeDeformerRenderEnvelope)bounds=globalThis.TypeDeformerRenderEnvelope.expandBounds(bounds,typeof surfaceEnvelopeScale==='number'?surfaceEnvelopeScale:1);
    var density=Math.max(.000001,Math.min(1.5,1440/bounds.w,1440/bounds.h,Math.sqrt(2400000/Math.max(1,bounds.w*bounds.h))));
    return {bounds:bounds,density:density,width:Math.max(1,Math.ceil(bounds.w*density)),height:Math.max(1,Math.ceil(bounds.h*density)),layout:{w:bounds.w,h:bounds.h,s:1,dx:-bounds.x,dy:-bounds.y},envelopeScale:typeof surfaceEnvelopeScale==='number'?surfaceEnvelopeScale:1};
  }

function fieldMaterialRenderer(id) {
        return function(ctx, glyphs, width, height, pixelScale, L, fm) {
          return TypeDeformerFieldMaterials.render(id, ctx, glyphs, width, height, pixelScale, L, fm, {
            params: params, renderContext: globalThis.TypeDeformerRenderContext && globalThis.TypeDeformerRenderContext.current(), strength: surfaceGlyphStrength, color: surfaceEffectColor,
            scratch: surfaceScratch, drawGlyph: drawSurfaceGlyph, traceContours: spectralTraceContours, font: glyphFontSpec, charInfo: charInfo, bounds: contentBounds
          });
        };
      }

function contentBounds(glyphs, pad) {
        var minX = Infinity, minY = Infinity, maxX = -Infinity, maxY = -Infinity;
        for (var i = 0; i < glyphs.length; i++) {
          var g = glyphs[i];
          // transformed corners: p' = O + t + R·K·S·(p − O)
          var rad = (g.rot || 0) * Math.PI / 180;
          var cosR = Math.cos(rad), sinR = Math.sin(rad);
          var tanX = Math.tan((g.skewX || 0) * Math.PI / 180);
          var tanY = Math.tan((g.skewY || 0) * Math.PI / 180);
          var ma = cosR * g.scaleX - sinR * tanY * g.scaleX;
          var mb = sinR * g.scaleX + cosR * tanY * g.scaleX;
          var mc = cosR * tanX * g.scaleY - sinR * g.scaleY;
          var md = sinR * tanX * g.scaleY + cosR * g.scaleY;
          var bx = g.bx != null ? g.bx : g.x;
          var by = g.by != null ? g.by : g.y;
          var bw = g.bw != null ? g.bw : g.w;
          var bh = g.bh != null ? g.bh : g.h;
          var xs = [bx, bx + bw], ys = [by, by + bh];
          for (var a = 0; a < 2; a++) for (var b = 0; b < 2; b++) {
            var lx = xs[a] - g.ox;
            var ly = ys[b] - g.oy;
            var px = g.ox + g.tx + ma * lx + mc * ly;
            var py = g.oy + g.ty + mb * lx + md * ly;
            var offsets = [[0, 0]];
            if (Math.abs(g.misregX) > 0.001 || Math.abs(g.misregY) > 0.001) offsets.push([g.misregX, g.misregY], [-g.misregX * 0.65, -g.misregY * 0.65]);
            if (Math.abs(g.moshPassAX || 0) > 0.001 || Math.abs(g.moshPassAY || 0) > 0.001) offsets.push([g.moshPassAX, g.moshPassAY], [g.moshPassBX, g.moshPassBY]);
            for (var pass = 0; pass < offsets.length; pass++) {
              var passX = px + offsets[pass][0], passY = py + offsets[pass][1];
              if (passX < minX) minX = passX; if (passX > maxX) maxX = passX;
              if (passY < minY) minY = passY; if (passY > maxY) maxY = passY;
            }
          }
        }
        if (!glyphs.length) { minX = minY = 0; maxX = maxY = 100; }
        return { x: minX - pad, y: minY - pad, w: (maxX - minX) + pad * 2, h: (maxY - minY) + pad * 2 };
      }

function fontMetrics(){return workerFontMetrics;}

function baselineOffset(g, fm) {
        if (g.fontAxes) fm = fontMetrics(g);
        var leading = g.h - (fm.ascent + fm.descent);
        return leading / 2 + fm.ascent;
      }

function glyphFontSpec(g,size){return axisFieldEditor.font(g,size);}
var workerRenderers={asemicDuctus: renderAsemicDuctus,
differentialType: renderDifferentialType,
marblingType:function(ctx,glyphs,width,height,pixelScale,L,fm,coverBase,livePreview){return renderMarblingTypeDirect(ctx,glyphs,pixelScale,L,fm,livePreview);},
liquidRope: fieldMaterialRenderer('liquidRope'),
repulsiveCurves: fieldMaterialRenderer('repulsiveCurves'),
sinewTorque: renderSinewTorque};
